目录
项目定位与技术栈

数据流与模块划分

核心业务对象

排班规则与优先级

重点功能演进

性能优化与求解器演进

统计、导入导出与配置

架构耦合审查与修复

测试与验证策略

历史顽疾与踩坑

当前关键文件

验证命令

最新交接提示词

相关聊天记录来源

1. 项目定位与技术栈
   这是一个机场国际值机岗位自动排班工具，主要服务约 15 人、每天不超过 10 个航班的小规模排班场景。

技术栈：

TypeScript 5.9

Vite 7

Bootstrap 5

Vitest 4

xlsx-js-style

html2canvas

浏览器 localStorage

Web Worker

HiGHS WebAssembly

可选在线航班查询：flight-query-tool.vercel.app

应用特点：

纯前端、本地优先。

没有传统后端数据库。

配置、排班、历史主要保存在浏览器本地。

Excel 在浏览器内解析和生成。

自动排班后来放入 Web Worker，避免主页面卡死。

在线航班查询是唯一明确的远程 HTTP 数据源。

正式排班只计算当前选中日期，不提前计算未来多个班次。

行政支援模式、人工拖拽、人工锁定仍然保留。

2. 数据流与模块划分
   基本数据流
   text
   浏览器操作
   -> AutoScheduleApp / ApplicationCoordinator
   -> app actions / controllers
   -> domain 规则
   -> 修改内存 AppState
   -> saveState()
   -> localStorage
   -> UI 重新渲染
   自动排班
   text
   主页面
   -> Web Worker
   -> generateSchedule()
   -> 领域调度器 / daily-schedule-optimizer
   -> HiGHS 求解
   -> 返回进度与结果
   -> 主线程写入 assignments
   -> 保存并重绘
   Excel
   text
   浏览器读取 Excel
   -> ArrayBuffer
   -> 解析
   -> 合并进 AppState
   -> 保存
   在线航班查询
   text
   航班页
   -> flight-query-tool.vercel.app/api/flights
   -> 对账预览
   -> 人工确认
   -> 更新 state.flights
   目录职责
   src/domain/：排班、轮值、疲劳、规则、反馈等业务规则。

src/infrastructure/：localStorage、Excel、分享文件、在线查询、Web Worker、HiGHS 适配器。

src/ui/：页面渲染。

src/app/：应用动作、Store、控制器、命令编排。

src/model.ts：核心类型与 AppState。

.agents/skills/：项目级协作与开发 Skill。

docs/research/：调研文档。

clone/：参考开源排班/求解器项目，未纳入正式构建。

3. 核心业务对象
   人员
   常规人员

行政支援人员

分队长

状态：正常、休假、病休等

资质：

岗位资质

夜班能力

CX 航前资质

值班资质

备勤资质

行政支援模式

航班
航班模板

当日航班计划

预定人数

岗位规则

岗位启用旅客人数阈值

分流岗位提前撤岗

到岗时间、开始时间、结束时间、备注

排班结果
Excel 式“岗位 / 人员”双列。

支持拖拽、交换、清空。

支持临时岗位。

支持缩放。

支持行政支援模式。

支持引导复用。

支持机动督导兼任。

支持人工锁定与自动证据清理。

月度轮值
CX 航前：1 人

值班人员：1 人

备勤人员：2 人

值班默认高疲劳。

CX 航前需要资质。

值班和备勤可以人工调换。

支持值班备勤 Excel 导入导出。

备勤通常关联到次日休息日。

备勤资质开关：关闭后不参加自动、手工、Excel 备勤安排。

统计
月度轮值明细

月度轻松班次统计

TR121/H02 月度承担次数

末班重点岗位统计

末班申报、送资料分开统计

末班重点岗位次数人工修正

月度一键清零

月份切换

导入导出末班重点次数

人员 × 航班总览矩阵

历史
按日期分组。

只读航班双列表格。

最新日期默认展开。

上一休一，因此“归档并排后天”。

支持上一班对比。

4. 排班规则与优先级
   总原则
   合理排班不是让每个人每天疲劳点完全相同，而是让负荷在连续工作班之间动态互补：

上一班累的人，下一班优先更轻、更早结束。

上一班轻的人，下一班可以适当多承担。

重点岗位既要避免连续，又要在一个月内公平轮换。

无法兼顾时可以突破软规则，但必须说明原因。

岗位完整性优先于疲劳软保护。

硬约束永远优先。

硬约束
绝不能突破：

人员状态

岗位资质

夜班能力

真实时间冲突

每日工时上限

人工锁定

值班晚撤锁定

KE166 机动督导必要锁定

岗位完整性

严格岗位衔接中明确不可突破的部分

同航司同日控制/一号交叉互斥硬约束

软约束
可以优先避开，但无人替代时允许兜底：

高负荷恢复

滚动负荷

跨工作日恢复

同岗轮换

工时疲劳均衡

普通岗位连续限制

值班中间休息

同日同航司控制/一号之外的普通重点岗位分散

推荐优先级顺序
多次讨论后形成的明确顺序：

text
硬约束
-> 人工值班 / KE166 锁定
-> 岗位完整性
-> 严格岗位衔接
-> 下班次值班人员预休（后已删除）
-> 跨工作日恢复
-> 次班截止保护
-> 重点岗位连续轮岗
-> 普通衔接
-> 在岗覆盖
-> 重点岗位长期频率
-> 工时与疲劳均衡
-> 普通岗位第三次连续复核
-> 稳定排序
后经用户要求调整：

删除“下班次值班人员预休”制度。

末班重点岗位公平顺序固定为：督导 → 一号 → 申报 → 送资料。

值班优先级高于末班重点岗位次数均衡。

值班人员中间减负顺序：少班 → 避重点 → 低疲劳。

同日同航司控制/一号交叉互斥提升为硬约束。

航司 + 规范岗位统一轮换。

重点岗位
系统通用重点岗位关键词：

text
一号、申报、督导、控制、送资料
同日 CX 专用重点岗位曾额外包含：

text
G18、G20
后续规则演进为：

同一航空公司 + 同一规范岗位共同轮换。

备注优先于物理柜台名。

例如：

FD101/G08/申报

FD202/G17/申报
统一归入 FD/申报。

同一航司不同航班的一号、控制、督导、申报、送资料统一统计。

不同航司不合并。

不同岗位类别不合并。

重点岗位连续轮岗
规则：

判断键：同一“航班号 + 具体岗位”曾用于旧逻辑。

后续改为同一“航空公司 + 规范岗位”。

上一工作班已由同一人承担，本班应优先换人。

有安全替代时，不得连续第二班。

搜索顺序：

空闲合格人员直接接替

同航班两人交换

重叠航班两人交换

最多三人，后扩展到最多五人闭环

开放链

不得把连续问题转移给其他人。

无安全方案时保留岗位完整性，并反馈具体原因。

同日同航司控制/一号交叉互斥是硬约束：

早班控制人员不能去后续同航司控制或一号；

早班一号人员不能去后续同航司控制或一号。

跨工作日恢复
上一班末班重点岗位人员，下个工作班：

避开明确配置的早班目标。

避开末班重点岗位。

可配置次班截止时间，例如 12:00 后尽量不排航班。

自动值班可交换日期。

人工值班不自动修改，只反馈冲突。

次班截止按航班开始时间判断。

同一人命中多条规则时，取最早截止时间。

后续收窄为：只保护明确配置的次班避让目标、截止时间之后航班、下个工作班末班重点岗位；不能因上一班做过 TR/申报，就把人从所有早班或所有申报岗位排除。

值班规则
值班人员当天至少承担一个开始时间早于 12:00 的航班。

不再锁死 08:30 或某个具体柜台。

值班晚撤岗位继续锁定。

值班人员完成早班和晚撤目标后，可以补有资质、不冲突、不超工时的普通岗位。

值班人员不能被自动轮岗移动已有值班目标岗位。

值班人员中间减负：

先尽量减少额外航班；

必须上岗时避开重点岗位；

再选择疲劳点更低岗位；

末班重点岗位公平、岗位完整性、值班锁定不被突破。

KE166 机动督导
机动督导与绑定柜台视为一个逻辑岗位。

可参加同航班或重叠航班闭环轮换。

普通人员无解时，完整资质分队长可作为督导兜底。

分队长接任督导后必须释放原重叠普通岗位。

任意一端是 KE166 时，禁止分队长并行承担两个督导。

最新讨论要求：

先排柜台并完成轮换；

最后再从柜台人员中选机动督导；

顶部督导与柜台同人双显示，但不重复计工时；

柜台 workHours = 0，顶部计完整工时；

两个岗位疲劳分别计算；

督导自身也要避免连续，优先上一工作班未担任机动督导的人。

分队长并行督导补缺
只有正常排班产生常规岗位空缺时才启用。

允许分队长同时承担两个短时重叠督导。

最大重叠时间可调，默认 30 分钟。

只允许两个督导，不允许兼任普通柜台或第三个督导。

工时按并集计算，重叠时间只算一次。

两个督导疲劳分别累计。

KE166 禁止使用并行督导。

行政支援
行政支援岗位和人员不计入工时、疲劳、负荷均衡。

行政支援模式开启后，同名行政岗位替换常规岗位。

没有同名行政替代项的常规岗位仍保留。

行政支援人员可拖入任意岗位，但仍检查状态、夜班、冲突、工时。

行政支援启用状态不进入配置 Excel。

引导岗位
默认从同航班最下面的常规岗位向上复用。

多个引导岗位使用不同人员。

引导复用不重复计算工时和疲劳。

支持手动改选，但只能选该航班已经上岗的正常常规人员。

不允许未参加该航班、行政支援或不存在的姓名。

机动督导兼任范围
默认禁止兼任备注包含“一号”“申报”“排查”的岗位。

禁止规则优先。

同时约束自动排班和人工拖拽。

旧排班中违反新规则的关联会自动清理。

末班重点岗位公平
实际结束时间严格晚于 23:00 才算末班。

23:00 整不算；23:01、23:55、跨午夜计入。

四类分别统计：

督导

一号

申报

送资料

同一航班、自然月、最近 8 个归档工作班分别统计。

固定顺序：

督导 → 一号 → 申报 → 送资料

后序岗位不得破坏前序公平。

TR121/H02 自动排班每人每月最多 2 次。

全部达到上限时留空。

人工拖拽允许超过 2 次。

督导资质人员的申报、送资料允许比普通人员少 1 次。

督导公平必要时可突破上一班恢复保护，但不能突破资质、时间、工时、值班锁定和岗位完整性。

同日 CX / 同航司重点岗位分散
最终规则不只针对 CX937/CX931。

按航空公司归组，不按完整航班号归组。

同一航司同一规范岗位共用轮换统计。

同日同航司控制/一号交叉互斥进入 HiGHS 硬约束。

自动排班、后置轮岗、人工调整、最终安全校验共用同一事实源。

备注优先于柜台名。

删除旧的 CX 专用后置换人逻辑，避免规则重复执行。

5. 重点功能演进
   早期功能
   增加 Python 启动脚本 dev.py。

配置页预填航班模板。

航班页输入航班号后自动补全时间、岗位、备注。

排班结果改为可拖拽表格。

支持 Excel 导入导出。

支持历史归档和次日/后天排班。

支持岗位启用旅客人数阈值。

支持行政支援模式。

支持引导复用。

支持分流提前撤岗。

支持同岗轮换、滚动负荷、跨日晚班减负。

支持月度轮值：CX 航前、值班、备勤。

支持排班反馈。

支持规则模块和可编辑规则。

支持值班疲劳点可调。

支持历史按日期显示。

全局 HiGHS 升级
从逐岗位排班升级为当天全部自动岗位和全部合格人员一起计算。

计算上限从 30 秒改为 120 秒，后改为 150 秒，再引入自适应最多 300 秒。

失败不采用半成品，不退回旧方法。

新增整体排班模块。

后期把排班放入 Web Worker，增加右下角实时进度。

通过缓存运行事实，将连续归档后的排班从约 10–11 秒降到约 300ms。

删除旧逐岗入口：

src/domain/kernel/schedule-task-assigner.ts

src/domain/kernel/schedule-task-order.ts

新增：

src/domain/kernel/daily-schedule-optimizer.ts

src/domain/assignments/ke166-supervisor-finalizer.ts

统计模块
新增左侧“统计”模块。

月度轮值明细迁移到统计页。

月度轻松班次统计迁移到统计页。

新增 TR121/H02 月度承担次数。

支持末班重点岗位统计、手动修正、导入导出、按月清零。

末班申报、送资料分开统计。

支持按航班分类查看。

支持人员 × 航班总览矩阵。

点击具体航班数字展开四类修改。

支持 − / + 人工修正。

支持导入/导出末班重点岗位次数。

支持当前月份一键清零。

支持月份切换查看历史月份。

每个自然月独立计算，新月份自动从 0 开始。

配置 Excel
规则模块所有手工填写数据尽量随“导出配置”进入 Excel。

行政支援模式不导入导出。

具体值班/备勤日期不放入配置 Excel。

某张规则表存在无效行时，该表不覆盖原规则，并提示工作表、行号和原因。

工作表存在则完整替换该类规则；不存在则保留当前规则。

下载模板已补齐：

nextWorkdayRecoveryMode

每周航班计划

跨航班重点岗位优先

7 类结构化规则

末班范围

导入改为“工作表是否存在”判断，合法空表可清空。

空人员表不再错误绑定旧设备人员 ID。

导入预览补齐结构化规则数量。

增加真实 XLSX 二进制往返测试。

在线航班查询
航班页新增“在线查询航班”。

默认查询当前排班日期，可修改日期。

工作日边界：排除当日 00:00–05:59，纳入次日 00:00–05:59。

只保留国际及港澳台航班。

航班号严格同名匹配模板。

已有航班不重复添加，不覆盖人数和人工时间。

支持“继续保留、建议新增、待确认删减”对账。

待删航班默认不勾选，必须人工确认。

图片识别
曾实现“上一班图片导入”。

使用 Tesseract.js 浏览器本机 OCR。

支持识别航班、岗位、人员。

后来因识别效果不好，用户要求删除。

图片识别功能已彻底删除，包括按钮、代码、测试、依赖和离线字库。

历史记录保留，不受删除影响。

航班选择
点击“归档并排后天”先打开本地航班选择窗口。

默认沿用本班航班。

支持全选、清空、逐项勾选。

模板与无模板临时航班自动去重，模板优先。

新班航班预定人数初始化为 0。

计算前不修改历史、日期、航班和当前班表。

计算成功后一次性提交归档、航班替换、排班结果和日期切换。

取消、失败、停止或无安全结果时完整保留原班表。

不依赖在线查询。

对比上一班
生成班表后点击“对比上一班”。

自动寻找当前日期之前最近一个已归档工作班。

只读展示，不能编辑或删除。

不会重新运行 HiGHS。

手动次数修正
统计页增加末班重点岗位次数人工修正。

维度：

月份

人员

航班号

岗位类别

类别：

督导

一号

申报

送资料

排班使用次数 = 实际历史次数 + 人工修正次数。

最低为 0。

修改后标记当前班表需重新排班，不静默重排。

不修改或伪造历史记录。

修正随配置 Excel 导出/导入跨设备恢复。

Excel 工作表：末班重点次数修正。

航班下拉只显示该人员具备对应类别资质的航班。

增加“当月次数清零”。

清零只作用于当前统计月份。

历史排班不删除。

展开后显示：修正 +0、实际 0、最终 0。

支持月份切换，新月份自动从 0 开始。

排班警告简化
所有黄色警告最多两句话。

格式统一：人员 → 事实 → 原因 → 决定 → 结果。

页面不再出现：

infeasible

changed-assignment-count

“双向岗位资质”

“完整重排方案”

内部诊断仍保留，方便查错。

取消班表格子中“上一班谁更累”的黄色比较提示。

底部“跨工作班动态平衡”记录保留。

缺员、恢复失败、轮岗失败等重要报警保留。

备勤资质
常规人员默认开启，行政支援默认关闭。

关闭后：

自动轮值不安排备勤；

手工选择不能选为备勤；

值班备勤 Excel 不能导入为备勤。

合格人员不足时备勤席位留空并明确提示。

旧本地数据、旧 Excel 按常规人员开启处理。

岗位启用旅客人数
上午航班同样遵守人数门槛。

预定人数为 0、门槛为 30 时，岗位格保留但不安排人员。

达到 30 后才自动安排。

门槛为 0 时始终启用。

KE166、引导、行政支援和上午岗位原有规则未改变。

项目地图与交接
新增 PROJECT_MAP.md：

分层架构

启动链

排班生成、求解、覆盖、后置复核数据流

目录索引

数据边界

规则修改定位

常用验证命令

新增/维护 HANDOFF.md：

新窗口继续工作的短期快照。

不是产品事实来源。

与当前规范、代码、测试和 Git 事实冲突时，以仓库现状为准。

Rule Evolution Skill
创建 autoschedule-rule-evolution Skill。

真实文件放在：

text
G:\浏览器下载\my-skills\autoschedule-rule-evolution
项目内建立目录联接：

text
.agents/skills/autoschedule-rule-evolution
用途：

审查或实现新排班规则；

检查旧规则覆盖、执行顺序、唯一事实源、后置复核、数据闭环和回归保护；

发现冲突先停下报告，不擅自取舍。

调用方式：

text
$autoschedule-rule-evolution
若客户端支持 Skill 斜杠菜单，也可能显示为：

text
/autoschedule-rule-evolution 6. 性能优化与求解器演进
问题现象
新增 FD573、MF8683 后，当天整体排班超过 30 秒。

真实规模：

15 名正常常规人员

6 个航班

54 个班表格子

160 条历史记录

原始复现：

第一阶段约 485 变量、678 限制

组合加入后约 1003 变量、2244 限制

接近或超过 30 秒

根因
补充疲劳、岗位衔接等组合关系时，模型从约 485 个变量膨胀到约 1000 个。

每增加一批组合，后续约 22 个字典序目标会重复精确求解。

组合变量按当前数量编号，后加组合可能改变早先编号，导致已完成目标被误判为变化并重复计算。

每个组合变量原来使用三条限制，其中两条在“只最小化正代价”前提下可能冗余。

同一模型逐层重新传入和求解，是主要耗时。

519 个前后航班组合 0/1 辅助变量是主要性能瓶颈之一。

已采用优化
完全冲突航班岗位组使用 clique 限制压缩。

部分兼容仍保留精细两两限制。

组合变量使用基于完整模型的稳定编号。

逐个字典序目标求解，只在必要时回到受影响目标。

保留空目标作为组合条件插入位置。

只扩展当前及已处理目标真正涉及的组合。

每个二元组合只使用一条必要下限限制。

冲突分组压缩：

两个航班完全重叠时，不再把每两个岗位分别限制；

整组写成“该人员在两组岗位中最多选一个”。

后续将同航司控制/一号互斥改为“每人 × 航司 × 重点岗位组”单个总和 <= 1，约束数量从 O(n²) 降到 O(n)。

HiGHS 原生多目标
发现 npm 包 highs@1.15.3-pre.4 提供持久模型 API。

项目后来使用项目内 @autoschedule/highs-ts 或 vendor/highs-ts。

导出接口：

Highs_passLinearObjectives

Highs_clearLinearObjectives

Highs_addRows

Highs_setSolution

Highs_changeColsCostByRange

Highs_changeObjectiveSense

原生多目标必须设置：

text
blend_multi_objectives=false
真实复杂模型从约 25.3 秒降到约 12.33 秒。

10 个航班、15 人压力测试约 2.04 秒。

34 项排班目标值全部保持一致。

按方案 B，完全同分时少数人员可能在等价岗位间互换。

自适应计算预算
原固定 150 秒。

当前月份存在末班重点人工修正或清零基准时，最多 300 秒。

其他月份、未勾选航班账本不增加本次排班时间。

计算提前完成会立即返回。

超时提示改为“超过允许时间（最长 5 分钟）”。

新增时间预算模块：

src/domain/kernel/daily-schedule-time-budget.ts

5000 条历史压力测试
15 人

10 航班

40 岗位

5000 条历史

连续生成 6 次

每轮切换行政支援模式

每轮检查：

40 个岗位不丢失

assignment ID 不重复

无无故空缺

单轮小于 20 秒

优化后 6 轮约 24.321 秒，平均约 4.05 秒/轮。

存储容量风险
2 万条历史约 4.75 MiB，接近 localStorage 上限。

历史页改为按需展开。

5000 条历史测试：历史页约 0.22 秒，32 岗位排班约 6.6 秒。

接近容量时重要存储报警，不自动删除历史。

OR-Tools 评估
结论：

OR-Tools CP-SAT 适合排班，但官方没有 JavaScript/WASM 浏览器包。

需要 EXE 或后端，开发难度明显更高。

当前纯浏览器架构继续使用 HiGHS 更合适。

OR-Tools 最适合做中期对照原型：

ExactlyOne

可选时间区间

NoOverlap

assumptions 无解标签

hint 初始方案

Timefold 适合 Java 后端，免费社区版可用，企业版功能不能计入免费推荐。

参考仓库已克隆到：

text
G:\浏览器下载\codex项目\autoschedule\clone
调研文档：

text
docs/research/github-scheduling-projects-and-solver-patterns.md
docs/research/scheduling-solver-performance.md
docs/research/free-workforce-scheduling-solvers.md
成熟开源参考项目
已浅克隆 10 个免费开源项目，约 509.7 MB：

HiGHS

OR-Tools

Timefold Solver

Timefold Quickstarts

PyWorkforce

Roster Wizard

OptaPlanner

UniTime

Staffjoy V2

shift-scheduling-mip

关键结论：

15 人、每天不超过 10 个航班，全局排班属于正常规模。

当前慢的主要原因更可能是模型中生成过多人员与岗位组合，不是 HiGHS 完全不适合。

短期最适合继续使用 HiGHS，重点参考成熟项目进行时间分组、减少无意义组合、复用上一次计算结果。

不照搬示例接受 FEASIBLE 的做法，当前合同要求所有目标证明最优。

Staffjoy V2 已废弃，不含核心自动优化器。

UniTime 对当前规模过重。

OptaPlanner 已迁入 Apache KIE 继续维护。

7. 统计、导入导出与配置
   统计页矩阵
   勾选几个末班重点航班，就显示几列。

人员 × 航班矩阵。

点击具体航班次数展开：

督导

一号

申报

送资料

每类支持 − / 当前次数 / +。

命令固定携带：

人员

月份

航班

类别

修改次数不改变人员列表排序。

保留日期和岗位明细展示。

顶部保留月份、跨航班总计、当月清零。

工具栏：

导入次数

导出次数

当月次数清零

专用 Excel
工作表：末班重点岗位次数

导出字段：

月份

人员编号

人员姓名

航班号

岗位类别

最终次数

导入校验：

月份

人员编号/姓名

航班

类别

非负整数

重复项

完整性

导入按“目标最终次数 − 当前原始实际次数”重建修正，不会重复累加。

当前航班范围变化或文件缺行时，阻止覆盖。

其他月份、其他航班、历史排班保留。

配置导入导出
顶部“导出配置” → 排班工具配置.xlsx

统计页“导出次数” → 末班重点岗位次数_月份.xlsx

导出改用浏览器原生 Blob 下载。

不要直接双击 dist/index.html，浏览器会阻止本地模块加载。

在线版或 python dev.py 本地 HTTP 版才能正常导入导出。

导入数据入口：

顶部“导入数据”

配置页“导入配置模板”

统计页“导入次数”只接受单独的 末班重点岗位次数 工作表，不接受完整配置文件。

完整配置文件导入不会坏，但旧格式可能没有最新末班次数工作表。

8. 架构耦合审查与修复
   初始问题
   截图中的 6 类问题本质上是一个总问题：

同一条排班规则，被调度器、规则页面、Excel、存储、反馈页面分别实现了一遍。
改一个规则，经常需要同时改很多文件，漏一个地方就会出现“界面说一套、实际执行另一套”。

具体包括：

规则合同与执行顺序分散。

排班事实、规则证据混在 Assignment 里。

反馈重新推算规则。

存储恢复是一个大函数。

配置处理散落。

编辑规则生命周期散落。

已完成断裂式重建
新建 src/schedule-rule-contract.ts

单一定义规则 ID、阶段、名称、反馈模式。

SchedulingRuleId 直接从定义表派生，不再和 model.ts 双写。

重建 candidate-priority.ts

统一候选事实、固定顺序、比较器、决定性规则。

新建 position-rotation-policy.ts

接管重点岗位和高疲劳岗位分类。

删除 src/domain/scheduling-policy.ts。

重建 assignment-eligibility.ts

返回结构化 violation。

自动排班、人工改派、缺员说明、值班检查共用。

删除 src/domain/assignment-validation.ts。

rotation-review-safety.ts

新增穷尽的 ROTATION_REVIEW_POLICIES。

移除散落的 review === ...。

反馈合同

独立反馈规则声明 feedbackKey。

schedule-rule-feedback.ts 使用穷尽 builder 注册表。

持久化

state-restoration.ts 严格校验 rule ID、stage、outcome。

结构化配置

新建 structured-policy-contract.ts。

新建 structured-policy-settings.ts，统一五类规则默认值和清洗。

UI 命令

新建 app-command-contract.ts。

59 个 click 命令、5 个 change 命令类型化。

未知命令不再静默吞掉。

新增测试：

assignment-eligibility.test.ts

app-command-contract.test.ts

同步 spec.md 技术职责说明和项目维护参考。

旧模块引用扫描为 0。

92 个生产 TypeScript 文件，循环依赖为 0。

62 个静态 data-action 全部已登记。

后续新规则演进防冲突
新增 autoschedule-rule-evolution Skill。

审查链路：

text
输入 -> 唯一事实源 -> 候选/硬约束 -> HiGHS 模型 -> 锁定/补缺 -> 后置复核 -> 最终 assignments -> Store/页面展示
对新增规则逐项回答：

唯一规则 ID、所属阶段、拥有字段和直接消费者。
是否把软目标当硬约束，或在后置阶段改回前序已满足的优先级。
值班优先级、值班中间减负、末班重点岗位公平、同日 CX 重点岗位、岗位衔接和轮岗是否互相覆盖。
历史归档、人工次数、Excel/localStorage 导入导出、统计和 UI 是否读写同一事实源。
最终班表是否仍满足岗位完整性、人员资格、时间冲突、疲劳和锁定规则。
修改门禁：

冲突时停止写入，列出冲突、影响和候选决策，等待 owner 裁决。

先建立失败测试。

规则实现放在所属 domain/规则模块。

不创建第二套统计/规则副本。

验证按风险升级。

停止条件：

第二套事实源；

职责归属不清；

后置复核推翻硬约束；

无法通过公共职责接口测试；

失败结果无法解释。

航司 + 规范岗位统一事实
新增唯一事实模块：

text
src/domain/rules/airline-rotation.ts
轮换键从“航班号 + 岗位”改为“航空公司 + 规范岗位”。

备注优先于物理柜台名。

同航司不同航班控制/一号交叉互斥进入 HiGHS 硬约束。

自动排班、后置轮岗、人工调整、最终安全校验共用同一事实源。

删除旧的 CX 专用后置复核模块：

text
src/domain/reviews/same-day-cross-flight-priority-review.ts
增加大数据场景下的后置轮岗性能保护。

值班减负被后置轮岗覆盖
真实案例：

text
值班人员华嘉慧
2026-08-26
必须承担上午航班和 TR121/H02
中间 CX931 有 G20 和 G14
22 日、24 日历史导入后：
HiGHS 初始结果让她在 G14
position-frequency-review 后置复核又把她换回 G20
修复：

后置轮岗允许改善值班减负，但不能增加值班人员额外航班、重点岗位或疲劳。

岗位确实缺人时，仍允许值班人员补缺。

修复位置：

text
src/domain/reviews/rotation-review-safety.ts
同航司控制/一号硬互斥
用户要求：

同一航空公司所有航班统一轮换。

备注为“申报”的，即使柜台号不同，也共用 航司/申报。

同日同航司控制/一号交叉互斥。

从 best-effort 后置复核提升为 HiGHS 硬约束。

后置复核不得重新产生违规最终 assignments。

实现：

唯一事实源：airline-rotation.ts

备注优先。

同航司硬约束。

自动和人工共用资格诊断。

最终安全复核拒绝违规安排。

中央规则合同移动硬约束区。

删除冗余后置改写。

增加大数据场景性能保护。

9. 测试与验证策略
   原来问题
   每次改动都跑全量 verify，包含很多无关测试，浪费时间。

开发过程中反复运行无关测试。

性能测试默认混入每次局部验证。

没有 PR 必需检查，本地提交门禁过重。

调整后策略
测试策略采用测试金字塔、左移测试、风险测试和变更影响分析的组合。依据见：

text
docs/research/development-testing-strategy.md
验证分为不同反馈周期：

L0 编辑反馈：

行为变更先运行能稳定失败的测试；

再运行本职责及直接消费者的最小测试集合；

不运行性能测试和生产构建。

L1 本地变更集：

显式相关测试；

Vitest related / --changed 候选集合；

npm.cmd run typecheck；

按风险补充真实入口检查。

L2 完整功能验证：

npm.cmd run typecheck

全部普通测试

npm.cmd run build

用于高影响代码本地交付，以及未来 PR/合并必需检查。

L3 性能验证：

仅在改动求解、Worker、候选搜索、大数据路径、性能阈值、依赖或运行时性能时运行：

text
npm.cmd run test:performance
固定环境定时基线尚未建立，不把单次本机墙钟结果写成可靠基线。

L4 发布完整验证：

发布、部署、owner 明确要求完整验收，或当前仓库 commit/push 门槛要求时运行一次：

text
npm.cmd run verify
变更风险决定范围
动态导入、Worker/WASM、读取源码文本的架构测试、Excel/localStorage、共享 AppState、迁移和跨模块数据合同可能不会被静态依赖图选中，必须显式补测。

纯协作文档只运行：

text
git diff --check
并人工核对规则引用，不运行应用测试。

推送门槛
任何实现、测试、产品事实、数据模板、构建或运行配置改动，必须先完成 npm.cmd run verify，退出码为 0 后才允许 commit 和 push。

纯协作文档按“验证”一节执行。

任一验证失败、未运行或存在未说明的剩余风险时，不得推送到远端。

push 只在 owner 明确授权后执行。

当前常用完整验证
powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run verify
git diff --check
git status --short
性能相关：

powershell
npm.cmd run test:performance 10. 历史顽疾与踩坑
新增规则覆盖旧规则，拆东墙补西墙。

scheduler.ts 过大，规则顺序隐含在代码顺序里。

初排先占用人员，后置轮岗找不到替代人。

重点岗位频率比较太靠后。

归档时丢失“一号”等原始备注，导致跨日保护失效。

把“值班岗位不可移动”错误扩大为“值班人员全天不能补岗”。

行政支援同名岗位重复显示。

行政支援负荷被错误统计。

递归补洞导致 Maximum call stack size exceeded。

规则保存后自动重排，覆盖当前班表。

人工调整后旧 decisionTrace 残留，反馈显示过期理由。

反馈写求解器术语，用户看不懂。

历史页一次显示全部记录，数据多时卡顿。

5000 条历史压力测试后改为按需展开。

Excel 导出曾因 XLSX.writeFile 不下载，后改为浏览器 Blob 下载。

Excel 模板直接回写会膨胀，后用定点 XML 更新。

在线航班查询 CORS 导致 Failed to fetch，后部署修复。

飞常准网页接口不能按未来日期查完整机场列表。

图片识别效果差，最终删除。

多个项目路径和端口混用，需统一到：

text
G:\浏览器下载\codex项目\autoschedule
只改 HiGHS，不查后置管线，导致模型正确但最终班表错误。

把页面刷新当成重新计算；刷新只恢复 localStorage 旧班表，必须点击“重新排班”。

测试夹具过于简单，没有包含历史、后置轮岗、真实 CX 时段和页面恢复状态。

过早认定是旧班表，而不是先检查实际生成结果的决策证据。

把规则名称当成优先级；真正需要追踪的是谁先生成结果、谁可以修改结果、谁可以覆盖谁。

增加全局保护时误伤岗位完整性，后来增加 coverage 阶段例外。

重点岗位判定函数多套语义漂移。

规则执行顺序由多个地方分别维护。

后置复核可以覆盖前序模型结果。

localStorage 恢复旧班表不会自动识别代码规则变化。

历史、人工次数、当前班表分别参与不同统计，缺少统一数据合同检查。

后置复核最终结果测试不足。

决策证据可能被后置阶段重写。

同日 CX 后置换人可能破坏已经成立的末班重点岗位公平。

后置换人会清掉原岗位全部自动决策证据。

值班中间休息目标优先级过高，可能牺牲重点岗位公平和连续保护。

11. 当前关键文件
    领域规则
    text
    src/domain/kernel/daily-schedule-optimizer.ts
    src/domain/kernel/daily-schedule-model.ts
    src/domain/kernel/daily-combination-model.ts
    src/domain/kernel/daily-schedule-safety.ts
    src/domain/kernel/daily-schedule-time-budget.ts
    src/domain/kernel/scheduling-kernel.ts
    src/domain/kernel/schedule-pipeline.ts
    src/domain/kernel/schedule-review-contract.ts
    src/domain/kernel/schedule-progress.ts
    src/domain/rules/airline-rotation.ts
    src/domain/rules/schedule-rule-contract.ts
    src/domain/rules/built-in-rule-registry.ts
    src/domain/rules/scheduling-execution-plan.ts
    src/domain/rules/structured-policy-contract.ts
    src/domain/rules/structured-policy-settings.ts
    src/domain/candidates/candidate-priority.ts
    src/domain/candidates/assignment-eligibility.ts
    src/domain/reviews/position-rotation-review.ts
    src/domain/reviews/consecutive-rotation-plan.ts
    src/domain/reviews/rotation-review-safety.ts
    src/domain/reviews/ke166-rotation-review.ts
    src/domain/reviews/late-priority-frequency-review.ts
    src/domain/reviews/late-shift-cutoff-review.ts
    src/domain/reviews/late-shift-recovery-review.ts
    src/domain/reviews/position-frequency-review.ts
    src/domain/reviews/schedule-feedback.ts
    src/domain/assignments/ke166-assignment.ts
    src/domain/assignments/ke166-supervisor-finalizer.ts
    src/domain/statistics/schedule-frequency.ts
    src/domain/statistics/late-priority-frequency.ts
    src/domain/statistics/late-priority-frequency-adjustment.ts
    src/domain/statistics/monthly-late-priority-statistics.ts
    src/domain/shared/schedule-run-facts.ts
    src/domain/solver/reassignment-optimizer.ts
    src/domain/solver/reassignment-model.ts
    基础设施
    text
    src/infrastructure/storage.ts
    src/infrastructure/state-restoration.ts
    src/infrastructure/excel.ts
    src/infrastructure/excel-rule-settings.ts
    src/infrastructure/duty-roster-excel.ts
    src/infrastructure/late-priority-counts-excel.ts
    src/infrastructure/flight-query.ts
    src/infrastructure/schedule-runner.ts
    src/infrastructure/schedule-worker-protocol.ts
    src/infrastructure/solver/highs-solver.ts
    src/schedule.worker.ts
    vendor/highs-ts
    UI
    text
    src/ui/components/statistics-page.ts
    src/ui/components/schedule-toolbar.ts
    src/ui/components/schedule-grid.ts
    src/ui/components/policy-structured-rules.ts
    src/ui/components/policy-settings-form.ts
    src/ui/components/flights-page.ts
    src/ui/components/online-flight-query-dialog.ts
    src/ui/components/history-page.ts
    src/ui/components/config-view.ts
    src/ui/projections/policy-rule-presentation.ts
    src/styles.css
    应用层
    text
    src/app/application-coordinator.ts
    src/app/schedule-run-controller.ts
    src/app/controllers/records-controller.ts
    src/app/controllers/transfer-controller.ts
    src/app/controllers/configuration-controller.ts
    src/app/store/records-commands.ts
    src/app/statistics-actions.ts
    src/app/workbook-actions.ts
    src/app/policy-actions.ts
    src/app/schedule-actions.ts
    src/app/history-actions.ts
    src/app/configuration-actions.ts
    src/app/command-contract.ts
    src/app/app-command-contract.ts
    规范与经验
    text
    AGENTS.md
    spec.md
    README.md
    HANDOFF.md
    PROJECT_MAP.md
    .agents/skills/autoschedule-dev/SKILL.md
    .agents/skills/autoschedule-dev/references/scheduling-maintenance.md
    .agents/skills/autoschedule-dev/references/development-lessons.md
    .agents/skills/autoschedule-rule-evolution/SKILL.md
    研究文档
    text
    docs/research/development-testing-strategy.md
    docs/research/scheduling-solver-performance.md
    docs/research/github-scheduling-projects-and-solver-patterns.md
    docs/research/free-workforce-scheduling-solvers.md
    参考仓库
    text
    clone/
    包含 OR-Tools、Timefold、HiGHS、OptaPlanner、UniTime 等，仅本地参考，不进入正式构建。

12. 验证命令
    每次修改后应运行：

powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run verify
git diff --check
git status --short
性能相关改动再运行：

powershell
npm.cmd run test:performance
本地开发：

powershell
cd "G:\浏览器下载\codex项目\autoschedule"
python dev.py
通常地址：

text
http://127.0.0.1:5173/
也可以双击：

text
G:\浏览器下载\codex项目\autoschedule\dev.py
不要直接双击 index.html 或 dist/index.html。
不要直接打开本地 HTML 文件，浏览器会阻止模块加载。

13. 最新交接提示词
    下面这段可以直接发给新窗口：

text
你现在接手项目：

G:\浏览器下载\codex项目\autoschedule

请始终使用简体中文交流。

开始前必须：

1. 完整读取 AGENTS.md、HANDOFF.md、spec.md、README.md。
2. 完整读取 .agents/skills/autoschedule-dev/SKILL.md。
3. 读取 .agents/skills/autoschedule-dev/references/scheduling-maintenance.md。
4. 读取 .agents/skills/autoschedule-dev/references/development-lessons.md。
5. 如命中规则演进、架构耦合、性能模型、职责拆分，读取 autoschedule-rule-evolution、improve-codebase-architecture、codebase-design。
6. 检查 git status 和全部未提交差异，禁止回滚、清理、覆盖已有修改。
7. 先说明你对需求的理解、修改范围、不改变的行为、冲突和验收边界。
8. 得到明确确认后再修改文件。
9. 不创建 plan.md，除非明确要求。
10. 不 commit、push、发布或部署，除非明确授权。
11. 不需要 Playwright，但必须完成代码验证。

项目是机场国际值机自动排班工具，技术栈为 TypeScript、Vite、Bootstrap、Vitest、xlsx-js-style。数据主要在浏览器 localStorage，自动排班通过 Web Worker + HiGHS。

当前重点规则：

- 硬约束永远优先：状态、资质、夜班、时间冲突、工时上限。
- 岗位完整性优先于疲劳软保护。
- 值班、KE166 机动督导、人工锁定不能被自动轮换随意移动。
- 重点岗位不得连续第二班由同一人承担相同“航司 + 规范岗位”。
- 有安全替代时必须轮换，不能只报警。
- 搜索顺序：空闲直接接替 -> 同航班两人交换 -> 重叠航班两人交换 -> 最多五人闭环/开放链。
- 无安全方案时保留岗位完整性，并反馈具体原因。
- 行政支援负荷不统计。
- 同名行政支援岗位替换常规岗位。
- 引导默认从同航班最下面常规岗位向上复用。
- 跨工作日恢复、次班截止、值班预休、重点岗位轮岗都有明确顺序。
- 值班预休制度已删除。
- 末班重点岗位公平顺序：督导 → 一号 → 申报 → 送资料。
- 同航司控制/一号交叉互斥是硬约束。
- 备注优先于物理柜台名。
- 值班中间减负顺序：少班 → 避重点 → 低疲劳。
- 统计页支持末班重点次数人工修正、导入导出、月度清零、月份切换。
- 人工修正进入排班均衡，但不伪造历史记录。

当前工作区可能有大量未提交修改，绝对不能清理或回滚。

完成后必须运行：

npm.cmd run typecheck
npm.cmd test
npm.cmd run build
npm.cmd run verify
git diff --check
git status --short

最终汇报必须明确：

- 实际完成内容
- 修改的核心文件
- 新增或修改的规则
- 新增回归测试
- 四项验证真实结果
- 未验证事项和剩余风险

14. 相关聊天记录来源
    与排班系统高度相关的上传文件：

核心整合
autoschedule 排班系统聊天记录整合.txt

架构诊断与修复
先停止吧-保存你所做的我下次-发继续的时候你再继续-33274372.md

先停止吧-保存你所做的我下次-发继续的时候你再继续-abf1bc0f.md

先停止吧-保存你所做的我下次-发继续的时候你再继续-cf5cc332.md

性能、求解器、超时
继续直到完成这次修改-如已完成请忽略-a4feee2d.md

我怎么才能不依赖每次给我发的这个东西呢-如果没有这个东西-我该怎么打开呢-3ee6c13a.md

我怎么才能不依赖每次给我发的这个东西呢-如果没有这个东西-我该怎么打开呢-07db4f6e.md

AGENTS.md-instructions-for-G-浏览器下载-c-eccd14d9.md

AGENTS.md-instructions-for-G-浏览器下载-c-f882a090.md

AGENTS.md-instructions-for-G-浏览器下载-c-ed244c85.md

规则演进与大量功能
AGENTS.md-instructions-for-G-浏览器下载-c-f9ea14a8.md

AGENTS.md-instructions-for-G-浏览器下载-c-fe5633db.md

AGENTS.md-instructions-for-G-浏览器下载-c-f369e659.md

AGENTS.md-instructions-for-G-浏览器下载-c-ea03987b.md

AGENTS.md-instructions-for-G-浏览器下载-c-ed244c85.md

OR-Tools / 自动排班 OT
grill-me-C-Users-Administrator-e9e00bee.md

测试策略
继续直到完成-ab637079.md

流程图 / 汇报
你参考一下现在比较成熟的这种模式-你不要自己闭门造车-0c78a481.md

你参考一下现在比较成熟的这种模式-你不要自己闭门造车-080c306b.md

其他排班相关
继续直到完成这次修改-如已完成请忽略-a4feee2d.md

AGENTS.md-instructions-for-G-浏览器下载-c-eccd14d9.md

AGENTS.md-instructions-for-G-浏览器下载-c-f9ea14a8.md

AGENTS.md-instructions-for-G-浏览器下载-c-f882a090.md

AGENTS.md-instructions-for-G-浏览器下载-c-fe5633db.md

AGENTS.md-instructions-for-G-浏览器下载-c-f369e659.md

AGENTS.md-instructions-for-G-浏览器下载-c-ed244c85.md

AGENTS.md-instructions-for-G-浏览器下载-c-ea03987b.md

以下文件已确认与排班系统无关，未合并：

电梯模型相关会话

Spotify / SpotiFLAC 下载会话

Windows 垃圾清理与 WinHance 调研

小红书账号查找

弹力球测试

航班查询工具项目

Codex 权限审批、工具审批、IDE 环境噪音

其他无项目内容的空会话或短会话
