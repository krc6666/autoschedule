# autoschedule 开发历史

本文件记录重要问题的事实、假设、尝试、证据、淘汰路线和最终边界。当前产品规则以 `spec.md` 为准，长期协作纪律以 `AGENTS.md` 为准。

## 2026-09-12：半休规则从局部修补转向通用闭环机制

- 问题：上午半休人员在初始候选池和求解模型中被排除后，后置补缺、恢复、轮岗、覆盖或收尾工序仍可能改写 `assignments`，最终页面出现 12:00 前岗位。局部测试通过不能证明最终保存结果合法。
- 事实：半休事实的唯一 owner 是 `src/domain/rules/half-rest.ts`；排班主链路经过求解、覆盖、后置复核和最终安全检查。规则曾由多个模块分别维护，缺少统一传播矩阵和最终凭证门禁。
- 根因：规则注册表维护了规则 ID、阶段和顺序，却没有强制每条 `assignments` 写入路径使用同一硬约束守卫；账本主要校验结构，最终复核也不是从全部硬规则自动组合而来。因此“候选池正确”不能推出“最终结果正确”。
- 取舍：不复制 EhomeBot 或其他排班项目的目录结构，只采用单一事实 owner、证据闸门、历史沉淀和分层验收思想。后续机制包括规则传播矩阵、统一 proposal/ledger 守卫、最终不可变结果和安全凭证安装门禁。
- 边界：中间阶段允许岗位空缺，但最终结果必须经过全量硬规则复核；人工调整的半休边界另行确认；严格恢复为半休补位让步必须显式记录，不能形成通用绕过。

## 2026-09-12：16 个失败最终归因于覆盖硬约束泛化

- 现象：原有 16 个失败表面分成两组：8 个覆盖场景直接报“无解”，另 7 个频率/优先级场景表现为选人不符合预期，另有 1 个旧测试仍断言已删除的覆盖行为。
- 初始判断：一开始将问题拆成“覆盖约束过严”和“频率/优先级失效”两个根因，并准备分别修复。
- 最终判断：两个判断实际是同一个根因——覆盖约束被错误泛化为全局硬约束。它同时收窄了求解器可行域、向后置重排注入全局 `retain-work`、在轮岗和最终复核中阻止合法的局部空缺，并注册了 `staff-coverage-required` 门禁。
- 修复范围：恢复 `staffCoverageModel` 的 best-effort 语义；移除重排选择中的全局 `retain-work`；保留 `policy.preventStaffWithoutWork` 的局部保护；删除轮岗和最终复核中的全局覆盖硬门禁；保留半休最低一班硬约束；删除对应的中央 `staff-coverage-required` 注册及未跟踪的 `staff-coverage.ts`。
- 为什么 7 个“根因二”用例间接变绿：覆盖硬约束不只是报告错误，还改变了可行域和候选集合，使原本正确的频率/优先级比较器无法在目标候选之间工作。解除错误的全局覆盖门禁后，原有频率、重点岗位和恢复顺序逻辑重新获得完整候选，7 个用例按规格选回了具体人员；没有修改频率/优先级业务代码。
- 证据：7 个用例均断言了具体人员 ID，部分还断言 `decisionTrace` 中的 `position-frequency`、`late-priority-frequency` 等规则记录，因此不是单纯从“无解”变成“有解”。完整测试在更新两条过时断言后为 100 个测试文件、750/750 通过。
- 防回归：在 `tests/domain/scheduling-policy.test.ts` 增加隔离测试 `keeps position-frequency selection independent from staff coverage`，直接通过公开的 candidate-priority comparator 验证频率差异和 `position-frequency` 规则决策，不经过 coverage 模块、solver 或 assignments，确保覆盖约束即使未来变化，频率规则仍有独立回归信号。
- 边界：本轮没有扩展人工调整半休边界，也没有把“best-effort 覆盖”重新提升为硬约束；本条记录只描述自动排班链路和测试证据。

## 2026-09-12：从排班聊天记录归纳的历史顽疾

以下条目根据 `排班聊天记录.md` 中的历次开发记录归纳，用于后续新增功能、规则或排班逻辑修改前查阅。

### 规则传播与后置流程

#### 1. 规则散落在多个模块

- 问题：同一条排班规则曾分别出现在调度器、规则页面、Excel、存储和反馈代码中，修改一处后其他地方仍执行旧语义。
- 原因：没有唯一事实 owner，也没有统一的规则合同和传播矩阵。
- 尝试：重建规则合同、候选比较器、资质诊断、反馈注册表和结构化配置模块。
- 结论：规则事实必须只在所属 domain 模块定义，其他模块只能消费公开判断函数或快照。
- 怎么防再犯：新增或修改规则前先读 `history.md`、`spec.md`，建立传播矩阵并检查候选、求解、后置、最终复核、保存和展示路径。

#### 2. 只改 HiGHS，最终班表仍然错误

- 问题：求解模型已经得到合法结果，但补缺、恢复、轮岗或覆盖阶段又写入了违规 `assignments`。
- 原因：后置流程没有统一通过同一守卫和账本，最终复核也没有覆盖全部写入路径。
- 尝试：引入统一 proposal/ledger 守卫、最终安全复核和安全凭证安装门禁。
- 结论：中间求解通过不等于最终结果合法，所有会替换 `assignments` 的路径都必须经过同一套边界检查。
- 怎么防再犯：每条硬规则都要有“合法结果 -> 后置故意改坏 -> 必须拒绝或保留旧结果”的反向测试。

#### 3. 规则执行顺序由多个地方分别维护

- 问题：规则注册表、求解目标顺序、后置 mutation 顺序和反馈顺序可能互相漂移。
- 原因：顺序信息隐含在多个文件和代码分支中。
- 尝试：建立中央规则合同和 `scheduling-execution-plan`，从规则定义派生执行计划。
- 结论：规则 ID、阶段、优化性质和执行顺序必须由单一合同派生，不能手工维护多份列表。
- 怎么防再犯：新增规则时检查中央合同、候选目标、后置计划、最终守卫和反馈键是否来自同一 owner。

#### 4. 保护规则误伤岗位完整性

- 问题：为保护恢复、轮岗或负荷而增加全局限制后，模型频繁无解或留下不应有的空缺。
- 原因：把 best-effort 目标泛化成全局硬约束，忽略岗位完整性优先和局部例外。
- 尝试：移除全局覆盖硬门禁，保留明确的局部保护和半休最低一班硬约束。
- 结论：硬约束、软目标和局部保护必须分开表达；“尽量覆盖”不能自动变成“必须覆盖”。
- 怎么防再犯：修改保护策略时同时测试可行域、岗位覆盖、频率/优先级选择和最终复核，禁止只看单个求解器用例。

#### 5. 值班不可移动被扩大为值班人员全天不可补岗

- 问题：值班人员为了保留目标岗位，被错误地禁止承担其他合法普通岗位，或后置轮岗破坏了值班减负。
- 原因：把“值班目标岗位不可移动”误解为“值班人员不可再分配”。
- 尝试：将值班规则拆成目标岗位锁定、岗位缺员补位和中间减负三个边界，并固定减负顺序。
- 结论：只锁定明确的值班目标；缺员时仍可让值班人员补位，但不得增加额外重点岗位或疲劳负担。
- 怎么防再犯：分别测试目标岗位锁定、合法补位、后置轮岗和重点岗位公平，不使用一个全局 `isDutyStaff` 开关代替细粒度规则。

#### 6. 重点岗位判定语义漂移

- 问题：同一岗位在不同模块按航班号、柜台号、备注或关键词被判成不同类别，轮换和公平统计不一致。
- 原因：存在多套重点岗位判定函数，且没有统一规范化键。
- 尝试：建立 `airline-rotation.ts` 和岗位规范化规则，备注优先于物理柜台名。
- 结论：轮换键应使用“航空公司 + 规范岗位”，同航司控制/一号交叉互斥作为统一硬约束。
- 怎么防再犯：所有自动、后置、人工和最终复核路径调用同一个 owner 函数，并用跨航班、不同柜台、备注优先的测试锁定语义。

#### 7. 后置换人会清掉或重写决策证据

- 问题：岗位被后置换人后，原 `decisionTrace` 残留、被清空或与新人员不匹配，页面显示过期理由。
- 原因：重排逻辑直接修改 assignment 字段，没有把证据视为与排班运行和规则版本绑定的数据。
- 尝试：引入排班运行标识、规则指纹和证据版本校验，并在自动改写时重建或清理不再适用的证据。
- 结论：决策证据必须能回答“哪次排班、哪个规则版本、哪条决策产生”，不能只依赖当前岗位字段。
- 怎么防再犯：后置改写和人工调整都要有证据一致性测试；反馈展示前检查运行 ID 和规则指纹是否匹配。

### 排班模型与性能

#### 8. 初排先占用人员，后置轮岗找不到替代人

- 问题：初始结果虽然覆盖岗位，但后置轮岗时没有可替代人员，只能把连续问题转移给别人或放弃轮岗。
- 原因：求解阶段和后置阶段没有共享完整候选与锁定信息。
- 尝试：将全日候选、资格、历史负荷、锁定和后置安全条件纳入统一模型，并提供有限深度的交换闭环。
- 结论：轮岗必须在可行替代存在时执行，不能为了“看起来轮换”破坏硬约束或把问题转移。
- 怎么防再犯：增加同航班交换、重叠航班交换、最多五人闭环和无安全方案保留原结果的测试。

#### 9. 递归补洞导致栈溢出

- 问题：复杂补缺或交换图触发 `Maximum call stack size exceeded`。
- 原因：递归搜索深度随人员、航班和组合数量增长，没有边界或记忆化保护。
- 尝试：限制闭环人数，使用匹配、记忆化和有界搜索替代无界递归。
- 结论：后置补缺搜索必须有明确深度上限、访问集合和失败回退。
- 怎么防再犯：大规模和最坏路径压力测试必须覆盖递归/搜索深度；禁止新增无界递归补缺。

#### 10. 组合模型膨胀导致排班超时

- 问题：增加航班和历史后，变量从约 485 增至约 1000，约束从约 678 增至约 2244，排班超过原 30 秒上限。
- 原因：组合变量过多、多个字典序目标重复精确求解、变量编号不稳定导致无意义重算。
- 尝试：使用 clique 约束、稳定变量编号、只在受影响目标上重算、删除冗余限制，并采用 HiGHS 原生多目标。
- 结论：性能瓶颈主要来自模型规模和重复求解，不应只更换求解器或盲目增加超时。
- 怎么防再犯：涉及求解模型、候选搜索或历史窗口的改动必须跑性能测试，记录变量数、约束数、目标数和耗时变化。

#### 11. 重点岗位频率比较过晚

- 问题：频率和优先级规则在目标顺序中太靠后，先被其他目标消耗候选，导致长期公平失效。
- 原因：没有明确区分硬约束、岗位完整性、连续轮岗和月度频率的优先级。
- 尝试：固定目标顺序，单独增加频率/优先级隔离测试，并在覆盖根因修复后重新验证具体选人。
- 结论：目标顺序是产品语义的一部分，不能仅按代码出现顺序或规则名称推断。
- 怎么防再犯：每次调整目标顺序必须测试具体人员选择、决策证据和软目标让步路径。

### 数据、导入与恢复

#### 12. 归档丢失“一号”等原始备注

- 问题：历史归档后重点岗位备注丢失，跨工作日恢复和同航司轮换无法识别原岗位。
- 原因：归档数据只保留展示字段，没有完整保存规则所需的备注和规范化事实。
- 尝试：在历史、Assignment 和规则快照中保留航班号、岗位、备注及规则版本相关字段。
- 结论：用于规则判断的原始事实不能在展示或归档转换中丢失。
- 怎么防再犯：新增归档/导入字段时同时验证恢复、统计、轮换和反馈读取的字段完整性。

#### 13. localStorage 恢复旧班表与当前规则不一致

- 问题：浏览器刷新或恢复时加载旧班表，用户误以为这是按当前规则重新生成的结果。
- 原因：旧班表没有规则版本指纹，刷新行为与重新排班行为混淆。
- 尝试：保存规则指纹，恢复时标记 `schedulePolicyStale`，并在页面提示需要重新排班。
- 结论：刷新只恢复数据，不代表重新计算；规则升级后旧班表只能作为待复核/待重排数据。
- 怎么防再犯：所有规则或规则事实变更都要检查旧班表指纹、刷新提示和重新排班入口。

#### 14. Excel 导入导出边界不稳定

- 问题：曾出现 `XLSX.writeFile` 不下载、模板直接回写膨胀、无效工作表覆盖旧规则等问题。
- 原因：浏览器下载能力、XLSX 二进制结构和“工作表存在/有效”语义没有单独建模。
- 尝试：改用 Blob 下载，采用定点 XML 更新，按工作表存在性和逐行校验决定是否替换，并增加真实 XLSX 往返测试。
- 结论：导入必须先预览和校验，单张表失败不能静默清空或覆盖原配置；格式合同不能因内部实现改变。
- 怎么防再犯：Excel 改动必须覆盖正常文件、合法空表、无效行、缺行、重复项、真实二进制往返和旧格式迁移。

#### 15. 数据容量接近 localStorage 上限

- 问题：历史记录增长后接近浏览器容量上限，固定阈值不足以反映实际剩余空间，可能造成保存失败或用户误以为已保存。
- 原因：只按固定 4MB 警告，没有估算序列化大小和捕获写入异常。
- 尝试：按待写入数据估算容量，接近上限时明确报警，不自动删除历史。
- 结论：容量不足必须显式失败并保留旧数据，不能静默丢数据或擅自清理。
- 怎么防再犯：存储改动必须测试接近上限、写入失败、旧数据保留和用户可见提示。

#### 16. 在线航班查询受外部环境限制

- 问题：在线查询曾因 CORS 或第三方接口无法按未来日期提供完整机场列表而失败。
- 原因：远程接口不是本地排班事实源，网络、浏览器策略和第三方能力不可控。
- 尝试：增加对账预览和人工确认，保留本地航班模板作为主数据，不让查询结果直接覆盖现有班表。
- 结论：在线查询只能作为建议输入，不能成为排班成功的必要条件。
- 怎么防再犯：外部请求必须有失败提示、人工确认、去重和不破坏本地数据的回退测试。

### 测试、反馈与工程流程

#### 17. 测试夹具过于简单

- 问题：测试全绿但真实页面结果错误，测试没有覆盖历史、后置轮岗、真实 CX 时段和页面恢复状态。
- 原因：测试只验证局部函数或初始候选，没有从最终 `assignments` 和用户入口验证行为。
- 尝试：按风险分层测试，增加真实入口、后置改坏、规则版本、恢复和页面验收。
- 结论：自动测试通过不等于需求完成，必须验证最终可观察结果。
- 怎么防再犯：每条硬规则至少有单元/模型测试、后置反向测试、最终结果测试和真实页面验收记录。

## 2026-09-14：KE166 机动督导空缺由阻断改为告警

- 问题：KE166 顶部机动督导无法形成完整安排时，最终守卫拒绝整张班表，导致其他已经安全排出的岗位也无法交付。
- 原因：`createKe166SnapshotScheduleGuard()` 没有声明 warning 级别，默认按阻断错误处理；页面也没有针对 KE166 督导空缺的专用标红和说明。
- 尝试：先用 ledger、完整排班入口和 Lit 页面三层回归复现，确认原实现分别抛出守卫错误、拒绝班表、缺少专用红色空格；本次不扩大到 KE166 重排器漏解。
- 结论：仅将 KE166 最终快照守卫降为 warning。既有独立安排、兼任和安全重排均失败时，保留 `unfilled` 并生成班表，warning 提示人工复核，人员空格标红显示“KE166机动督导未安排”；其他守卫不变。
- 怎么防再犯：长期保留守卫级别、完整入口和页面展示回归；修改 KE166 最终完整性时必须同时核对最终 assignment、warning sink 和可持久显示的空缺投影。

## 2026-09-14：申报/送资料二合一岗位统一归入送资料

- 问题：同一岗位规则同时包含“申报”和“送资料”时，统计曾分别累计两类，一般同岗轮换、末班候选、后置复核、页面和次数 Excel 也可能形成不同口径；初版修复后，旧申报修正虽然已从跨航班合计和排班算法排除，仍会被航班单格小计隐藏计入。
- 原因：申报和送资料原先按关键词独立命中，缺少“二合一优先归送资料”的统一岗位语义；页面小计和 Excel 又直接遍历固定四类，没有消费航班实际适用类别。
- 尝试：先在 `spec.md` 和规则传播矩阵固定边界，再建立统一二合一判断，令末班频率和一般同岗轮换共同消费；统计投影增加航班适用类别，UI、人工修正和专用次数 Excel 改用该投影。完整测试暴露隐藏小计后，又先补红灯，确认旧申报修正把航班小计错误增加 5 次，再在领域统计层过滤不适用类别。
- 结论：二合一岗位只产生送资料 1 次；同航班独立申报岗仍产生申报 1 次；旧申报修正不迁移并停止参与统计与排班，专用次数文件包含不适用类别时整表拒绝。
- 怎么防再犯：岗位类别只由统一领域函数判定；新增或调整类别时同时覆盖历史、当前班表、人工账本、候选排序、求解目标、后置复核、一般轮换、航班小计、UI 和 Excel，并保留二合一、分开岗位及旧修正三组回归测试。

## 2026-09-14：安全凭证测试不再依赖运行当天

- 问题：完整测试在日期变化后失败，“停止并采用”用例的安全凭证写死 `2026-09-12`，而页面日期为实际当天 `2026-09-14`。
- 原因：测试夹具把动态页面日期写成固定常量，触发了本应生效的安全凭证日期一致性守卫。
- 尝试：定向复现确认产品守卫正确拒绝过期凭证，没有放宽守卫，只让测试凭证读取协调器当前日期。
- 结论：这是日期敏感测试缺陷，不是二合一规则回归；产品安全逻辑保持不变。
- 怎么防再犯：凡测试入口默认使用当天日期时，凭证、归档和断言必须从同一视图状态派生日期，除非用例明确冻结系统时间。

## 2026-09-14：Worker 失败不再静默回退主线程

- 问题：Worker 返回错误、触发 `onerror`、构造失败或发送数据失败后，系统会静默在浏览器主线程重新执行完整排班，页面因此长时间无响应，Worker 的原始错误也只有主线程重试再次失败时才会显示。
- 原因：`schedule-runner.ts` 把所有 Worker 故障统一交给 `retryOnMainThread`，没有区分“浏览器根本不支持 Worker”和“Worker 已受支持但本次运行失败”。
- 尝试：先将四种 Worker 故障测试改为要求立即 reject；旧实现的 5 个用例均失败，并各自耗时约 18 至 20 秒完成一次主线程排班。随后删除故障重试路径，按创建、运行和数据发送阶段保留原始错误原因，失败时终止 Worker 并清除安全快照。
- 结论：只有 `typeof Worker === "undefined"` 时允许主线程回退；Worker 已存在后的任何上述故障都直接失败，由现有应用错误提示交给用户决定是否重试。
- 怎么防再犯：Worker 边界测试必须分别覆盖协议错误、`onerror`、构造、`postMessage`、安全快照清除、资源释放和无 Worker 回退；不得用“自动恢复”掩盖后台线程故障。

#### 18. 反馈使用求解器术语，用户无法理解

- 问题：页面出现 `infeasible`、`changed-assignment-count`、“双向岗位资质”等内部术语，用户无法判断下一步。
- 原因：反馈直接暴露内部诊断文本，没有独立的用户语言层。
- 尝试：按“人员 -> 事实 -> 原因 -> 决定 -> 结果”重写警告，保留内部诊断供开发排查。
- 结论：内部错误、规则诊断和用户提示必须分层，用户提示应说明谁、什么时间、为什么、系统做了什么。
- 怎么防再犯：新增错误路径时同时提供内部日志/诊断和简洁用户文案，并测试关键文案不泄露内部术语。

#### 19. 页面刷新被误当成重新计算

- 问题：用户刷新页面后看到旧班表，却以为系统已经按最新规则重新排班。
- 原因：恢复旧状态没有明确的过期标记和提示，刷新与计算在交互上没有区分。
- 尝试：增加规则指纹和 `schedulePolicyStale` 提示，明确告知需要点击重新排班。
- 结论：恢复、重新计算和安装是三个不同状态转换，不能用页面加载成功代替排班成功。
- 怎么防再犯：页面验收必须包含刷新、旧规则恢复、重新排班、失败保留旧班表四个步骤。

#### 20. 只验证局部行为，缺少反向回归

- 问题：同一个 bug 修复后没有长期测试，后续改动容易重新引入；合法结果被后置改坏时无人拦截。
- 原因：测试偏向“当前代码返回什么”，没有围绕用户不变量和失败回退设计。
- 尝试：建立红灯 -> 最小修复 -> 绿灯流程，增加隔离测试、回归测试和最终守卫测试。
- 结论：每个已确认 bug 都必须留下保护用户可观察行为的回归测试；软目标不能误做成拒绝门禁。
- 怎么防再犯：按 `AGENTS.md` 执行先读历史、先写失败测试、建立传播矩阵、分层验证和交付报告。

#### 21. 过早认定“旧班表”或“两个根因”

- 问题：遇到错误结果时曾先假设是旧班表，或把同一根因拆成多个问题，导致修复方向发散。
- 原因：没有先核对最终结果、决策证据、完整测试差异和撤销对比验证。
- 尝试：通过 git 对比、失败分类、最终 assignments 检查和隔离测试重新归因。
- 结论：先建立可重复的失败信号，再区分现象、根因和间接恢复；不要按错误消息数量推断根因数量。
- 怎么防再犯：诊断时记录证据、假设和反证；修复前后都跑原始复现和完整链路验证。

## 2026-09-14：TR121/H02 增加工作班冷却保护

- 问题：同一人员承担 TR121/H02 一号后只间隔两三个工作班又被安排，末班重点岗位的月度频率均衡无法表达“短期先让别人承担”。
- 原因：原实现只有连续承担和月度/最近工作班次数比较，没有针对 TR121/H02 的工作班冷却事实；值班晚撤排序和后置重排安全也没有消费该规则。
- 尝试：先在真实 `generateSchedule` 入口加入红灯，复现最近工作班人员被选中的旧行为；随后新增统一冷却事实 owner、0 至 30 配置、候选字典序目标、值班目标后移、后置重排质量复核和突破决策记录。
- 结论：默认避开随后 3 个已归档工作班；冷却优先于月度轮次，但不硬删候选，所有人冷却时为保证岗位完整选择距离上次承担最久者；月上限 2 次、硬约束和岗位完整性仍优先，coverage 可在减少空缺时突破。
- 怎么防再犯：冷却只由 `tr121-h02-cooldown.ts` 判断；任何候选、值班、覆盖、恢复、轮岗、KE166 收尾改动都必须通过传播矩阵和后置反向测试，不能在局部模块复制 H02 关键词判断。

## 2026-09-14：统计页增加末班重点岗位展示筛选

- 问题：统计页末班重点岗位表只能显示四类合计，查看所有人员的某一类次数需要逐个展开单元格。
- 原因：页面汇总表固定读取 `row.totalCount`，没有展示层的当前类别状态；明细和统计算法本身已经提供四类分类次数。
- 尝试：在统计月份同一工具栏加入“全部、督导、一号、申报、送资料”紧凑按钮，页面只在渲染时从已有分类结果选择汇总值；默认仍显示四类合计，移动端沿用工具栏换行。
- 结论：筛选只改变表格列名和展示数字，不写入状态、不改变统计计算、人工修正、排班逻辑或 localStorage；明细展开和加减入口保持原样。
- 怎么防再犯：展示筛选必须消费现有统计投影，不在 UI 复制历史计数或类别判定；新增类别按钮时同时保留默认“全部”、选中态、窄屏换行和各类别展示回归。

## 2026-09-14：上午半休误伤凌晨与 KE166 后置收尾

- 问题：选择叶琳为上午半休后，上午岗位空缺未被有效补位；同时晚间/凌晨岗位被错误减少，KE166 收尾还可能把半休人员全天排除。
- 原因：半休规则复用了宽泛的 `isPreNoonFlight`，将 `01:00` 跨午夜航班当作上午；KE166 finalizer 和兼任候选又直接检查 `activeStaffIds`，没有按任务时段判断。
- 尝试：用 2026-09-14 的最小场景先写红灯，确认 08:00 岗位可补但 01:00 被留空；再针对晚间 KE166 收尾建立回归。
- 结论：半休事实 owner 新增运营日早班判定（06:00 至 12:00），凌晨（00:00 至 06:00）不受上午半休限制；KE166 两条候选路径改为调用统一 `halfRestPeriodViolation`，只在任务落入禁排时段排除。
- 怎么防再犯：涉及半休的初始模型、值班目标、补缺、恢复、轮岗、KE166 收尾和最终检查必须使用同一时段函数；回归同时覆盖上午补位、下午/晚间保留、凌晨保留和 KE166 后置候选。

## 2026-09-14 KE166/H02 轮岗提示把不同人员的原因合并

- 问题：黄色岗位提示将候选人的拒绝原因去重汇总，无法知道每个人为什么没被自动换上，人工交换弹窗也没有同步展示自动未轮换的解释。
- 原因：重排候选检查仅传出无人员归属的原因文字，警告又压缩成最多两个概括原因。
- 尝试：在不更改资格判断、求解和提交条件的前提下，保留原候选检查的逐人诊断；轮岗警告、悬停及交换弹窗复用同一份人名与上一归档末班岗位证据。
- 结论：有明确个人拒绝原因时逐人说明；完整换位无解或计算超时分别说明，不能把手工允许调整误写成自动允许换人。
- 怎么防再犯：保留从轮岗入口到警告的逐人回归，并核对悬停与弹窗共用同一份文字且不改变最终岗位安排。

## 2026-09-15：KE166 初排预留方案触及值班与岗位完整性冲突（未实施）

- 问题：owner 希望在普通岗位初排时保留能在 KE166 合法柜台兼任机动督导的人员，同时保持 CX937 等其他航班满岗；原先仅按督导基础合格人数恰好为 1 预留，不覆盖“纸面合格多人、实际只有一人可兼任”的情况。
- 原因：`KE166` 顶部督导在整体普通柜台模型之后收尾。提高预留目标时，若简单把全部普通岗位完整性整体移到 KE166 前面，会连带改变已有的 `KE166 → 值班 → 岗位完整性` 优先关系；独立人员的预留目标若不继续比较既有次日值班保护，还会改变同等合格人选之间的选择。
- 尝试：先填传播矩阵；从真实 `generateSchedule` 和初排入口写三种场景及多人纸面合格场景。后者红灯：初排把可兼任人员分给重叠航班，KE166 柜台交给其他人。临时引入初排独立/兼任预留目标并整体提前普通岗位完整性后，目标场景转绿，但相邻 134 项完整排班回归有 2 项失败：独立督导改选次日值班人员；没有 KE166 柜台但可由其他人接替值班目标时，KE166 督导反而留空。
- 结论：该实现方向尚未安全；已撤回本轮产品代码、规格和测试改动，保留矩阵作为待裁决草案。此前两项 KE166/值班回归不得改成新的错误预期；需明确“其他航班岗位完整性”和既有 KE166/值班、次日值班保护之间的边界后重新设计，再做红灯和最终班表回归。
- 怎么防再犯：不可只依靠预留成功这一项测试，更不能为了保护 CX937 无差别交换中央目标桶；同时验证不能挖空其他航班、KE166 与值班目标顺序、次日值班保护、独立优先、兼任关联和最终 ledger/schedule-guard。

## 2026-09-15：KE166 督导初排预留与值班身份解耦

- 问题：两名纸面合格督导中一人被另一唯一资质岗位占用，另一人兼值班并被冲突的 CX937 晚撤目标占走；KE166 柜台由可替代者承担，督导收尾缺人。
- 原因：初排只在督导基础合格人数恰好为 1 时预留；多名基础合格但仅一人实际可用时，值班晚撤目标先于岗位完整性锁定，后置再换无法保证原航班岗位不空。
- 尝试：先用公共初排和最终排班入口写红灯；首次无柜台夹具留有空闲独立督导，是测试边界错误，修正为全部人员被普通岗位占用且 KE 柜台禁止兼任后才保留回归。未采用此前“全部完整性整体提前、独立人员分别最大化”的方案；新模型只在现有两项普通岗位完整性目标之后比较“至少一名督导可独立或在合法柜台兼任”，沿用自动资格与兼任规则。复核时新增两段早班合计已满 8 小时的红灯，确认单独两两检查衔接仍可能高估独立人选，补入整日工时约束并将合法柜台兼任工时去重。
- 结论：值班身份及上一班承担末班重点岗位都不自动排除 KE166 督导；具体值班晚撤岗位与 KE166 冲突时，优先完整普通岗位与 KE 可完成性，晚撤目标按既有规则处理。独立优先、无可行兼任时标红留空均沿用原收尾与守卫。
- 怎么防再犯：同时检查初排和最终 assignments，覆盖 CX937 满岗、独立优先、禁止兼任留空、值班身份和次日值班既有回归；不能仅看基础合格人数或把值班身份当作人员硬锁。
- 验证：第一轮初排人员断言红灯后转绿；第二轮累计工时红灯后 4 项通过。第一轮相邻 190 项通过；第二轮修复后的完整 `npm.cmd run verify` 退出 0（普通测试 828 项、性能测试 16 项、构建成功）。尚未在真实浏览器使用 9/18 当天配置手动验收。

## 2026-09-15：跨航班重点岗位优先的列表顺序不是航班排班顺序

- 问题：看到 CX937 排在 KE166 前面、人员去了 CX 柜台时，容易直接认定系统先排 CX，或认为把 KE166 上移就一定能保住它的普通柜台。
- 原因：普通岗位进入同一个整体模型；规则按航班号和岗位名称精确匹配，H02、G20 等普通柜台只有被勾选才参与。启用的多条规则按列表顺序进入逐层目标，但仅在时段重叠、人员同时有资格且确有可行让步时影响选择；硬约束、岗位完整性、值班和 KE166 专项预留等前序目标不能因此让步。
- 尝试：对照规则匹配函数、整体模型的重叠候选目标、目标桶执行顺序，以及 KE166/H02 与 CX/G20 重叠岗位的模型测试；未以实际 9/20 浏览器班表验证上移列表后的结果。
- 结论：列表上移只提高已匹配规则之间的相对优先级，不改变普通航班的逐个排班顺序，也不能保证某个人必定去 KE166；这不是已确认的开关失效或岗位关键词漏匹配故障。
- 怎么防再犯：排查跨航班抢人时，先核对两条规则是否启用且精确勾选岗位、航班是否重叠、人员是否同时合格，再核对岗位完整性、值班和 KE166 预留等前序目标；不能只凭列表位置或最终去向归因。

## 2026-09-15：最后一个软目标超出允许差距不再拒绝整张班表

- 问题：使用真实配置执行“归档并排后天”时，Worker 在 15% 后报告“当天排班计算未完成，请重试”；同一配置缺少完整历史时可以生成，加入页面的累计历史后稳定失败。
- 原因：当天整体模型已经完成所有核心目标及前序软目标，并保存了完整可行方案；最后一层“历史疲劳”求解得到的相对差距约为 5.52%，超过合同允许的 5%。HiGHS 适配器把这次软目标改进未达标错误升级成全局 `failed`，丢弃了上一层已经保存的完整方案。外层“Worker 运行失败”只是包装，不是 Worker 崩溃。
- 尝试：使用 owner 导出的完整浏览器状态程序化还原归档与后天航班选择，先定位到 `candidate:historical-fatigue`；再用匿名两层目标测试固定“核心目标完成、软目标结果超 gap”的边界，旧实现红灯返回 `failed`。修复后用同一完整状态走公共排班入口，最终安全复核通过并成功生成。
- 结论：仅当超出允许差距的是 `best-effort` 目标且存在上一份完整方案时，停止继续优化并返回上一方案，质量标记为 `time-limited-feasible`；不采用超范围的新解。核心 `required` 目标不放宽，没有完整方案时仍失败。
- 怎么防再犯：求解器适配器必须区分“核心目标失败”和“软目标本层没有合格改进”；时间耗尽、无 incumbent、结果超出允许 gap 三种软层退出都要验证是否能回退上一份完整方案，并保留真实入口的最终安全复核。

## 2026-09-15：新增可配置的同航班人员互斥规则

- 问题：两名办理效率较慢的人员需要避免被安排在同一航班；限制还要支持只针对指定航班，人工拖拽则应允许操作但明确报警。
- 原因：原系统没有人员对互斥这一业务事实，初排模型、补缺/恢复/轮岗、人工调整、最终守卫和配置导入导出都没有统一的判断来源。
- 尝试：先在整体模型、后置候选、人工调整和最终 ledger/schedule-guard 写红灯；新增唯一事实 owner、规则合同、规则页卡片、localStorage/Excel 往返和人工突破回归。补查配置变更路径后，又为航班模板改名、人员编号改名和删除人员补上互斥规则同步测试与实现。
- 结论：互斥规则可配置多条；航班留空表示全部航班，指定航班按规范化航班号匹配，不同航班不触发，同一人员在同航班兼任不触发。自动链路在无法同时安排两人时只保留可行的一人、另一岗位留空并交由完整性提示；人工调整允许突破并写入红色警告，下一次自动排班重新执行硬互斥。
- 怎么防再犯：所有自动写入路径必须消费同一互斥 owner，并由最终守卫扫描完整 assignments；配置表缺失时保留旧值、空表才清空、非法人员行整表拒绝；模板/人员标识变更必须同步互斥配置，不能只更新资格、历史或岗位规则。

## 2026-09-16：独立排班脚本必须复用本地排班页面口径

- 问题：脚本读取同一份 Excel、从零连续跑 14 天后得到“肖萍 7 次、杨洁涵 2 次”等不均衡结果，但通过 `127.0.0.1` 打开的本地排班系统页面从零排班明显更均衡；直接把脚本结果当成算法问题，结论不可靠。
- 原因：旧脚本按自然日推进，页面按项目定义的下一个工作班推进；旧脚本没有完整复用页面背后的归档、班表安装和统计入口；旧脚本只统计本次生成结果，没有同时计算“历史 + 当前班表 + 人工修正”的统计页口径；也没有打印关键岗位最终人员、`decisionTrace`、warning 和候选拒绝原因。
- 尝试：将脚本改为支持 `--zero --excel` 和 `--state` 两种初始模式；按工作班间隔推进；复用页面相同的归档、班表安装和月度统计入口；支持半休参数；增加 TR121/H02、KE166 机动督导每日明细、决策证据和候选诊断；使用真实 Excel 从零运行 14 个工作班。
- 结论：修正口径后，14 个工作班全部成功生成；TR121/H02 的 11 名合格人员均至少承担 1 次，只有完成第一轮后才出现第 2 次，H02 本身是均衡的。旧的“7 比 2”不能作为轮换失效证据，主要是测试脚本与本地排班页面的输入、日期推进和统计口径不等价。
- 怎么防再犯：独立测试脚本必须复用本地 `127.0.0.1` 排班页面背后的公共状态、归档、安装和统计入口，不得自行复制一套历史推进或统计逻辑；明确区分“本次新增次数”和“本地页面累计口径”；排班测试按工作班推进而不是默认按自然日推进；每天输出关键岗位最终结果和候选拒绝诊断；交付时明确脚本未覆盖的 Worker 启动、页面渲染等边界，以及尚未实际长跑验证的模式和参数。

## 2026-09-16：标准 Skill 安装不应过度排查

- 问题：owner 只要求从明确的 GitHub 仓库安装一个 Codex skill，并让它能在 VS Code 中通过 `/` 调用；实际过程在安装之外又检查仓库、读取完整说明并反复查询 VS Code/Codex 文档，造成不必要等待。
- 原因：没有把任务收敛为“找到 `SKILL.md` -> 使用官方安装脚本 -> 验证目标文件存在 -> 告知重载方式”，把低风险安装误当成需要全面验证的集成排查；GitHub API 限流后虽已通过只读 clone 找到路径，仍继续做了与成功交付无关的探索。
- 尝试：先调用 GitHub API 检查仓库结构，限流后浅克隆仓库并定位 `skills/karpathy-guidelines/SKILL.md`，再用官方 `skill-installer` 成功安装；安装后又额外检索官方文档和本机文件。
- 结论：本次实际安装命令约 3 秒完成，后续大部分耗时没有提高安装结果的可靠性。对结构明确的公开 skill 仓库，应优先执行最短官方安装流程；目标目录和 `SKILL.md` 存在即满足安装验收。
- 怎么防再犯：同类请求先用 `skill-installer` 的官方脚本直接安装；只有脚本因路径、权限、网络或结构失败时，才按失败证据逐级检查仓库。成功后只验证安装目录与 `SKILL.md`，说明 VS Code 重载或新建会话后使用 `/`，然后停止，不追加无关检索。

## 2026-09-18：KE166 督导兼任掩盖真实人手缺口

- 问题：`KE166` 机动督导兼任普通柜台后，两个岗位表面都已填，但实际只有一名真人；原模型仍按普通岗位完整性把另一名可用常规人员放到重叠的 `CX937`，导致配置中优先级更高的 `KE166` 没有获得应有的第二名真人。
- 原因：顶部机动督导在普通柜台模型后收尾，初排模型只判断“之后是否还能找到一名督导”，没有把“独立督导 + 已配置优先柜台”建模为两名不同常规人员容量；第一次补目标时又只收集了督导本人对柜台的选择，漏掉其他常规人员对同一柜台的选择，因此目标最多只能看到 1 人。
- 尝试：先用 `KE166 09:15-11:15`、`CX937 09:25-11:25` 和两名常规人员建立最终入口红灯；调试模型确认容量目标缺少第二人的选择变量。修正收集范围后，发现全局绝对目标会腾空无关唯一资质岗位，于是把生效范围收窄为规则页中排在 `KE166` 后方的重叠岗位，并在容量目标后先保护其他重叠岗位。随后补行政支援不计数、无人时兼任兜底和 Ledger 后置改坏反向测试。
- 结论：当允许兼任的 `KE166` 柜台在“跨航班重点岗位优先”中排在 `CX937` 等重叠岗位上方时，督导与柜台按两名不同常规人员计算；有安全常规人员就留给 `KE166`，必要时较低优先岗位留空。A1-A6 行政支援不计入；真无第二名常规人员时仍允许合法兼任；无关唯一岗位不为此腾空。
- 怎么防再犯：凡是“一人兼任两格”的规则，岗位填充数和不同真人容量必须分开建模；跨航班优先的特殊留空例外必须消费页面顺序，不能扩大到未排在其后的岗位；失败测试要同时覆盖模型选择、最终 `generateSchedule()` 结果、行政支援边界、不可行回退、无关岗位不变和后置 Ledger 拒绝改坏。

## 2026-09-18：双组共享配置与严格隔离工作区（第一阶段）

- 问题：两组人员需要共用同一套规则、航班模板和岗位配置，但人员、当前班表、历史、值班和备勤必须完全独立；旧状态只有一套根级数据，无法表达组边界。
- 原因：现有 `AppState` 将共享配置和组内事实混在同一层，恢复、排班和历史查询天然只有一个工作区。
- 尝试：先建立共享配置、A/B 工作区和当前组标识的数据模型；新增迁移红灯，验证旧单组状态整体进入 A、B 保持空、共享配置只保留一份；本阶段不接排班和页面消费，避免局部接入造成跨组半迁移。
- 结论：状态版本升级到 6；旧单组状态迁移到默认 A 组，B 组不复制人员或历史；共享规则、航班模板、岗位配置由 `shared` 持有，组内事实由 `groups` 持有。后续排班、统计、导入导出和切组必须只消费当前组。
- 怎么防再犯：任何冷却、恢复、轮岗、值班、统计或排班入口都必须显式接收当前组工作区；禁止从另一组读取人员或历史；新增跨组行为前先重新确认业务合同并扩展传播矩阵。

## 2026-09-18：双组切换先保存，当前组投影隔离（第二阶段）

- 问题：数据模型已拆出 A/B 工作区，但现有领域模块仍读取根级 `AppState` 字段；若直接切组，可能继续显示上一组人员、班表或历史。
- 原因：排班与页面入口尚未全部改成显式工作区参数，必须先有一个统一的当前组投影边界。
- 尝试：在 Store 增加当前组投影同步、`switchGroup` 和 dirty 状态；所有现有命令执行后写回当前组；页面增加 A/B 选择器，切组前有未保存改动时先提示保存，取消则保持原组；切换后清空半休和历史编辑临时状态。
- 结论：当前阶段采用兼容投影，不复制共享规则；A/B 人员、航班、历史、值班和班表切换后互不串用。导入导出和领域模块显式工作区改造仍未完成。
- 怎么防再犯：后续每个导入、排班、统计、值班入口都必须证明只消费当前组；切组测试要覆盖未保存确认、取消切换、B 组空数据和切回 A 组保留原数据。

## 2026-09-18：双组严格隔离闭环（第三阶段）

- 问题：数据模型和切组已经存在，但排班、统计、历史、值班、备勤及配置 Excel 仍可能从根级投影或另一组编号取数；人员新增/改编号也可能制造跨组重复。
- 原因：兼容投影尚未在所有入口形成单一边界；共享岗位资质和同航班人员规则同时承载两组人员编号，不能简单整表覆盖当前组。
- 尝试：先写当前组 Worker、统计/历史、值班/备勤、导入导出和跨组编号冲突红灯；Store 初始化先从 `shared/groups` 激活当前投影；新增/改编号与导入拒绝跨组重复；共享岗位资质和人员型规则导入/导出按当前组合并，保留另一组编号。
- 结论：排班只发送当前组人员、历史和值班事实；统计、历史、值班、备勤和当天航班只读当前组；配置导出默认当前组人员/航班/资质，规则仍共享；配置导入默认写当前组且不抹掉另一组人员型规则。
- 怎么防再犯：新增入口必须通过 Store 当前组投影；任何共享配置中带人员编号的字段必须区分当前组替换与另一组保留；两组人员编号在新增、修改、导入三条路径都要拒绝冲突；回归测试同时覆盖 A→B→A 切换、Worker 请求、统计/历史、值班/备勤和 Excel 往返。

## 2026-09-20：跨航班重点优先从岗位对象改为人员对象

- 问题：原“跨航班重点岗位优先”选择的是航班和岗位，无法直接表达“叶琳必须优先留给 KE166，不论她最后落在 KE166 的哪个合法岗位”；规则名称和实际保护对象也容易让排班员误解。
- 原因：唯一事实、HiGHS 目标、后置重排、决策反馈、KE166 兼任容量、规则页、Excel 和 localStorage 全都保存或匹配岗位名称。只改页面名称会留下多套含义；旧岗位列表也无法可靠推断应该选择哪些人员。
- 尝试：先把领域红灯改为 `flightNo + staffId`，再沿完整传播链切换唯一事实；补充同航班换岗、低优先行调往高优先行、后置调离、KE166 第二真人、行政支援不计数、A/B 组编号保留、人员改号/删除、Excel 非法编号和 v6 迁移回归。迁移方案冲突处没有猜测岗位对应人员，而是保留规则行、航班和启用状态，清空旧选择让用户重选。
- 结论：规则更名为“跨航班重点人员优先”，每行保存“优先航班 + 人员编号列表”。同一选中人员在重叠航班都有合法岗位时按页面从上到下决定去向；只在一行选中时也优先留在该航班，同航班内部换岗位仍满足规则。一般规则不突破硬约束或制造空缺；KE166 兼任额外真人继续是明确例外，可让更低行重叠航班留空。状态升到 v7，旧岗位选择不自动转成人员；旧工作表名不作为长期别名继续识别。
- 怎么防再犯：改变规则对象时必须同时替换唯一事实和每个消费者，不能保留岗位字段作影子兼容；有序人员规则的后置守卫必须允许“低优先行 → 高优先行”，同时拒绝反向调动；共享规则中的人员编号必须按当前组编辑/导出并保留另一组编号；任何不可逆语义迁移都要选择“不猜测”并用迁移测试固定。

## 2026-09-20：引导来源被岗位分类截断，普通空缺样式退回白底

- 问题：引导人员没有取该航班整条岗位链最下面岗位上的人；普通空缺虽然存在 `is-unfilled` 状态类，排班表中却仍显示白底，无法一眼发现。
- 原因：初排、补缺、人工刷新和人工资格校验分别把引导来源重复写死为“常规”分类，导致更靠下的机动督导或分流岗位被跳过；通用 `.schedule-cell.is-unfilled` 样式又明确设置成白底，只有半休和 KE166 特例另行标红，黄色提示还可能覆盖空缺颜色。
- 尝试：先核对现有行政支援模式开关，确认无需新增开关；再用“常规岗 → 分流岗 → 引导岗”的具体顺序固定最下方选择，并把四条路径收敛到同一个引导来源判断；空缺继续复用现有 `is-unfilled` 投影，只修正样式和与黄色提示同时存在时的优先级。
- 结论：引导从同航班整条显示链由下向上选择正常常规人员，排除引导岗位自身和行政支援分类岗位；所有无人员岗位统一标红，特殊空缺说明继续保留。
- 怎么防再犯：引导来源资格只能由 `guideSourceStaff()` 定义，初排、补缺和人工路径不得复制分类判断；空缺是否存在由 assignment 投影决定，颜色不得只为某个业务特例单独实现。

## 2026-09-20：纠正“行政支援岗位”与“行政支援人员”混淆

- 问题：上一轮把“行政支援除外”错误理解为排除行政支援岗位，导致行政支援岗位即使由常规人员承担，也不能成为引导来源；这与实际业务要求相反。
- 原因：`guideSourceStaff()` 同时检查了岗位分类和人员类型，把两种不同事实合并成一个排除条件；三条引导排序路径虽已共用人员资格判断，却没有统一表达“行政岗位常规人员优先”的排序层级。
- 尝试：先核对现有 `adminSupportEnabled` 开关、HiGHS 任务和行政支援回归，确认行政岗位当前已经是“非行政岗位先填、剩余常规人员再填、无人则留空”；不改这条成熟链路。随后只纠正引导来源事实，并增加统一比较器供初排、补缺和人工刷新消费。
- 结论：行政支援岗位由常规人员承担时，该人员是引导第一优先来源；没有这类来源时才取整条岗位链最下面的其他正常常规人员。排除依据只看人员类型，行政支援人员本人永远不能成为引导。
- 怎么防再犯：涉及“行政支援”时必须分别写清 `PositionRule.category` 与 `Staff.staffType`；来源资格与来源排序分别保留单一事实 owner，并用“行政岗位 + 常规人员”和“普通岗位 + 行政人员”两个反向案例固定边界。

## 2026-09-20：历史编辑白名单误拦只读导出

- 问题：进入历史日期编辑后，Excel、HTML 和 PNG 导出按钮仍然显示，但点击只提示“请先保存或取消编辑”，无法导出正在编辑的历史班表。
- 原因：历史编辑使用统一命令白名单保护临时草稿，三个只读导出命令没有列入白名单，因此在到达导出控制器前就被拦截；底层导出本来已经读取当前历史草稿和目标日期。
- 尝试：没有放宽整个历史编辑门禁，只把 `export-schedule`、`export-share-html`、`export-share-png` 加入现有白名单；回归同时确认三个导出收到目标历史日期，并确认重新排班仍被拒绝。
- 结论：历史编辑期间可以导出目标日期的 Excel、HTML 和 PNG；重新排班、归档、切日期和配置修改等会改变状态的命令继续禁止。
- 怎么防再犯：事务式编辑门禁要区分只读命令和写命令；新增只读查看或导出入口时，必须覆盖历史草稿场景，并用反向断言证明写命令没有被一并放开。

## 2026-09-20：未达旅客门槛的正常空白被误标为缺员

- 问题：岗位配置的启用旅客人数高于当天航班预定人数时，系统按规则不自动排人，但页面仍给空白岗位添加 `is-unfilled`，与真正缺少合格人员的空缺一样标红。
- 原因：页面只按 `staffName` 是否为空决定通用空缺类，没有消费排班任务已经使用的旅客门槛事实；此前“所有无人员岗位统一标红”的展示结论遗漏了“未启用岗位是正常空白”这一边界。
- 尝试：先用页面公开 DOM 写 50/100、120/100 和零门槛三例红灯；再从 `schedule-tasks.ts` 提取现有门槛比较为唯一 helper，让原自动排班判断和页面共同消费。没有使用完整 `shouldAutoAssign()` 作为展示依据，因为它还包含引导、人工岗位和行政支援模式，可能误改其他空白格。
- 结论：未达到旅客门槛的普通空白保留可编辑输入但不标红；达到门槛或零门槛后的无人岗位继续标红。低于门槛时，半休和 KE166 专用空缺仍按原规则标红并保留说明；排班算法、assignment 状态和持久化结构不变。
- 怎么防再犯：空缺展示必须区分“岗位未启用”和“岗位已启用但缺人”；门槛比较只能消费 `meetsPassengerThreshold()`，不得在 UI 再写一套数值比较；页面回归固定三种门槛边界和专用空缺反例。

## 2026-09-22：普通重点岗位改为集合驱动并与末班账本隔离

- 问题：重点岗位原先由五类关键词自动识别，无法按岗位名单维护；末班手改次数也不适合表达普通重点岗位跨航司轮换。
- 原因：一般同岗频率、连续轮岗和末班统计共用关键词入口，普通岗位没有独立集合与“月份+人员+航司+规范岗位”账本。
- 尝试：先固定业务合同和传播矩阵，再为普通重点集合、普通次数修正和旧状态迁移写红灯；保留末班四类统计与账本，不在 UI 或排班中复制第二套末班事实。
- 结论：普通重点只认集合，普通手改只消费一般同岗频率/连续轮岗；集合外关键词岗位回到高/低疲劳普通岗；末班重点继续使用原四类账本和统计页。
- 怎么防再犯：普通重点判定统一调用集合 owner；末班统计统一调用 `late-priority-*` owner；每次修改集合或普通账本都标记当前班表需重新排班，并保留末班隔离回归。

## 2026-09-22：普通重点热路径优化与性能门槛校准

- 问题：普通重点集合与次数账本接入后，十航班重复计算、默认班表基准和 105 岗压力用例超过原墙钟门槛。
- 原因：集合判断、普通重点历史索引和人工修正最初在高频路径重复计算；优化后真实规模恢复正常，但新增规则仍使重复运行和压力场景稳定高于旧版本。
- 尝试：将集合预编译并缓存规则命中，历史索引按航司短路，人工修正预聚合；同机数据降至默认 p99 约 30.35 秒、十航班单轮约 12.7 至 14.9 秒、六轮约 83.7 秒、压力五轮约 287.5 秒。
- 结论：保留十航班单轮 20 秒硬门槛；默认 p50/p99 调为 32 秒，十航班六轮合计调为 100 秒且外壳调为 110 秒，压力测试外壳调为 360 秒。
- 怎么防再犯：性能门槛变更必须同时保留单轮上限和完整性断言，并记录同机基线；不得用扩大外壳代替热路径优化。

## 2026-09-22：给其他 Agent 的提示词写太宽，导致只读广搜空转

- 问题：owner 要给另一会话做「普通重点岗位集合 + 统计式手改次数」。第一版交接提示词目标方向大致对，但写成「先读全套材料 / 先广搜现有链路 / 先澄清 5 个歧义再动手」，对方长时间只读、全仓 `rg`、开子任务，迟迟不进入可确认的拍板与实现。
- 原因：把「少造轮子、先对齐事实」写成了无边界探索任务；没有先圈死必读路径，也没有先把产品口径收成可勾选答案。广搜范围（怎么找代码）和功能范围（做什么）被混在同一份过长提示里，Agent 优先满足“读完再问”，表现为慢和散。
- 尝试：中途补发「停广搜、只问 5 题」；再根据现有代码指出必读窄路径（`position-rotation-policy`、`late-priority-*`、`statistics-page` / `statistics-actions`、`model` 中的末班手改字段等）；owner 拍板 `1A 2A 3A 4C 5A` 后，改成短提示：末班统计保持不变，另增「普通重点岗位」集合与手改账本，只接一般同岗频率与连续轮岗。
- 结论：第一版不是业务目标写反，而是提示词过宽、缺停条件。正确拆法是：① 先缩必读文件；② 用 A/B 题让 owner 一次拍板；③ 再用短实现提示锁定“做 / 不做”。未拍板前禁止全仓探索式搜索当进度。
- 怎么防再犯：
  1. 给其他 Agent 写交接提示时，默认短：目标一句、必读路径列表、明确不做项、确认前禁止广搜。
  2. 已知相关模块时直接点名文件，不要写“搜索整个 domain/app/ui”。
  3. 产品歧义改成带推荐答案的选择题，owner 用选项回复；不要让实现 Agent 先“读到自己会问”。
  4. 区分两句指令：“别广搜”管读代码范围；“缩短范围”管功能边界；两句都写清。
  5. 本类协作弯路也写入 `history.md`，不限于代码 bug。

# autoschedule 开发历史
