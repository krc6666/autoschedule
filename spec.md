# 自动排班产品规格

## 产品定位

自动排班是面向机场地勤保障团队的静态、本地优先排班工具，用于把航班、岗位资质、人员状态和历史疲劳合并为当天可执行的保障排班，并输出适合留档和转发的结果。

## 当前能力

- 管理人员编号、姓名、常规/行政支援类型、是否为分队长、CX航前资质、值班资质、备勤资质、夜班能力、在岗状态和备注；配置页人员信息默认收起，可独立展开编辑。分队长字段通常不参与候选排序、频率、疲劳或轮岗判断，只在“分队长并行督导补缺”规则满足全部前提时作为时间冲突硬约束的受控例外。人员状态变为病假或休假时，当前活动排班立即按最新人员状态重新计算；恢复正常后重新进入合格候选池。
- 在配置页维护可复用的航班计划模板（航班号、时段、岗位和备注）；在航班页输入航班号即可自动带出模板。
- 航班页可调用 `flight-query-tool.vercel.app` 查询武汉机场指定工作日的国际及地区出港计划；查询日期默认使用当前排班日期并允许临时修改，固定排除所选日期 `00:00-05:59` 起飞的航班、纳入次日 `00:00-05:59` 起飞的航班。查询后按“继续保留、建议新增、待确认删减”与当前航班计划对账：只有航班号与现有模板完全一致且当前计划尚未包含的航班可以勾选新增，新增使用模板中的保障开始时间、结束时间、岗位和备注，预定人数初始化为 0；线上未查到的当前航班只列为待删候选，默认不勾选，必须由排班员选择并二次确认后才删除。系统不得自动转换不同名称、覆盖现有航班数据或自动删除航班；查询失败、查询结果为空、查询日期与当前排班日期不一致时禁止批量删减并保留当前航班计划。成功增删后已有排班结果失效，必须重新排班；航班模板、岗位规则和历史记录不受影响。
- 管理航班时段、岗位清单、岗位资质、岗位疲劳点、常规/引导/机动督导/分流/行政支援分类和岗位启用旅客人数阈值；分流岗位可配置提前撤岗分钟，岗位规则按航班折叠，支持批量新增、上下调整和柜台编号一键降序。
- 导入人员、航班计划、航班配置模板、岗位配置、历史排班及月度值班备勤 Excel。
- 每次点击“生成排班”或“重新排班”只计算当前选中的日期，不提前生成未来工作班。当天全部需要自动安排的常规、分流岗位和全部满足基础资格的正常常规人员一次进入同一个 HiGHS 整体模型；模型先保证12点前岗位完整性和稀缺岗位，再按中央规则合同逐层固定值班、保护、公平和历史疲劳结果。所有业务质量目标值必须保持最优；达到相同业务质量后不再按人员编号逐个定稿，HiGHS 可以返回任一等价方案，因此少数同分人员可能在等价岗位间互换。`KE166` 顶部机动督导仍在普通柜台整体排定后单独收尾，引导、行政支援和人工临时岗位继续按各自规则处理。
- 当天整体模型最多计算 30 秒。求解超时、无解或安全复核失败时不采用任何中间结果，页面保留生成前的班表并使用普通业务语言提示失败，不显示求解目标、内部状态或变量名称；因人员状态、资质、夜班、时段冲突或工时上限导致的正常缺员可以留空，但必须显示具体原因。
- 排班计算运行期间只允许页面浏览、关闭提示和班表显示调整；再次计算、切换行政支援、修改业务数据、导入、归档和重置等操作统一提示等待，不得先修改状态或启动第二个后台任务。后台任务成功、失败或发送数据失败后都必须释放 Worker，随后允许正常重试。
- 自动阻止无资质、病休、夜班能力不足、时间冲突和超过每日工时上限的分配；同航班顶部“督导”拖入兼任范围允许的空白岗位时，是目标岗位资质、同航班时间冲突与重复工时规则的明确例外，但不得绕过机动督导兼任范围。
- 排班页按航班生成 Excel 式双列：每个航班表头横跨“岗位”和“人员”两列，岗位名称及岗位配置备注位于左格，人员姓名及当次临时备注位于右格；不同航班的岗位按各自配置从上到下独立排列，不按柜台字母或数字跨航班匹配。默认岗位列 64px、人员列 56px，航班多时横向滚动；支持在 70% 至 160% 之间缩放列宽和字号并在本机记住比例。航班标题显示到岗备注；性质为“机动督导”的岗位固定置于首行，其余岗位严格按岗位配置逐条生成和排序，只有引导岗位位于底部分隔线下方；每区保留可直接填写岗位和任意姓名的空白新增格，临时项可修改或删除；普通岗位按原约束拖拽，顶部督导只能拖入同航班兼任范围允许的空白岗位形成同步的督导兼任。
- 当前班表生成后，排班工具栏提供“对比上一班”开关。上一班严格取当前排班日期之前最近一个包含历史记录的已归档工作日，不机械按自然日减一天，也不读取当前日期或未来日期的归档；展开后在当前班表上方使用与历史页一致的只读航班双列表显示日期、航班、岗位、人员、工时和疲劳，当前班表继续保留在下方。对照表不得编辑或删除历史、不得改变当前状态或触发排班计算；没有更早归档时按钮显示“暂无上一班记录”并禁用。对照数据范围与现有历史归档一致，不额外纳入未归档的引导或行政支援岗位。
- 排班页可切换行政支援模式；关闭时只显示常规体系岗位，开启后同航班同名的行政支援岗位替换常规岗位并保持为空，没有行政同名项的常规岗位继续保留。行政支援人员在独立着色名单中维护；仅当没有满足硬约束的常规人员可用时，才允许把具备该岗位资质的行政支援人员拖入补位。行政支援岗位和行政支援人员均不计入工时、疲劳、负荷均衡、末班重点岗位恢复保护及后续疲劳归档。
- 人员负荷与疲劳表只统计常规人员；在岗位数量、岗位资质和硬约束允许时，自动排班优先覆盖当天尚未获得实际工时的正常常规人员，病假和休假人员不要求产生工时。
- 月度轮值必须按“值班 → CX航前 → 备勤”的顺序生成。月表仍只列与当前排班日期属于同一“上一休一”班组的本月工作日；每一行的值班和 CX 航前发生在该工作日，两名备勤发生在其后一个自然日的休息日，界面和统计必须显示实际次日备勤日期。值班拥有绝对优先级并最先安排：每名有值班资质的正常常规人员在当月工作日席位充足时至少值班 1 次，在此基础上继续均衡，使值班次数最多与最少的差值不超过 1；当月值班席位少于资质人数时，无法满足的缺额跨月轮换。
- 同一个工作班轮值块中的值班与 CX航前、两名次日备勤完全互斥：值班人员不得同时承担该工作班的 CX 航前或其后的次日备勤。CX航前与次日备勤可以由同一人承担；两名次日备勤人员之间仍不得重复。
- 值班完成后安排 CX航前，只使用具备 CX航前资质的正常常规人员，并使所有 CX航前资质人员的当月次数最多与最少相差不超过 1。唯一 CX 资质人员值班当天没有其他人选时，值班保持不变，CX航前留空。
- 值班完成后安排备勤。只有具备备勤资质的正常常规人员参加自动备勤轮值、手工调整和 Excel 导入；每名备勤资质人员在当月备勤席位充足时至少备勤 2 次，在满足保底后继续均衡，使备勤次数最多与最少的差值不超过 1。常规人员默认开启备勤资质，行政支援默认关闭；取消资质后，包含该人员的原人工轮值失效并重新按当前资质计算。资质人员不足时对应席位留空并提示，不允许用无资质人员兜底。若当月工作日数量不足，优先满足值班刚性要求，备勤不足 2 次的资质人员和实际次数在月表及反馈中说明，该缺额不计入违约。
- 排班主区在人员名单和班表右侧固定显示当日CX航前、值班和次日两名备勤，使默认比例下可同时核对班表与轮值人选；窄屏时摘要自动移到班表上方。完整的月度轮值明细位于独立“统计”页，CX航前与值班/备勤分别展开，按人员显示本月次数和实际日期；月表直接显示值班、航前和备勤差值、值班首轮覆盖、备勤 2 次保底及计划疲劳。月表可直接调整并实时更新统计，值班与备勤选择对方时只有双方调整后的岗位资质都满足才自动交换，CX航前可与任一次日备勤选择同一人，也可按日恢复默认轮值。月表提供与现行横向表相近的 Excel 下载：顶部合并标题，星期和 `MMDD` 双层日期表头，人员逐行，当前轮值预填红色 `24`、浅橙色“备勤”和浅色“休”；修改后可从专用入口重新导入。
- 值班备勤表导入以当前排班日期确定目标月份及本班工作日奇偶，识别工作表名称中的“值班/备勤”、横向日期表头、人员姓名以及单元格 `24/值班/备勤/休`。`24`或“值班”记为表头当天值班，“备勤”记入前一个工作班的次日备勤；“休”和空白不生成安排。导入前必须预览工作班日期、值班人员、实际次日备勤日期和两名备勤，确认后按表头实际覆盖的日历日期替换值班与备勤人工覆盖，但保留 CX 航前及未被表头覆盖的跨月席位；例如 7 月表没有 8 月 1 日列时，不得清空 7 月 31 日对应的次日备勤。未匹配或重名人员、非正常常规人员、无值班资质、无备勤资质、同日多名值班、超过两名备勤、轮值人员重合或与保留的 CX 航前冲突时阻止导入；缺少值班或不足两名备勤只警告并允许确认。导入结果视为人工调整，自动均衡不得覆盖；“重新均衡本月”经确认后可以清除这些人工安排。
- 值班任务默认计 12 点疲劳，当日值班卡片、月度计划疲劳和排班反馈均直接显示该分值；分值同时参与航班岗位候选排序、人员负荷和历史归档，CX航前与备勤不增加疲劳。排班员可维护有序的值班岗位优先项，每项包含启用状态、航班号和岗位或备注关键词；系统按顺序尝试第一个满足人员状态、岗位资质、夜班能力、时段冲突和工时上限硬约束的晚撤目标。默认顺序为 `TR121 / H02`、`TW616 / 一号`。全部配置项不可执行时，回退为依次检查最晚、倒数第二晚航班，并在每档中按一号、督导、申报、送资料排序；后备晚撤项每次只启用一项，当前项不可执行后才推进下一项，不得同时锁定全部后备项。值班人员还必须在最终班表中保留至少一个开始时间严格早于 `12:00` 的航班；上午的具体岗位可以被安全轮岗移动，晚撤岗位落位后按具体 assignment 锁定。两个值班目标完成后，值班人员可以继续承担有资质、时间不冲突且不超过工时上限的其他普通岗位，并正常参与疲劳、衔接和负荷比较，不得再因值班身份被整天排除；未采用的后备值班重点岗位仍优先交给其他人员，避免值班人员额外承担重点岗位。目标本身优先于同岗轮换、疲劳和岗位衔接保护，突破保护时写入决策记录与反馈。
- 排班结果下方提供两部分反馈，随自动排班和人工调整实时重算：第一部分“航班与人员安排”报告航班数量与密集程度、人员覆盖、当日工时/疲劳差和航班衔接；第二部分“规则执行情况”逐条报告12点前岗位完整性、高负荷保护、“上一工作日晚班人员跟踪”“本班末班人员预告”、12点前上午航班值班和值班晚撤等规则，明确显示“已执行”“需复核”或“无基准”及证据。上一班跟踪按人员使用“姓名 + 航班英文代码 + 一号/申报/督导/控制/送资料”紧凑列示，不显示时间、完整数字航班号、柜台号或疲劳点；本班仅说明该人员“已避开”或“未落实（航班英文代码 + 重点岗位标签 + 简短原因）”。本班预告仍列出最后一批中全部命中当前重点岗位规则的人员，供下个工作日恢复保护。班表格子的琥珀色警告每条最多两句话，固定按“人员 → 事实 → 原因 → 决定 → 结果”说明，不显示求解器状态、目标名称、变量名称或内部重排术语。12点前反馈必须列出“已突破严格限制仍安排”、因跨航班抽调产生的源岗位空缺，或“因合格人数不足而无法填满”的具体人数和原因；值班反馈逐人说明 CX 航前、值班和次日两名备勤的实际安排，并说明自动值班恢复交换或人工值班冲突。
- 左侧工作模块固定为 `总览 → 配置 → 航班 → 排班 → 规则 → 统计 → 历史`。第 5 个工作模块为独立的“规则”页，公开“当前排班规则清单”；每类规则和每条岗位衔接规则均默认收起，可独立展开编辑。页首使用规则名称、处理环节或说明进行搜索。机器执行规则必须直接来自中央 `SCHEDULING_RULES` 合同，用户页面通过唯一中文投影展示为 `必须遵守 → 优先安排 → 补齐岗位 → 保护与均衡 → 最后比较 → 结果检查`；内部规则 ID、执行器类型、阶段代码和依赖关系不得作为普通用户配置显示。候选判断使用具名字段逐项比较，不把严格衔接、日期或工时疲劳合成为不可解释的权重分。排班后分级轮岗读取最近两个已归档工作日：重点岗位和高疲劳普通岗位从上一班重复时开始复核，低疲劳普通岗位只在即将形成第三次连续承担时复核；空闲合格人员直接接替无解后，把同航班和全部目标时段重叠航班中已经排班的人员也纳入候选，按两人、三人、四人、五人依次寻找参与人数最少的安全闭环或以空闲合格人员补齐末端岗位的开放式连续腾挪。清单同时展示系统实际执行的岗位生成、岗位复用、岗位衔接、当前参数和调整入口。可配置值班疲劳点数、值班岗位有序优先项、机动督导兼任范围、月度轻松班次统计时段、当日/滚动工时与疲劳均衡、高负荷岗位衔接保护、滚动负荷上限、分级连续轮岗和跨工作日恢复保护；跨工作日恢复保护可新增、删除、启停和编辑多条末班重点岗位规则（适用航班、岗位名称或备注、关键词、可选的次班截止时间）及多条早班避让目标，另可新增多条结构化“岗位衔接间隔”规则。高负荷、滚动负荷和跨工作日恢复均只保留启用开关：启用后统一优先避开受保护人员，无安全替代时仍保证岗位完整；这些规则不提供没有执行差异的“强保护”选项。只有结构化岗位衔接间隔规则保留“优先避开/严格限制”强度；严格限制在12点以后阻止违规落位，12点前常规岗位没有未受限替代人员时允许突破并在反馈留痕。值班和 `KE166` 特殊锁定仍高于衔接保护。点击“保存规则”只保存设置：已有班表继续显示并标记“待重新排班”，规则执行反馈暂停；下一次点击“重新排班”或“归档并排后天”才生成执行新规则的班表。过期班表仍可导出或归档，但必须二次确认。
- 重点岗位频率均衡、安全重排与分级连续轮岗共用同岗轮换开关。重点岗位仅指性质为“常规”且岗位名称或备注包含“一号”“申报”“督导”“控制”“送资料”的岗位，并按同一“航班号 + 岗位”分别滚动轮换：所有合格人员完成第 N 次后，才允许任何人开始第 N+1 次。末班督导、一号、申报和送资料另按航班建立自然月与最近 8 个已归档工作班账本，固定按 `督导 → 一号 → 申报 → 送资料` 逐层比较；前一类达到当前最优值后才进入下一类。重点岗位频率高于恢复、疲劳、普通岗位衔接、当日在岗覆盖和跨工作班负荷互补；安全重排可在同航班及重叠航班内联动最多五人，但仍不得突破人员状态、人员类型、岗位资质、夜班能力、时间冲突、每日工时、值班或 `KE166` 锁定、岗位完整性和严格岗位衔接。`TR121 / H02` 按一号统计，自动排班时每名合格人员每个自然月最多 2 次；全部合格人员均达到 2 次时该岗位自动留空，人工拖拽允许超过 2 次。未命中重点岗位关键词的普通岗位继续按高疲劳、低疲劳分级轮岗，且不得撤销已经成立的重点岗位频率结果。
- 统计页按自然月展示常规人员的“提前下班”和“下午无航班”。提前下班只看每人当天最后一个实际航班的截载时间，必须严格早于可配置节点才计 1 次；同航班的机动督导、引导等复用记录不得重复计数，当日值班人员排除，备勤人员照常计入。下午无航班按可配置时段判断，当天参加过实际排班且没有任何航班与该时段重叠时计 1 次，早晚有航班但下午为空仍计入；值班和备勤人员均照常计入。病假、休假、行政支援和全天无排班人员不计；当前未归档排班覆盖同日历史，页面同时显示当天明细与每人本月累计。
- 统计页按自然月提供统一的“末班重点岗位统计”。航班选项只列当前实际结束严格晚于末班结束界线、且配置了对应常规岗位的航班；页面一次只展示选中航班，不同时展开全部航班。选中航班后分别显示“督导”“一号”“申报”“送资料”四类统计，岗位名称或备注包含对应文字即可归类，因此 `TR121 / H02` 按备注显示为“一号”，“申报/送资料”组合岗位同时进入申报和送资料两类。每类只列该航班对应常规岗位资质名单中状态正常的常规人员，并保留 0 次人员；申报和送资料表继续显示并标记督导资质人员，但最高、最低及差值优先按非督导资质人员计算，仅在没有非督导资质人员时才比较全部资质人员。统计合并本月已归档记录和当前尚未归档的最终排班，当前排班覆盖同日历史，人工换人后实时更新，同一人员同一日期在同一航班同一类别只计 1 次。病假、休假、行政支援、无对应资质、空缺、其他航班、普通岗位和实际结束时间未超过界线的记录不计；每类分别显示具体日期、最高/最低次数及差值。该按航班展示的统计只呈现事实，不改变自动排班使用的末班重点岗位公平轮换、规则优先级或排班结果。
- 将当天排班归档为历史负荷，支持按历史统计窗口继续排班。
- 历史页按工作日期分组，以只读的航班“岗位 / 人员”双列表格还原每天整体排班；最新工作日默认展开，其他日期可按需展开。
- 导出保障明细 Excel、可离线打开的 HTML 排班页和高清 PNG 排班图片。

## 核心用户路径

1. 在配置页维护人员、岗位规则和排班约束，或导入配置模板。
2. 在航班页确认当天航班、时段和岗位。
3. 选择排班日期并生成排班；每次只生成当前日期，完成当前班后再按顺序生成下个工作班。
4. 在排班页查看逐条排班反馈，复核待补位、人员疲劳、航班衔接和上一工作日晚班减负，必要时人工改派。
5. 在统计页核对月度轮值、轻松班次和按航班划分的末班重点岗位承担次数；需要时导入、调换或重新均衡月度轮值。
6. 导出 Excel、HTML 或 PNG；确认当天结果后可仅归档，或点击“归档并排后天”，把当天负荷写入历史，跨过一个休息日后立即生成下个工作日排班。

## 排班规则

### 合理排班宏观原则

- 合理排班不要求每个工作班所有人的疲劳点完全相同，而是在岗位完整和安全约束成立后，让相邻两个工作班形成动态负荷互补。重点岗位的长期轮换先于这种日常轻重调节：督导、一号、申报、控制和送资料都先保证低频合格人员完成当前轮次，再比较上一班负荷、恢复、当天工时和疲劳；但不得反向破坏值班、`KE166`、岗位完整性或严格衔接。
- 上一工作班负荷必须使用具名事实逐项比较，顺序固定为：累计疲劳点、最晚下班时间、总工时、重点岗位数量；全部相同时才进入后续规则，不得相加、乘权重或换算为综合分。
- 上一工作班末班重点岗位人员仍执行恢复保护：在不破坏重点岗位轮次公平时，先避开本班重点岗位和配置的截止时间后航班，必须增加航班时优先选择结束更早的航班，避免早班结束后长时间空档再返回晚班。该防割裂要求只适用于末班重点岗位恢复人员；与重点岗位公平冲突时让步，但硬约束、特殊锁定、岗位完整性和严格衔接不让步。
- 重点岗位使用同一“航班号 + 具体岗位”判断连续承担。上一工作班已经承担时，本班生成阶段必须先寻找未连续的安全人选；不得为了消除一人的连续承担而给交换或重排中的其他人员制造新的连续重点岗位。不同航班的同名岗位不合并为同一岗位，人员可以在不同重点岗位之间安全轮换。
- 高疲劳普通岗位同样使用同一“航班号 + 具体岗位”判断连续承担；疲劳点达到规则页现有高负荷阈值时，上一工作班已经承担的人员在本班生成阶段排在未连续人员之后。低于阈值的普通岗位允许连续两个工作班，引导岗位不参与该轮岗。
- 多个安全方案依次选择：先保证末班 `督导 → 一号 → 申报 → 送资料` 的分层公平，再保证一般重点岗位当前轮次，然后避免重点岗位连续承担，随后比较恢复、普通岗位衔接、当天覆盖、滚动负荷、连续高负荷、跨工作班负荷、当日工时与疲劳以及稳定顺序。没有安全替代时岗位完整性优先，但必须说明唯一资质、值班或 `KE166` 锁定、时间冲突、工时、严格衔接或会制造空缺等实际原因。

### 规则合同与冲突裁决

本节是当前排班优先级的唯一总合同，只汇总下方已经生效的详细规则，不新增或改变任何行为。后续条款负责解释具体条件，不得改变本节顺序；实现、规则页面、README 或测试与本节不一致时，必须先报告差异，不得自行选择一方覆盖另一方。

| 阶段           | 责任                                                                         | 当前规则                                                                                                                                                                                                                                                                         |
| -------------- | ---------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 硬约束         | 决定人员是否允许进入候选池，不可突破                                         | 人员状态、人员类型、岗位资质、夜班能力、时间冲突、每日工时上限                                                                                                                                                                                                                   |
| 特殊岗位锁定   | 在普通候选比较前保留必须落位的人员和岗位                                     | `KE166` 机动督导兼任预留、值班12点前上午航班、值班晚撤岗位                                                                                                                                                                                                                       |
| 岗位完整性     | 在硬约束允许范围内优先填满真实岗位                                           | 12点前稀缺岗位优先、跨航班抽调、同航班空缺下沉                                                                                                                                                                                                                                   |
| 人员保护与公平 | 岗位可填满时调整候选人员，不主动制造空缺；`TR121 / H02` 月上限是明确留空例外 | 严格岗位衔接、末班督导公平、末班一号公平、末班申报公平、末班送资料公平、一般重点岗位频率、重点岗位连续承担、跨工作日恢复、末班重点岗位次班截止保护、高疲劳普通岗位连续承担、当天早晚负荷分散、普通岗位衔接、在岗人员覆盖、滚动负荷、连续高负荷、跨工作班负荷互补、工时与疲劳均衡 |
| 最后比较       | 前述条件相同时最后比较历史疲劳，不固定同质量人员姓名                         | 历史疲劳；全部业务质量目标值相同的完整班表均为等价结果                                                                                                                                                                                                                           |
| 排班后轮岗复核 | 前述阶段完成后进行最后的连续性检查                                           | 分级连续轮岗复核；不得撤销已经成立的重点岗位频率结果                                                                                                                                                                                                                             |

真实运行按以下管线执行：先生成当天有效岗位任务并应用硬约束；再把除 `KE166` 顶部机动督导以外的全部自动岗位、全部真实合格候选和值班要求放入同一个 HiGHS 模型，按中央规则合同逐层锁定当天完整方案；随后执行同航班空缺下沉和分队长并行督导补缺，再依次复核末班重点岗位公平、一般重点岗位频率、跨工作日恢复、次班截止和分级连续轮岗，安排 `KE166` 顶部机动督导后重新复查公平与连续轮岗，最后计算系统说明、反馈和输出顺序。后序恢复与连续轮岗不得撤销已经成立的重点岗位频率；公平重排可以让步恢复、疲劳、普通衔接和当日零工时保护，但不得绕过硬约束、特殊锁定、严格衔接或岗位完整性。

整体模型为每个“岗位可由某人承担”的真实组合计算具名事实，并按以下顺序逐层求出最优值、锁定后再进入下一层：`KE166机动督导预留 → 值班岗位 → 岗位完整性与稀缺岗位 → 严格岗位衔接 → 末班督导公平 → 末班一号公平 → 末班申报公平 → 末班送资料公平 → 一般重点岗位频率 → 重点岗位连续承担 → 跨工作日恢复 → 末班重点岗位次班截止保护 → 高疲劳普通岗位连续承担 → 普通岗位衔接与当日在岗覆盖 → 滚动负荷 → 连续高负荷 → 跨工作班负荷互补 → 工时与疲劳均衡 → 历史疲劳`。每一步只有在前一步相同时才继续比较，不允许换算成综合权重；达到相同业务质量后不再增加人员编号目标。上午确实无人替代时，为岗位完整性突破严格衔接是既有的唯一时段例外；重点岗位公平重排本身和12点以后均不得突破严格衔接。

| 冲突双方                                                                   | 当前裁决                                                                                                     | 必须留下的证据                                   |
| -------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------ |
| 任意软规则与人员状态、资质、夜班、冲突或工时上限                           | 硬约束优先；无法满足时允许岗位留空                                                                           | 具体不合格原因和缺员数量                         |
| `KE166` 机动督导兼任预留与值班12点前上午航班                               | `KE166` 预留优先；值班人员改用其他开始时间严格早于12:00的航班                                                | `KE166` 锁定结果或值班上午航班未落实原因         |
| 值班身份与重点岗位连续承担                                                 | 保留值班身份；值班优先项内部先尝试其他未连续重点岗位，全部无解才回到连续岗位                                 | 改用的值班岗位，或连续轮岗未落实的具体原因       |
| 值班目标与岗位衔接、疲劳、跨工作日恢复或月度频率                           | 已选定的值班目标优先，但不得突破硬约束                                                                       | 命中的值班优先项、回退位置或保护突破原因         |
| 分队长并行督导与普通时间冲突                                               | 仅在常规岗位仍空缺且重叠不超过配置上限时允许两个督导并行；其余时间冲突仍禁止                                 | 两个督导航班、重叠分钟、释放人员和补齐岗位       |
| 12点前岗位完整性与严格衔接、疲劳、恢复或轮岗保护                           | 岗位完整性优先；无人替代时允许突破保护                                                                       | “已突破严格限制仍安排”或客观缺员原因             |
| 跨工作班负荷互补或恢复保护与重点岗位公平轮换                               | 重点岗位长期公平优先；有安全方案时五类重点岗位均可让步软保护，不得突破硬约束、特殊锁定、岗位完整性和严格衔接 | 对应岗位的本月次数、近期次数和阻塞原因           |
| 末班四类岗位之间                                                           | 固定按督导、一号、申报、送资料逐层锁定，后序不得扩大前序差值                                                 | 每类调整前后的最高、最低次数及差值               |
| 值班首选晚撤目标与一号公平                                                 | 安排后差值会超过 1 时改试下一晚撤目标，不强行占用首选                                                        | 跳过的目标、当前次数和实际采用目标               |
| `TR121 / H02` 自动月上限与岗位完整性                                       | 每人本月达到 2 次即退出自动候选；全部达到上限时岗位留空，人工改派不受此限                                    | 各合格人员本月次数及岗位留空原因                 |
| 跨工作班负荷互补与一般重点岗位月度频率                                     | 一般重点岗位频率优先，跨工作班负荷随后调节                                                                   | 上一班负荷及本月、最近6个归档工作日频率结果      |
| 重点岗位频率与当日工时疲劳、历史疲劳或最后比较                             | 重点岗位低频人员优先                                                                                         | 本月次数、最近6个归档工作日次数和安全重排结果    |
| 高疲劳普通岗位连续承担与跨工作班负荷互补、当日工时疲劳或低疲劳普通岗位轮换 | 高疲劳普通岗位先解除连续承担；但不得突破更早的恢复保护或特殊锁定                                             | 阈值、上一工作班同岗记录、交换路径或具体阻塞原因 |
| 普通岗位连续轮岗与已成立的重点岗位频率结果                                 | 重点岗位结果优先，普通轮岗不得撤销                                                                           | 交换成功路径或“连续轮岗未落实”原因               |
| 行政支援岗位与常规自动排班                                                 | 行政岗位不自动占用常规候选；仅在没有安全常规人员时人工补位                                                   | 常规候选不可用原因和人工调整结果                 |

代码中的 `SchedulingRuleId` 只作为机器执行标识，不直接展示给普通用户。规则页面必须从同一中央合同生成中文业务分组、规则名称、作用、状态和允许的操作；新增规则时必须同时登记执行标识、阶段、用户投影、反馈证据和回归测试，不得只更新其中一处。

### 硬约束

- 只有状态为“正常”的人员可以被分配。配置页修改人员状态后必须立即重新计算当前活动排班；加载本地数据时也必须清除旧排班快照中已经病假或休假的人员。历史归档只参与工时与疲劳计算，不得绕过当前状态把人员带入当天排班。
- 常规岗位只能分配岗位规则中列出的资质人员。
- 与配置夜班窗口重叠的航班只能分配可上夜班的人员。
- 同一人员的两个工作时段不得重叠；首尾相接不视为冲突。唯一例外是同航班顶部“督导”拖入一个空白岗位形成的督导兼任，该兼任属于同一段实际工作而非两段重叠任务。
- 同一人员当天累计工时不得超过配置上限，默认 12 小时。
- 常规的手动补位岗位可以保留为待补位，由排班员人工选择人员；但航班在12:00以前开始时，性质为“常规”的岗位即使标记手动补位也必须参与自动排班。
- 自动排班严格按岗位配置规则 ID 逐条生成常规、引导、机动督导和分流岗位，不生成航班清单或其他航班中的未配置岗位，也不自动补充通用临时岗位；启用旅客人数只控制是否自动安排，低于阈值的已配置岗位仍显示为空白可编辑格。
- 行政支援模式默认关闭；关闭时不生成行政支援岗位，也不显示行政支援人员池。开启时，同航班同名的行政支援规则替换常规规则并始终留空，不能同时生成两个同名岗位；未配置行政同名规则的常规岗位仍按原规则显示和自动排班。行政支援人员不参与自动排班；只有所有具备资质的常规人员都因病休、休假、时段冲突、夜班能力或工时上限等硬约束不可用时，才允许手动拖入具备该岗位资质的行政支援人员，且仍须通过状态、资质、夜班能力、时段冲突和每日工时上限校验。硬约束校验仍把行政补位视为实际占用，但行政支援岗位和行政支援人员的分配不计入人员负荷表、工时与疲劳均衡、滚动和连续高负荷保护、月度轻松班次、末班恢复保护或归档疲劳。
- 分流岗位只在原航班12:00以后开始、与下一航班的重叠不超过岗位配置的提前撤岗分钟时生效；生效后缩短原岗位结束时间并允许同一人员保障下一航班，早班不适用。
- 岗位规则的“启用旅客人数”大于 0 时，仅当当天航班预定人数达到阈值才自动安排该岗位；未达到阈值时仍生成空白岗位格，阈值为 0 表示始终自动安排。该人数门槛同样适用于12:00以前开始航班的常规岗位，不得因上午岗位完整性而绕过。
- “引导”分类岗位严格按照该航班当前岗位显示顺序，从下向上复用已经排到常规岗位的正常常规人员；分流、行政支援、引导及临时岗位人员均不能成为来源。多个引导岗位依次选择不同人员。排班员可在每个引导人员格中改选或输入该航班已经上岗的其他正常常规人员；未参加该航班、行政支援或不存在的人员必须拒绝。人工选择保留到来源人员不再满足条件或重新自动排班为止。引导复用不额外累计工时、疲劳或历史负荷。
- “机动督导”是固定在每个航班首行的独立岗位分类，并按自身资质自动安排；内置岗位和从旧状态一次性迁移的岗位全部使用“常规”，只有排班员之后明确选择时才成为机动督导，保存和重新加载不得按岗位名称擅自改变分类。自动生成 `KE166` 排班时，系统先完成全部常规柜台安排和重点岗位轮换，再从具备机动督导资质、满足状态、人员类型、夜班能力、时间冲突、每日工时上限和严格衔接硬条件的人员中选择顶部督导；上一工作班未担任同一督导者优先。通常只要存在不占用该时段柜台的合格人员，就让该人员独立担任顶部督导，督导工时和疲劳按自身配置计算。唯一的轮岗例外是：最佳独立督导上一工作班已经承担同一督导、本班另有实际岗位，且 `KE166` 当前柜台或重叠航班安全重排后存在未连续、具备完整机动督导和允许兼任柜台资质的人员；此时优先让该人员保留柜台并兼任顶部督导，解除连续督导，不要求原独立督导反向具备柜台资质。不存在该轮岗方案时，只有没有独立督导人选、否则会造成督导或柜台空缺，才允许普通缺员兼任或通过安全重排把合格督导放入兼任范围允许的柜台。兼任的两格保持同人、同一时段工时只计算一次，督导和柜台疲劳分别累计。兼任范围由规则页的允许/禁止规则控制，禁止优先，默认禁止岗位备注包含“一号”“申报”“排查”的岗位。开启行政支援模式后仍执行同一顺序，未被同名行政规则替换的机动督导不得留空。其他航班的人工督导兼任、同步清空、资质例外、兼任范围校验和重新自动排班后清除人工兼任继续按原规则执行。
- 人员信息中的“分队长”通常仅作为身份字段保留，不改变普通督导、`KE166` 机动督导或其他岗位的候选顺序、频率均衡、工时疲劳和连续轮岗。所有具备 `KE166` 机动督导资质的正常常规人员按同一规则参与独立督导选择和安全轮岗，不因分队长身份延后或提前。只有常规排班及岗位空缺下沉后仍存在常规岗位空缺时，才允许进入另一条“分队长并行督导补缺”例外；任意一端为 `KE166` 时禁止并行承担两个督导。CX航前、值班、备勤和人工拖拽仍只按各自已有条件执行。

### 排序与疲劳

- 排班结果仍按航班开始时间展示；督导优先显示，其他岗位按岗位配置顺序展示，可对单个航班一键将 G/H 柜台按编号从大到小排列。展示顺序不决定12点前岗位的计算顺序。
- 自动分配完成后，如果同一航班参与自动排班的常规岗位出现中间空缺、下方岗位已有人员，系统必须在不减少该航班已填常规岗位总数的前提下重新匹配合格人员，按岗位配置顺序优先填满上方岗位并把无法避免的空缺尽量沉到底部。已经由整体模型选定的重点岗位人员固定在原重点岗位，普通补缺不得把其挤走。重新匹配不得绕过人员状态、岗位资质、夜班能力、时间冲突、工时上限或新增严格岗位衔接违例；人工待补位、机动督导、引导和行政支援岗位不参与普通岗位匹配。`KE166` 机动督导兼任人员在常规岗位间移动时必须保留顶部督导关联和工时去重，所有人员均按移动后的岗位配置重新计算岗位备注与疲劳，引导人员随后按调整后的最下方已填常规岗位重新选取。
- “分队长并行督导补缺”位于岗位完整性阶段，在岗位空缺下沉之后、人员保护和轮岗复核之前执行。它是时间冲突硬约束的受控例外：仅当自动排班仍有常规岗位空缺时，可让一名状态正常的常规分队长同时承担两个均具备资质的督导；两个航班必须不同，且按航班原始保障开始和结束时间计算的重叠不得超过规则页配置上限，默认 30 分钟、可调范围 0–720 分钟，0 表示禁止并行。分流提前撤岗只影响普通个人工时，不得缩短该重叠判断。
- 并行督导不得扩展为普通柜台兼任或三个督导并行，不得移动值班锁定、已安排的 `KE166` 独立机动督导或缺员兼任组、人工岗位、引导复用或行政支援，也不得违反人员状态、两个督导资质、夜班能力、每日工时上限、严格衔接和跨工作日恢复保护。补缺最多安全重排 3 名常规人员；每一步最多把一个空位传递到另一个岗位，同一人员和同一岗位不得在一条搜索链中重复改派，超过边界时停止该方案，禁止继续枚举循环或全排列。重排必须减少常规岗位空缺且不得产生其他空缺；失败时保持原排班。双督导工时按两个完整督导时段的并集计算，两个岗位疲劳点分别累计。多个安全方案依次比较减少空缺数量、分队长原当日工时、原当日疲劳、人员配置顺序，不使用综合权重；成功时在决策记录和排班反馈中列出并行航班、重叠分钟和补位结果。
- 12点前常规岗位完整性是排班最高优先级：系统先提取所有开始时间早于12:00、已经达到“启用旅客人数”门槛且性质为“常规”的单个岗位，逐岗统计满足人员状态、岗位资质和夜班能力的可胜任人数，再把稀缺程度作为当天整体模型中岗位完整性阶段的固定顺序；同一航班内同样优先保证候选更少的岗位。该优先级不覆盖启用旅客人数门槛，但仍覆盖手动补位标记；12:00整开始的航班不在此范围。
- 12点前岗位先使用当前目标时段没有冲突且不超过工时上限的空闲常规人员。没有空闲候选人时，才从已经排入其他12点前航班且符合目标岗位资质、人员状态、夜班能力和工时上限的人员中抽调；抽调不复制重叠任务，原岗位改为空缺并在反馈标注“因抽调至XX航班/岗位而空缺”。系统不为此从12点以后航班抽调人员。
- 12点前的高负荷恢复、滚动负荷、重点岗位频率均衡、分级连续轮岗、跨工作日恢复保护和岗位衔接规则均不得主动造成常规岗位空缺。高负荷恢复、滚动负荷和跨工作日恢复只调整公开候选顺序，无安全替代时直接保证岗位完整；只有岗位衔接间隔配置为“严格限制”时，才在无人替代的12点前岗位上允许突破并精确反馈“已突破严格限制仍安排”。值班晚撤人员预留同样不得导致12点前常规岗位空缺。
- 状态非正常、岗位资质不符、夜班能力不符、时间冲突或超过每日工时上限仍是不可突破的硬约束。硬约束导致同时可用的合格人数少于岗位数时允许留空，反馈必须标注“因合格人数不足而无法填满”，列出缺少的岗位人数及状态、资质、夜班、冲突或工时原因；因跨航班抽调形成的源岗位空缺单独说明，不混同为疲劳保护空缺。
- 12点及以后航班按开始时间处理，并继续在岗位完整性前提下优先安排合格人员更少、限制更严格的岗位。航班宽松时优先覆盖尚未获得实际工时的正常常规人员；至少有 4 个航班且压力不为宽松时，候选比较同时计算投放后的当日工时差、历史窗口滚动工时差和当日疲劳差。压力的固定边界为：存在无合格人员任务、峰值并发岗位数与合格常规人员数之比达到 90%，或计划岗位工时与常规人员工时容量之比达到 75% 时为密集；达到 60% 或 45% 时为紧张。行政支援岗位和人员不进入上述分子、分母或个人负荷。这些均衡目标只能调整候选顺序，不能清空已经可以由常规人员承担的岗位。
- 当日值班人员满足人员状态、岗位资质、夜班能力、时间冲突和工时上限硬约束后，必须先按值班岗位有序优先项尝试落位；同一人员上一工作班已承担某个优先项对应的相同“航班号 + 岗位”时，先保持原配置顺序检查其他未连续优先项，只有其他目标全部不可执行时才允许回到该连续岗位。值班人员安排到某个一号目标后，如果同航班合格人员本月一号次数最高与最低会相差超过 1，必须跳过该目标并继续尝试下一项；`TR121 / H02` 达到个人自动月上限时同样跳过。全部配置项不可执行时，才使用最晚航班的一号、督导、申报或送资料岗位，最晚航班没有可执行目标时再使用倒数第二晚航班的同类岗位。任一时刻只启用一个晚撤候选，当前候选失败后才尝试下一项；未采用的后备项不得提前锁定值班人员。同一个上午岗位不能同时算作晚撤目标；值班人员还必须在最终排班中承担一班开始时间严格早于 `12:00` 的航班。上午具体岗位可以在安全轮岗中移动，只要最终仍保留至少一个上午航班。两项规则均在反馈中说明命中的配置顺序、轮岗后的具体岗位、回退落位或复核原因。值班身份与岗位完整性高于连续轮岗，已经选定且不可替换的值班晚撤目标仍优先于岗位衔接、高负荷恢复、滚动负荷、跨工作日恢复保护、一般重点岗位频率和分级连续轮岗；末班四类公平及 `TR121 / H02` 自动上限在选定晚撤目标前检查，不视为选定后的保护突破。其他保护被突破时必须留下结构化决策记录。
- 高负荷岗位默认定义为疲劳点不少于 4，或岗位配置含“一号、申报、控制”等非空备注；默认恢复时间为 360 分钟。保护开启时优先安排不在恢复期的合格人员；如果恢复期限制会导致岗位空缺，则允许安排仍在恢复期的常规人员，并在反馈中标注“已超保护仍安排”。低负荷岗位不受该规则影响。
- 岗位衔接间隔规则只在目标航班和目标岗位都匹配时生效；候选人此前承担指定前序航班的晚撤岗位，且从前序任务结束到目标任务开始的间隔小于规则值时触发。“优先避开”不删除候选，只在公开的候选顺序中把合规人员排在前面；“严格限制”在存在合规候选时排除违规人员，12点以后所有候选都违规时目标岗位留空并说明命中的规则，12点前常规岗位无人替代时可以突破并反馈留痕。值班目标和 `KE166` 特殊锁定允许高于该保护。默认规则将 `CX931` 的督导、G20、G19 到 `TR121 H02` 的最小间隔设为 180 分钟，排班员可编辑、停用、删除或继续新增同类规则。
- 滚动负荷上限默认统计目标岗位开始前 360 分钟内已经完成的岗位疲劳；仅当即将投放的岗位属于高负荷，且窗口内疲劳加新岗位疲劳超过 8 点时触发。系统优先选择未超限或超限更少的合格人员；如果所有候选人都会超限，则仍须选择其中负荷较低者填满岗位，不得主动留空。
- 末班重点岗位公平轮换在初始候选和完整排班后执行。实际结束时间严格晚于规则页“末班结束界线”的常规督导、一号、申报和送资料进入同航班独立账本，默认界线为 `23:00`，`23:00` 整不计入，`23:01`、`23:55` 和跨午夜 `00:10` 计入；普通岗位即使更晚结束也不计入。四类固定按督导、一号、申报、送资料逐层比较，每类先比较排班日期所在自然月次数，再比较最近 8 个已归档工作班次数；前一类结果已经改善后，后一类岗位、一般同岗频率和连续轮岗均不得扩大前一类差值。低频督导保留给督导；申报和送资料分别统计，不合并，具备同航班督导资质的人员比较时加 1，即实际少 1 次视为公平，不强迫追平。完整班表和 `KE166` 收尾后各复核一次：存在安全方案时优先改由低频合格人员，可在同航班和重叠航班内最多五人整体调整。四类公平都可以让步跨工作日恢复、次班截止、普通衔接、疲劳和当日零工时保护，但不得突破状态、类型、资质、夜班、时间、工时、值班、`KE166`、岗位完整性或严格衔接。`TR121 / H02` 自动安排还须满足个人本月少于 2 次，岗位下沉与后置重排执行同一检查；全部合格人员均达到 2 次时保留空缺，不使用无资质人员兜底，人工改派不受上限限制。任一当前适用的公平比较范围最高与最低相差至少 2 次且本班没有安排给低频人员时，班表格子才显示琥珀色警告；只差 0 或 1 次、或本班已经交给低频人员时不提示。无法安全调整时除 `TR121 / H02` 月上限外仍保留岗位完整性，并用不超过两句话说明实际阻塞原因。
- 一般重点岗位同岗频率均衡在初始候选排序和生成阶段安全重排中执行。统计键严格为同一“航班号 + 岗位”；先比较排班日期所在自然月累计次数，同数时再比较最近 6 个已归档工作日。只要当前人员次数高于其他合格人员，就必须尝试改由低频人员，不再等待累计达到 2 次；因此全体合格人员完成第 N 次前，不允许任何人开始第 N+1 次。重排把同航班和全部工作时段重叠航班纳入同一求解，最多五人参与，并优先使用改动岗位最少的完整方案。目标重点岗位频率必须降低，其他重点岗位不得恶化已经成立的末班分层公平；恢复、疲劳、普通衔接、上一班负荷和原人员当日零工时不能否决公平改善。无法调整时保留岗位完整性，并反馈无其他资质人员、特殊锁定、时间冲突、工时上限、严格衔接或会制造空缺等实际原因。琥珀色频率报警另行检查当前班加前 7 个已归档工作日及包含当前班的自然月；最高与最低相差至少 2 次且本班没有安排给最低频人员时才提示，差距正在改善时不提示。
- 跨工作班负荷互补使用最近一个已归档工作日作为基准，并按“累计疲劳点 → 最晚下班时间 → 总工时 → 重点岗位数量”逐项比较。较轻人员仍优先承担本班较重岗位，但该判断低于全部重点岗位的月度和近期轮次公平，高于普通岗位的当日工时疲劳、历史窗口疲劳和稳定顺序。被重点岗位公平突破时，“跨工作班动态平衡”反馈必须列出实际人员、岗位和优先规则。
- 分级连续保护同时在生成阶段和完整排班后执行。重点岗位先完成月度与近期轮次公平，再避免上一工作班相同“航班号 + 岗位”的连续承担；高疲劳普通岗位和低疲劳普通岗位随后处理，均不得撤销重点岗位频率结果。低于高负荷疲劳阈值的普通岗位允许连续两个工作班，只在本班将形成第三次连续承担时复核。全部业务质量相同时允许任一等价人员安排。
- 连续复核把同航班和目标时段重叠航班的可移动岗位、已排人员与空闲合格人员放入同一求解，搜索最多五人参与的闭环或开放式连续腾挪；只有整条方案通过后才一次性应用。重点岗位复核可以让步恢复、截止、一般疲劳、高负荷、滚动负荷、“优先避开”岗位衔接和原人员当日零工时，但不得突破岗位资质、人员状态、夜班能力、时间冲突、每日工时、值班/人工/机动督导锁定、严格岗位衔接、岗位完整性或已成立的更高优先级重点岗位频率。普通岗位复核不得移动重点岗位，也不得撤销前序结果。
- `KE166` 常规柜台先独立完成连续轮岗，顶部机动督导随后安排并单独检查督导轮岗。独立督导可以与同航班或目标时段重叠航班中的常规岗位组成最多五人参与的最短安全闭环或开放式连续腾挪，但交换后仍须保持督导与柜台关系合法。独立督导将形成连续、而未连续的合格督导已经位于允许兼任的 `KE166` 柜台，或可从重叠航班通过最多五人安全腾挪进入该柜台时，允许把独立督导转换为柜台兼任督导；搜索围绕柜台和重叠航班人员继续传递空位，不要求原独立督导反向具备柜台资质。原独立督导必须已有其他实际岗位；未连续督导可以保留原柜台并兼任，或在整条安全方案完成后进入允许兼任柜台。普通缺员兼任以及已经形成的兼任组仍把机动督导与兼任柜台作为一个不可拆分的逻辑岗位整组轮换，组内两格必须始终同人且工时不重复。所有具备目标岗位完整资质的正常常规人员按同一顺序参与，不设置姓名或分队长特例。没有安全替代时保持岗位完整，按人员、连续次数、实际原因、保留决定和当前连续次数说明结果；兼任状态同时保留两格并说明兼任兜底原因。值班晚撤锁定岗位、其他人工结果、行政支援、引导复用和人工临时岗位不参与自动交换；值班上午岗位可以移动，但最终必须保留至少一个12点前上午航班。重点岗位和高疲劳普通岗位第二次连续没有安全替代时保留岗位完整性，低疲劳普通岗位第三次连续没有安全替代时同样保留岗位完整性；每种反馈都必须列出具体业务原因。异常岗位使用琥珀色提示，红色只表示岗位空缺或硬错误；人工改派后只重新核对异常证据，不自动覆盖人工决定，已解决时立即取消琥珀色。未归档结果不进入跨日判断。
- 跨工作日恢复保护使用规则页可配置的“末班结束界线”，默认 `23:00`。最近一个已归档工作日中，实际结束时间严格晚于该界线且命中已启用末班重点岗位规则的承担人员进入保护名单；默认按岗位名称匹配“督导”，按岗位备注匹配“一号”“申报”“送资料”，“控制”可由排班员新增。恢复保护在重点岗位轮次公平已经确定后执行：不影响公平时先避开结构化早班目标、全部末班重点岗位和截止时间后的航班；与重点岗位公平冲突时让步。自动生成的值班恢复交换仍只在没有人工覆盖的日期之间进行。恢复调整不得突破硬约束、特殊锁定、岗位完整性或严格岗位衔接，也不得撤销已经成立的重点岗位频率结果；无法同时满足时保留更高优先级结果并反馈具体原因。
- 每条末班重点岗位规则可以单独设置“次班截止时间”，留空表示该条不限制。命中规则的人员在下个工作班中，航班开始时间达到或晚于截止时间时触发保护；同一人命中多条规则时使用最早截止时间。截止前的岗位优先供受保护人员完成必要工时，截止后的岗位优先选择未受保护人员；多名受保护人员竞争截止前岗位时，依次按截止时间更早、上一班实际结束更晚及既有公平顺序选择。初始排班仍使受保护人员在截止后上班时，系统由统一求解器检查同航班和时段重叠航班的完整安全重排，并优先选择能使该人员最终下班更早的方案。调整不得移动值班或 `KE166` 锁定、破坏重点岗位频率、扩大工时或疲劳差、减少已填岗位或违反硬约束和严格衔接；无安全方案时允许突破以保证岗位完整，并在“上一工作日晚班人员跟踪”中使用姓名、航班英文代码、岗位备注及具体原因反馈。人工把受保护人员安排到截止后岗位时必须二次确认。
- “归档并排后天”先以当前日期替换归档记录，再把日期推进两天并调用同一排班规则；后天航班有变化时可调整航班计划后重新排班。
- 历史疲劳只统计排班日期之前、配置窗口内的记录，默认 7 天。
- 连续工作天数产生额外疲劳惩罚，默认每个连续日 5 点。
- 岗位疲劳点是当前岗位规则的最终当日计分；夜班能力是硬约束，不重复放大已经包含夜间影响的岗位疲劳点。
- 排班结果展示岗位配置备注，但隐藏“一号”字样；单元格临时备注独立保存，不覆盖岗位配置备注。

## 数据模型与生命周期

- `Staff`：人员身份、常规/行政支援类型、分队长标记、CX航前资质、值班资质、备勤资质、夜班能力、状态和备注。
- `Flight`：当天航班、时段、岗位和备注。
- `FlightTemplate`：可被当天航班按航班号套用的时段、岗位和备注预设。
- `PositionRule`：航班与岗位的资质、疲劳点、常规/引导/机动督导/分流/行政支援分类、提前撤岗分钟和手动补位标记。
- `Assignment`：当天生成或人工调整后的岗位分配，分别保存岗位配置备注、当次手工备注、系统说明和结构化决策记录；决策记录保存最终岗位真正命中的规则、阶段、结果和说明，不占用排班单元格的姓名或备注区域。人工换人、交换、清空、重新填写或机动督导同步导致原自动选择失效时，必须同时清除相关系统说明和结构化自动决策记录，反馈只能根据最终排班事实及仍有效的记录生成。
- `HistoryRecord`：已确认并归档的历史工作与疲劳记录；归档必须保存岗位配置原始备注及人工备注，包括界面隐藏的“一号”，不得使用展示文本覆盖业务数据。
- `DutyRosterOverride`：按工作班日期保存人工调整后的CX航前、值班和次日两名备勤；未调整日期按当月工作日顺序实时计算，备勤的实际日期由工作班日期加一天得到。
- `DutyPositionPriority`：值班人员目标岗位的有序优先项，保存航班号、岗位或备注关键词及启用状态。
- `MobileSupervisorCoverageRule`：机动督导兼任岗位的结构化允许或禁止规则，保存适用航班、匹配位置、关键词和启用状态。
- 应用状态使用版本化 JSON 存储在浏览器 `localStorage`，当前版本为 3。版本 1 升级时一次性把当时已选“机动督导”的岗位改为“常规”；版本 1/2 升级到版本 3 时把 `TR121 / 一号` 值班优先项迁移为 `TR121 / H02`。旧本地数据或旧配置 Excel 缺少备勤资质字段时，常规人员按已开启处理，行政支援按关闭处理。连续轮岗不保存按人员定制的迁移或姓名特例，岗位资质始终以用户配置和导入数据为准；版本 3 以后用户手工调整的机动督导分类、值班优先项和岗位资质均按原值保存，不再重复覆盖；损坏或不兼容的数据会回退到内置初始配置。
- 应用没有后端、账号、遥测或远端数据写入。

## Excel 合同

### 导入

识别以下工作表名称或包含这些关键词的名称：

- `人员信息`：编号、姓名、是否可上夜班、状态、备注、人员类型（常规或行政支援）、CX航前资质、值班资质、备勤资质和是否为分队长。
- `航班计划`：当天航班号、开始时间、结束时间、预定人数（运力）、涉及岗位、备注。
- `航班配置` / `航班模板`：可复用航班号、开始时间、结束时间、涉及岗位、备注。
- `岗位配置`：航班号、分类、岗位名称、备注、可胜任人员、疲劳点、启用旅客人数和提前撤岗分钟。
- `各航班岗位疲劳度计算`：航班号、岗位名称、岗位疲劳点数。
- `历史排班` / `排班结果`：日期、航班、岗位、姓名、时段、工时、疲劳点和备注。
- `规则参数`：除行政支援开关外的可编辑标量规则参数，使用稳定参数代码识别。
- `岗位衔接规则`、`值班岗位优先`、`次班恢复目标`、`末班重点岗位`、`机动督导范围`：分别保存五类有序结构化规则，并保留规则 ID 和 Excel 行顺序。

导入配置类工作表会替换对应当前配置并清空尚未归档的排班；导入历史记录按日期、航班、岗位和姓名去重合并。五类规则工作表存在且全表合法时完整替换对应规则，只有表头没有数据行时表示清空该类规则；工作表不存在时保留当前规则。规则表任一非空行无效时整张表不覆盖，并提示工作表名称、Excel 行号和错误，其他合法工作表仍正常导入。`规则参数`按行局部导入，无效参数不覆盖当前值。行政支援启用状态和具体日期的CX航前、值班、备勤安排不属于配置 Excel；旧模板没有规则工作表时必须保持当前规则不变。

### 导出

- `保障明细`：按现行业务表习惯，以航班为岗位/人员双列并排展示；人员格包含可见岗位备注与当次手工备注。
- `排班结果`：机器可读的逐岗位长表，可再次导入。
- `排班结果`的备注列必须保留岗位配置原始备注及人工备注；“一号”只允许在保障明细等展示视图中隐藏，不能从机器可读数据中删除。
- `人员排班`：按人员展开的航班、岗位、时段和工时明细。
- 左侧“导入数据”用于混合导入配置或历史；配置页的“导入配置模板”只更新配置，历史页的“导入历史排班结果”只累计疲劳历史；左侧“导出配置”只导出配置和模板，排班页的“导出排班结果”导出当天结果。
- “导出配置”必须同时导出 `规则参数` 和五类结构化规则工作表，使再次导入时恢复全部可编辑规则；不导出行政支援启用状态，也不把按日期的CX航前、值班和备勤安排混入配置工作簿。
- HTML 文件自包含样式，可离线打开、打印和转发。
- PNG 使用与 HTML 相同的航班视角和人员视角生成高清长图。

## 技术架构

- TypeScript 5.9，严格类型检查。
- Vite 7 负责开发服务和静态生产构建。
- Bootstrap 5 与 Bootstrap Icons 提供组件、布局和图标基础。
- Lit 3 负责组件渲染与生命周期；Zustand vanilla + Immer 提供唯一应用状态 owner 和不可变命令提交。
- `@bubblyworld/highs-ts` 为普通重排提供 HiGHS WebAssembly 混合整数约束求解；项目内的 `@autoschedule/highs-ts` 基于同一成熟求解器，为当天整体模型补充原生多目标接口。人员与岗位选择使用 0/1 变量，工时、疲劳差和只负责表达组合关系的辅助量使用有实际上下限的连续变量。当天模型使用具名约束和原生分层字典序目标，不生成 LP/MPS 字符串，也不使用跨规则综合权重；对当前班表完全不适用或所有系数均为 0 的空目标在求解前移除，非空目标的顺序和最优值不变。整体 deadline 为 30 秒，失败不返回部分方案。
- `xlsx-js-style` 处理 Excel 导入、样式和导出。
- `html2canvas` 生成排班 PNG。
- Vitest 4 保护时间、疲劳、约束、排班、Excel 映射和状态恢复等核心逻辑。
- `src/domain/` 按 assignments、candidates、coverage、duty-roster、feedback、flights、kernel、reviews、rules、shared、solver 和 statistics 能力边界组织，只处理业务事实；`src/infrastructure/` 处理存储、Excel、分享、浏览器 Worker 和求解器适配；`src/ui/` 只渲染只读投影并发出类型化命令；`src/app/` 通过唯一 Store、应用动作和专用控制器提交状态变化。`src/app.ts` 只挂载根组件。
- `kernel/scheduling-kernel.ts` 是唯一排班主干，使用事务账本提交内部类型化规则提案并生成最终快照；排班后复核由 `kernel/schedule-pipeline.ts` 按固定注册计划串联，阶段顺序、进度百分比和可见提示统一来自 `kernel/schedule-progress.ts`，可见任务同时根据当前航班和具名业务设置生成，不显示未执行的 KE166 复核。
- `kernel/daily-schedule-optimizer.ts` 把当前日期全部自动普通岗位与真实合格人员收进一个深模块，通过单一 `optimizeDailySchedule` 接口返回完整方案、值班岗位锁定和缺员说明；`assignments/ke166-supervisor-finalizer.ts` 在普通柜台方案确定后完成顶部机动督导收尾。调用方不接触模型变量、约束或 HiGHS 求解细节。
- `rules/schedule-rule-contract.ts` 单一维护规则 ID、阶段、中文名称和反馈模式；`rules/rule-registry.ts` 校验依赖并稳定排序，`rules/built-in-rule-registry.ts` 把每个注册项直接连接到真实类型化执行器。候选事实与固定逐项顺序由 `candidates/candidate-priority.ts` 负责；当天任务落位、空缺原因和自动决策证据由 `kernel/daily-schedule-optimizer.ts` 根据整体最优结果统一生成，不保留旧逐岗执行入口。
- 轮岗、恢复重排及分队长并行督导补缺共用 `solver/reassignment-optimizer.ts`、中性 `SolverPort` 与 `reviews/rotation-review-safety.ts`；所有可精确表达的资格、冲突、工时、保岗和值班上午条件进入同一 0/1 模型，求解后仍执行全量安全复核。不同用途只声明明确策略差异，不复制安全算法，并遵守各自既有参与人数边界。
- 排班反馈按只读事实、运营反馈、规则反馈和组合入口分层；反馈只能说明最终结果与结构化决策，不得在生成反馈时重新换人或执行另一套候选排序。
- 标量规则默认值和清洗由 `schedule-settings.ts` 统一，五类结构化规则的数据合同、默认值和清洗由 `structured-policy-contract.ts` 与 `structured-policy-settings.ts` 统一；规则 Excel 表由独立适配器负责；持久化恢复和班表生命周期分别通过单一领域入口处理，局部数据损坏不得使无关配置整体回退。
- 结构化规则集合的字段更新统一通过 `policy-actions.ts`，后台排班生命周期和工作簿导入分别通过专用控制器；生产模块依赖不得形成循环。

## 运行、验证与分发

- 开发：`npm.cmd run dev`
- 类型检查：`npm.cmd run typecheck`
- 核心测试：`npm.cmd test`
- 生产构建：`npm.cmd run build`
- 完整验证：`npm.cmd run verify`
- 构建产物：`dist/`，可部署到任意静态站点或直接作为静态包分发。
- GitHub Pages 由 `.github/workflows/deploy-pages.yml` 在 `main` 分支推送后自动构建和部署。

## 验收标准

- 完整验证命令退出码为 0。
- 默认配置可以生成排班，所有自动分配均满足硬约束。
- 人工改派无法绕过资质、状态、夜班、冲突和工时规则。
- 配置与历史 Excel 可以解析，三类分享文件可以在浏览器端生成。
- 桌面和移动端均能完成核心路径，页面无不可访问的资源或阻塞性控制台错误。

## 授权边界

项目按根目录 `LICENSE` 中的 MIT License 分发，版权人为二次元虎哥。
