# 自动排班产品规格

## 产品定位

自动排班是面向机场地勤保障团队的静态、本地优先排班工具，用于把航班、岗位资质、人员状态和历史疲劳合并为当天可执行的保障排班，并输出适合留档和转发的结果。

## 当前能力

- 管理人员编号、姓名、常规/行政支援类型、是否为分队长、CX航前资质、值班资质、夜班能力、在岗状态和备注；配置页人员信息默认收起，可独立展开编辑。分队长字段仅记录人员身份，不参与自动排班候选、排序、频率、疲劳或轮岗判断。人员状态变为病假或休假时，当前活动排班立即按最新人员状态重新计算；恢复正常后重新进入合格候选池。
- 在配置页维护可复用的航班计划模板（航班号、时段、岗位和备注）；在航班页输入航班号即可自动带出模板。
- 航班页可调用 `flight-query-tool.vercel.app` 查询武汉机场指定工作日的国际及地区出港计划；查询日期默认使用当前排班日期并允许临时修改，固定排除所选日期 `00:00-05:59` 起飞的航班、纳入次日 `00:00-05:59` 起飞的航班。查询后按“继续保留、建议新增、待确认删减”与当前航班计划对账：只有航班号与现有模板完全一致且当前计划尚未包含的航班可以勾选新增，新增使用模板中的保障开始时间、结束时间、岗位和备注，预定人数初始化为 0；线上未查到的当前航班只列为待删候选，默认不勾选，必须由排班员选择并二次确认后才删除。系统不得自动转换不同名称、覆盖现有航班数据或自动删除航班；查询失败、查询结果为空、查询日期与当前排班日期不一致时禁止批量删减并保留当前航班计划。成功增删后已有排班结果失效，必须重新排班；航班模板、岗位规则和历史记录不受影响。
- 管理航班时段、岗位清单、岗位资质、岗位疲劳点、常规/引导/机动督导/分流/行政支援分类和岗位启用旅客人数阈值；分流岗位可配置提前撤岗分钟，岗位规则按航班折叠，支持批量新增、上下调整和柜台编号一键降序。
- 导入人员、航班计划、航班配置模板、岗位配置、历史排班及月度值班备勤 Excel。
- 12:00 以前开始的航班先汇总全部常规岗位，按每个岗位的可胜任人数从少到多全局排班；12:00 及以后开始的航班仍按开始时间处理。
- 自动阻止无资质、病休、夜班能力不足、时间冲突和超过每日工时上限的分配；同航班顶部“督导”拖入兼任范围允许的空白岗位时，是目标岗位资质、同航班时间冲突与重复工时规则的明确例外，但不得绕过机动督导兼任范围。
- 排班页按航班生成 Excel 式双列：每个航班表头横跨“岗位”和“人员”两列，岗位名称及岗位配置备注位于左格，人员姓名及当次临时备注位于右格；不同航班的岗位按各自配置从上到下独立排列，不按柜台字母或数字跨航班匹配。默认岗位列 64px、人员列 56px，航班多时横向滚动；支持在 70% 至 160% 之间缩放列宽和字号并在本机记住比例。航班标题显示到岗备注；性质为“机动督导”的岗位固定置于首行，其余岗位严格按岗位配置逐条生成和排序，只有引导岗位位于底部分隔线下方；每区保留可直接填写岗位和任意姓名的空白新增格，临时项可修改或删除；普通岗位按原约束拖拽，顶部督导只能拖入同航班兼任范围允许的空白岗位形成同步的督导兼任。
- 排班页可切换行政支援模式；关闭时只显示常规体系岗位，开启后同航班同名的行政支援岗位替换常规岗位并保持为空，没有行政同名项的常规岗位继续保留。行政支援人员在独立着色名单中维护；仅当没有满足硬约束的常规人员可用时，才允许把具备该岗位资质的行政支援人员拖入补位。行政支援岗位和行政支援人员均不计入工时、疲劳、负荷均衡、末班高负荷保护及后续疲劳归档。
- 人员负荷与疲劳表只统计常规人员；在岗位数量、岗位资质和硬约束允许时，自动排班优先覆盖当天尚未获得实际工时的正常常规人员，病假和休假人员不要求产生工时。
- 月度轮值必须按“值班 → CX航前 → 备勤”的顺序生成。月表仍只列与当前排班日期属于同一“上一休一”班组的本月工作日；每一行的值班和 CX 航前发生在该工作日，两名备勤发生在其后一个自然日的休息日，界面和统计必须显示实际次日备勤日期。值班拥有绝对优先级并最先安排：每名有值班资质的正常常规人员在当月工作日席位充足时至少值班 1 次，在此基础上继续均衡，使值班次数最多与最少的差值不超过 1；当月值班席位少于资质人数时，无法满足的缺额跨月轮换。
- 同一个工作班轮值块中的值班与 CX航前、两名次日备勤完全互斥：值班人员不得同时承担该工作班的 CX 航前或其后的次日备勤。CX航前与次日备勤可以由同一人承担；两名次日备勤人员之间仍不得重复。
- 值班完成后安排 CX航前，只使用具备 CX航前资质的正常常规人员，并使所有 CX航前资质人员的当月次数最多与最少相差不超过 1。唯一 CX 资质人员值班当天没有其他人选时，值班保持不变，CX航前留空。
- 值班完成后安排备勤。每名正常常规人员在当月备勤席位充足时至少备勤 2 次，在满足保底后继续均衡，使备勤次数最多与最少的差值不超过 1；病休、休假和行政支援不参与。若当月工作日数量不足，优先满足值班刚性要求，备勤不足 2 次的人员和实际次数在月表及反馈中说明，该缺额不计入违约。
- 排班主区在人员名单和班表右侧固定显示当日CX航前、值班和次日两名备勤，使默认比例下可同时核对班表与轮值人选；窄屏时摘要自动移到班表上方。完整的月度轮值明细位于独立“统计”页，CX航前与值班/备勤分别展开，按人员显示本月次数和实际日期；月表直接显示值班、航前和备勤差值、值班首轮覆盖、备勤 2 次保底及计划疲劳。月表可直接调整并实时更新统计，值班与备勤选择对方时在资质允许时自动交换，CX航前可与任一次日备勤选择同一人，也可按日恢复默认轮值。月表提供与现行横向表相近的 Excel 下载：顶部合并标题，星期和 `MMDD` 双层日期表头，人员逐行，当前轮值预填红色 `24`、浅橙色“备勤”和浅色“休”；修改后可从专用入口重新导入。
- 值班备勤表导入以当前排班日期确定目标月份及本班工作日奇偶，识别工作表名称中的“值班/备勤”、横向日期表头、人员姓名以及单元格 `24/值班/备勤/休`。`24`或“值班”记为表头当天值班，“备勤”记入前一个工作班的次日备勤；“休”和空白不生成安排。导入前必须预览工作班日期、值班人员、实际次日备勤日期和两名备勤，确认后按表头实际覆盖的日历日期替换值班与备勤人工覆盖，但保留 CX 航前及未被表头覆盖的跨月席位；例如 7 月表没有 8 月 1 日列时，不得清空 7 月 31 日对应的次日备勤。未匹配或重名人员、非正常常规人员、无值班资质、同日多名值班、超过两名备勤、轮值人员重合或与保留的 CX 航前冲突时阻止导入；缺少值班或不足两名备勤只警告并允许确认。导入结果视为人工调整，自动均衡不得覆盖；“重新均衡本月”经确认后可以清除这些人工安排。
- 值班任务默认计 12 点疲劳，当日值班卡片、月度计划疲劳和排班反馈均直接显示该分值；分值同时参与航班岗位候选排序、人员负荷和历史归档，CX航前与备勤不增加疲劳。排班员可维护有序的值班岗位优先项，每项包含启用状态、航班号和岗位或备注关键词；系统按顺序锁定第一个满足人员状态、岗位资质、夜班能力、时段冲突和工时上限硬约束的目标。默认顺序为 `TR121 / H02`、`TW616 / 一号`。全部配置项不可执行时，回退为依次检查最晚、倒数第二晚航班，并在每档中按一号、督导、申报、送资料排序；目标落位前不把值班人员分给其他岗位，目标本身优先于同岗轮换、疲劳和岗位衔接保护，突破保护时写入决策记录与反馈。
- 排班结果下方提供两部分反馈，随自动排班和人工调整实时重算：第一部分“航班与人员安排”报告航班数量与密集程度、人员覆盖、当日工时/疲劳差和航班衔接；第二部分“规则执行情况”逐条报告12点前岗位完整性、高负荷保护、“上一工作日晚班人员跟踪”“本班末班人员预告”、08:30 前早班值班和值班晚撤等规则，明确显示“已执行”“需复核”或“无基准”及证据。上一班跟踪必须列出被保护人员上一班的航班、岗位和疲劳，以及本班最早/最晚岗位、当日岗位疲劳、是否避开已配置早班目标和无法避开的具体原因；本班预告必须列出最后一批中疲劳点最高岗位的人员，最高点并列时全部列出，供下个工作日恢复保护。12点前反馈必须列出“已突破严格限制仍安排”、因跨航班抽调产生的源岗位空缺，或“因合格人数不足而无法填满”的具体人数和原因；值班反馈逐人说明 CX 航前、值班和次日两名备勤的实际安排，并提示月度轮值差异。
- 左侧工作模块固定为 `总览 → 配置 → 航班 → 排班 → 规则 → 统计 → 历史`。第 5 个工作模块为独立的“规则”页，公开“当前排班规则清单”；每类规则和每条岗位衔接规则均默认收起，可独立展开编辑。页首可搜索规则编号、名称、分类、航班号、岗位和说明，匹配规则自动展开，清空搜索后恢复搜索前的折叠状态。机器执行规则必须直接从中央 `SCHEDULING_RULES` 合同生成清单，不得另写一套容易失真的手工编号；清单明确显示执行阶段 `硬约束 → 特殊岗位锁定 → 岗位完整性 → 人员保护与公平 → 稳定排序 → 排班后轮岗复核`。候选判断使用具名字段逐项比较，不把严格衔接、日期或工时疲劳合成为不可解释的权重分。排班后分级轮岗读取最近两个已归档工作日：重点岗位从上一班重复时开始复核，普通岗位只在即将形成第三次连续承担时复核，并按同航班两人交换、重叠航班两人交换、最多三人闭环交换依次尝试。清单同时展示系统实际执行的岗位生成、岗位复用、岗位衔接、当前参数和调整入口。可配置值班疲劳点数、值班岗位有序优先项、机动督导兼任范围、月度轻松班次统计时段、当日/滚动工时与疲劳均衡、高负荷岗位衔接保护、滚动负荷上限、分级连续轮岗和跨工作日恢复保护；跨工作日恢复保护可新增、删除、启停和编辑多条“目标航班号 + 岗位或备注关键词”早班避让目标，另可新增多条结构化“岗位衔接间隔”规则。高负荷、滚动负荷和跨工作日恢复均只保留启用开关：启用后统一优先避开受保护人员，无安全替代时仍保证岗位完整；这三类规则不提供没有执行差异的“强保护”选项。只有结构化岗位衔接间隔规则保留“优先避开/严格限制”强度；严格限制在12点以后阻止违规落位，12点前常规岗位没有未受限替代人员时允许突破并在反馈留痕。值班和 `KE166` 特殊锁定仍高于衔接保护。保存后立即应用于已有排班或下一次排班。
- 重点岗位频率均衡、安全重排与分级连续轮岗共用同岗轮换开关。重点岗位仅指性质为“常规”且岗位名称或备注包含“一号”“申报”“督导”“控制”“送资料”的岗位；统计严格使用同一“航班号 + 岗位”作为键，不把不同航班的同名岗位合并。重点岗位初始候选先确认使用该人员不会使工作时段重叠的后续岗位失去全部可用替代人员，再比较排班日期所在自然月累计次数，同数时比较最近 6 个已归档工作日，较少者优先；不能仅因某人还能承担后续普通岗位就压过重点岗位频率。该比较位于连续高负荷保护之后、工时与疲劳均衡之前，且不得突破前序硬约束、特殊岗位锁定、岗位完整性、岗位衔接或疲劳保护。普通岗位不参与自然月和最近 6 个工作日的频率均衡，只在即将形成第三次连续承担时进行低优先级复核。排班后先复核重点岗位，再复核普通岗位；后续普通岗位轮换不得撤销已经成立的重点岗位频率均衡。
- 统计页按自然月展示常规人员的“提前下班”和“下午无航班”。提前下班只看每人当天最后一个实际航班的截载时间，必须严格早于可配置节点才计 1 次；同航班的机动督导、引导等复用记录不得重复计数，当日值班人员排除，备勤人员照常计入。下午无航班按可配置时段判断，当天参加过实际排班且没有任何航班与该时段重叠时计 1 次，早晚有航班但下午为空仍计入；值班和备勤人员均照常计入。病假、休假、行政支援和全天无排班人员不计；当前未归档排班覆盖同日历史，页面同时显示当天明细与每人本月累计。
- 统计页按自然月展示 `TR121 / H02` 承担次数，只列当前 `TR121 / H02` 常规岗位资质名单中状态正常的常规人员，并保留 0 次人员。统计合并本月已归档记录和当前尚未归档的最终排班；当前排班覆盖同日历史，人工换人后实时更新，同一人员同一日期只计 1 次。病假、休假、行政支援、空缺和其他航班或岗位不计；表格按次数从少到多、同数按人员配置顺序排列，并显示具体日期、最高/最低次数及差值。该统计只展示事实，不参与自动排班规则或优先级。
- 将当天排班归档为历史负荷，支持按历史统计窗口继续排班。
- 历史页按工作日期分组，以只读的航班“岗位 / 人员”双列表格还原每天整体排班；最新工作日默认展开，其他日期可按需展开。
- 导出保障明细 Excel、可离线打开的 HTML 排班页和高清 PNG 排班图片。

## 核心用户路径

1. 在配置页维护人员、岗位规则和排班约束，或导入配置模板。
2. 在航班页确认当天航班、时段和岗位。
3. 选择排班日期并生成排班。
4. 在排班页查看逐条排班反馈，复核待补位、人员疲劳、航班衔接和上一工作日晚班减负，必要时人工改派。
5. 在统计页核对月度轮值、轻松班次和 `TR121 / H02` 承担次数；需要时导入、调换或重新均衡月度轮值。
6. 导出 Excel、HTML 或 PNG；确认当天结果后可仅归档，或点击“归档并排后天”，把当天负荷写入历史，跨过一个休息日后立即生成下个工作日排班。

## 排班规则

### 规则合同与冲突裁决

本节是当前排班优先级的唯一总合同，只汇总下方已经生效的详细规则，不新增或改变任何行为。后续条款负责解释具体条件，不得改变本节顺序；实现、规则页面、README 或测试与本节不一致时，必须先报告差异，不得自行选择一方覆盖另一方。

| 阶段 | 责任 | 当前规则 |
| --- | --- | --- |
| 硬约束 | 决定人员是否允许进入候选池，不可突破 | 人员状态、人员类型、岗位资质、夜班能力、时间冲突、每日工时上限 |
| 特殊岗位锁定 | 在普通候选比较前保留必须落位的人员和岗位 | `KE166` 机动督导兼任预留、值班08:30前早班、值班晚撤岗位 |
| 岗位完整性 | 在硬约束允许范围内优先填满真实岗位 | 12点前稀缺岗位优先、在岗人员覆盖、跨航班抽调、同航班空缺下沉 |
| 人员保护与公平 | 岗位可填满时调整候选人员，不主动制造空缺 | 岗位衔接、跨工作日恢复、滚动负荷、连续高负荷、重点岗位频率、工时与疲劳均衡 |
| 稳定排序 | 前述条件完全相同时保证结果可重复 | 历史疲劳、人员编号 |
| 排班后轮岗复核 | 前述阶段完成后进行最后的连续性检查 | 分级连续轮岗复核；不得撤销已经成立的重点岗位频率结果 |

真实运行按以下管线执行：先生成当天有效岗位任务并应用硬约束；再完成 `KE166`、值班等特殊预留；随后逐岗位完成候选比较和岗位填充；完整分配后依次执行同航班空缺下沉、重点岗位频率安全重排、分级连续轮岗复核，最后重新计算系统说明、反馈和输出顺序。后置阶段不得绕过硬约束、移动已锁定岗位、减少已填岗位数量或撤销已经成立的更高优先级结果。

普通候选在没有特殊预留或阶段回退时，使用具名字段按顺序逐项判断：`值班岗位 → KE166机动督导 → 稀缺资质预留 → 当日在岗覆盖 → 岗位衔接 → 跨工作日恢复 → 滚动负荷 → 连续高负荷 → 重点岗位频率 → 工时与疲劳均衡 → 历史疲劳 → 人员编号`。每一步只有在前一步相同时才继续比较，不允许换算成综合权重。`KE166` 兼任人员的提前预留是该候选顺序之前的特殊锁定，因此与值班早班发生冲突时仍按下表裁决。

| 冲突双方 | 当前裁决 | 必须留下的证据 |
| --- | --- | --- |
| 任意软规则与人员状态、资质、夜班、冲突或工时上限 | 硬约束优先；无法满足时允许岗位留空 | 具体不合格原因和缺员数量 |
| `KE166` 机动督导兼任预留与值班08:30前早班 | `KE166` 预留优先；值班人员改用其他可执行早班 | `KE166` 锁定结果或值班早班未落实原因 |
| 值班目标与岗位衔接、疲劳、跨工作日恢复、频率或轮岗 | 值班目标优先，但不得突破硬约束 | 命中的值班优先项、回退位置或保护突破原因 |
| 12点前岗位完整性与严格衔接、疲劳、恢复或轮岗保护 | 岗位完整性优先；无人替代时允许突破保护 | “已突破严格限制仍安排”或客观缺员原因 |
| 重点岗位频率与工时疲劳、历史疲劳或稳定排序 | 重点岗位低频人员优先 | 本月次数、最近6个归档工作日次数和安全重排结果 |
| 普通岗位连续轮岗与已成立的重点岗位频率结果 | 重点岗位结果优先，普通轮岗不得撤销 | 交换成功路径或“连续轮岗未落实”原因 |
| 行政支援岗位与常规自动排班 | 行政岗位不自动占用常规候选；仅在没有安全常规人员时人工补位 | 常规候选不可用原因和人工调整结果 |

代码中的 `SchedulingRuleId` 是机器执行标识，规则页面的 `R00` 等编号只是展示标识。两者必须维护明确对应关系；新增规则时必须同时登记执行标识、阶段、展示说明、反馈证据和回归测试，不得只更新其中一处。

### 硬约束

- 只有状态为“正常”的人员可以被分配。配置页修改人员状态后必须立即重新计算当前活动排班；加载本地数据时也必须清除旧排班快照中已经病假或休假的人员。历史归档只参与工时与疲劳计算，不得绕过当前状态把人员带入当天排班。
- 常规岗位只能分配岗位规则中列出的资质人员。
- 与配置夜班窗口重叠的航班只能分配可上夜班的人员。
- 同一人员的两个工作时段不得重叠；首尾相接不视为冲突。唯一例外是同航班顶部“督导”拖入一个空白岗位形成的督导兼任，该兼任属于同一段实际工作而非两段重叠任务。
- 同一人员当天累计工时不得超过配置上限，默认 12 小时。
- 常规的手动补位岗位可以保留为待补位，由排班员人工选择人员；但航班在12:00以前开始时，性质为“常规”的岗位即使标记手动补位也必须参与自动排班。
- 自动排班严格按岗位配置规则 ID 逐条生成常规、引导、机动督导和分流岗位，不生成航班清单或其他航班中的未配置岗位，也不自动补充通用临时岗位；启用旅客人数只控制是否自动安排，低于阈值的已配置岗位仍显示为空白可编辑格。
- 行政支援模式默认关闭；关闭时不生成行政支援岗位，也不显示行政支援人员池。开启时，同航班同名的行政支援规则替换常规规则并始终留空，不能同时生成两个同名岗位；未配置行政同名规则的常规岗位仍按原规则显示和自动排班。行政支援人员不参与自动排班；只有所有具备资质的常规人员都因病休、休假、时段冲突、夜班能力或工时上限等硬约束不可用时，才允许手动拖入具备该岗位资质的行政支援人员，且仍须通过状态、资质、夜班能力、时段冲突和每日工时上限校验。硬约束校验仍把行政补位视为实际占用，但行政支援岗位和行政支援人员的分配不计入人员负荷表、工时与疲劳均衡、滚动和连续高负荷保护、月度轻松班次、末班恢复保护或归档疲劳。
- 分流岗位只在原航班12:00以后开始、与下一航班的重叠不超过岗位配置的提前撤岗分钟时生效；生效后缩短原岗位结束时间并允许同一人员保障下一航班，早班不适用。
- 岗位规则的“启用旅客人数”大于 0 时，仅当当天航班预定人数达到阈值才自动安排该岗位；未达到阈值时仍生成空白岗位格，阈值为 0 表示始终自动安排。12:00以前开始航班的常规岗位不受该阈值影响，始终必须尝试自动填满。
- “引导”分类岗位严格按照该航班当前岗位显示顺序，从下向上复用已经排到常规岗位的正常常规人员；分流、行政支援、引导及临时岗位人员均不能成为来源。多个引导岗位依次选择不同人员。排班员可在每个引导人员格中改选或输入该航班已经上岗的其他正常常规人员；未参加该航班、行政支援或不存在的人员必须拒绝。人工选择保留到来源人员不再满足条件或重新自动排班为止。引导复用不额外累计工时、疲劳或历史负荷。
- “机动督导”是固定在每个航班首行的独立岗位分类，并按自身资质自动安排；内置岗位和从旧状态一次性迁移的岗位全部使用“常规”，只有排班员之后明确选择时才成为机动督导，保存和重新加载不得按岗位名称擅自改变分类。规则页维护结构化兼任范围，每条规则包含启用状态、适用航班（留空表示全部航班）、匹配位置（岗位名称或岗位备注）、关键词和允许/禁止方式；禁止规则优先。某航班存在生效的允许规则时，机动督导只能兼任至少命中一条允许规则且未命中任何禁止规则的岗位；没有允许规则时，默认允许未被禁止的岗位。默认在全部航班禁止岗位备注包含“一号”“申报”“排查”的岗位。自动生成 `KE166` 排班时，如果该航班的督导被明确配置为机动督导，系统必须先在该航班仍生效且兼任范围允许的数字常规岗位中安排一名同时具备机动督导资质的正常常规人员，再把同一人同步到顶部督导；如果没有允许兼任的常规岗位，则保留该人员只担任顶部督导，并在有其他合格人员时由其他人员填充常规岗位。该预留高于值班早班、负荷与疲劳排序，但存在其他合格人员时不得主动造成其他岗位空缺。开启行政支援模式后仍执行该规则，只有实际配置了同名行政规则的岗位参与替换，常规体系的机动督导不得因其他柜台的行政替换而留空。兼任人员的数字岗位姓名保持不变，同一时段工时只计算一次，机动督导和数字岗位分别累计各自配置的疲劳点。其他航班已安排的顶部督导只能拖入同一航班兼任范围允许的空白岗位，顶部姓名保持不变，目标岗位显示与顶部同步的同一人；顶部督导改人或清空时，所有关联目标同步更新或清空。该操作不要求督导具备目标岗位资质，也不重复计算同航班工时或触发同航班时间冲突，但目标岗位仍累计自身疲劳点并在反馈列出兼任。目标已有人员、目标为另一顶部督导、跨航班或兼任范围禁止时拒绝；拖动已关联的目标岗位时同样必须通过兼任范围。自动排班后的常规岗位空缺重排不得把已关联机动督导移动到禁止岗位；加载旧排班时清除已经违反当前兼任范围的关联。重新自动排班会清除人工督导兼任。普通岗位的其他拖拽继续执行原有资质、占用与冲突规则。
- 人员信息中的“分队长”仅作为身份字段保留，不得改变普通督导、`KE166` 机动督导或其他岗位的自动候选、任务顺序、频率均衡、工时疲劳、连续轮岗和稳定排序。分队长与其他具备相同岗位资质的正常常规人员执行完全相同的排班规则；CX航前、值班、备勤和人工拖拽仍只按各自已有条件执行。

### 排序与疲劳

- 排班结果仍按航班开始时间展示；督导优先显示，其他岗位按岗位配置顺序展示，可对单个航班一键将 G/H 柜台按编号从大到小排列。展示顺序不决定12点前岗位的计算顺序。
- 自动分配完成后，如果同一航班参与自动排班的常规岗位出现中间空缺、下方岗位已有人员，系统必须在不减少该航班已填常规岗位总数的前提下重新匹配合格人员，按岗位配置顺序优先填满上方岗位并把无法避免的空缺尽量沉到底部。重新匹配不得绕过人员状态、岗位资质、夜班能力、时间冲突、工时上限或新增严格岗位衔接违例；人工待补位、机动督导、引导和行政支援岗位不参与普通岗位匹配。`KE166` 机动督导兼任人员在常规岗位间移动时必须保留顶部督导关联和工时去重，所有人员均按移动后的岗位配置重新计算岗位备注与疲劳，引导人员随后按调整后的最下方已填常规岗位重新选取。
- 12点前常规岗位完整性是排班最高优先级：系统先提取所有开始时间早于12:00的航班中性质为“常规”的单个岗位，逐岗统计满足人员状态、岗位资质和夜班能力的可胜任人数，再按人数从少到多全局安排；同一航班内同样先排候选更少的岗位。该优先级覆盖启用旅客人数阈值和手动补位标记，12:00整开始的航班不在此范围。
- 12点前岗位先使用当前目标时段没有冲突且不超过工时上限的空闲常规人员。没有空闲候选人时，才从已经排入其他12点前航班且符合目标岗位资质、人员状态、夜班能力和工时上限的人员中抽调；抽调不复制重叠任务，原岗位改为空缺并在反馈标注“因抽调至XX航班/岗位而空缺”。系统不为此从12点以后航班抽调人员。
- 12点前的高负荷恢复、滚动负荷、重点岗位频率均衡、分级连续轮岗、跨工作日恢复保护和岗位衔接规则均不得主动造成常规岗位空缺。高负荷恢复、滚动负荷和跨工作日恢复只调整公开候选顺序，无安全替代时直接保证岗位完整；只有岗位衔接间隔配置为“严格限制”时，才在无人替代的12点前岗位上允许突破并精确反馈“已突破严格限制仍安排”。值班晚撤人员预留同样不得导致12点前常规岗位空缺。
- 状态非正常、岗位资质不符、夜班能力不符、时间冲突或超过每日工时上限仍是不可突破的硬约束。硬约束导致同时可用的合格人数少于岗位数时允许留空，反馈必须标注“因合格人数不足而无法填满”，列出缺少的岗位人数及状态、资质、夜班、冲突或工时原因；因跨航班抽调形成的源岗位空缺单独说明，不混同为疲劳保护空缺。
- 12点及以后航班按开始时间处理，并继续在岗位完整性前提下优先安排合格人员更少、限制更严格的岗位。航班宽松时优先覆盖尚未获得实际工时的正常常规人员；至少有 4 个航班且压力不为宽松时，候选比较同时计算投放后的当日工时差、历史窗口滚动工时差和当日疲劳差。压力的固定边界为：存在无合格人员任务、峰值并发岗位数与合格常规人员数之比达到 90%，或计划岗位工时与常规人员工时容量之比达到 75% 时为密集；达到 60% 或 45% 时为紧张。行政支援岗位和人员不进入上述分子、分母或个人负荷。这些均衡目标只能调整候选顺序，不能清空已经可以由常规人员承担的岗位。
- 当日值班人员满足人员状态、岗位资质、夜班能力、时间冲突和工时上限硬约束后，必须先按值班岗位有序优先项尝试落位；全部配置项不可执行时，才使用最晚航班的一号、督导、申报或送资料岗位，最晚航班没有可执行目标时再使用倒数第二晚航班的同类岗位。值班人员还必须承担一班 08:30 及以前开始的航班；两项规则均在反馈中说明命中的配置顺序、回退落位或复核原因。值班目标与岗位完整性均优先于岗位衔接、高负荷恢复、滚动负荷、跨工作日恢复保护、重点岗位频率均衡和分级连续轮岗；突破跨工作日恢复保护时必须留下“值班早班锁定优先”等结构化决策记录。
- 高负荷岗位默认定义为疲劳点不少于 4，或岗位配置含“一号、申报、控制”等非空备注；默认恢复时间为 360 分钟。保护开启时优先安排不在恢复期的合格人员；如果恢复期限制会导致岗位空缺，则允许安排仍在恢复期的常规人员，并在反馈中标注“已超保护仍安排”。低负荷岗位不受该规则影响。
- 岗位衔接间隔规则只在目标航班和目标岗位都匹配时生效；候选人此前承担指定前序航班的晚撤岗位，且从前序任务结束到目标任务开始的间隔小于规则值时触发。“优先避开”不删除候选，只在公开的候选顺序中把合规人员排在前面；“严格限制”在存在合规候选时排除违规人员，12点以后所有候选都违规时目标岗位留空并说明命中的规则，12点前常规岗位无人替代时可以突破并反馈留痕。值班目标和 `KE166` 特殊锁定允许高于该保护。默认规则将 `CX931` 的督导、G20、G19 到 `TR121 H02` 的最小间隔设为 180 分钟，排班员可编辑、停用、删除或继续新增同类规则。
- 滚动负荷上限默认统计目标岗位开始前 360 分钟内已经完成的岗位疲劳；仅当即将投放的岗位属于高负荷，且窗口内疲劳加新岗位疲劳超过 8 点时触发。系统优先选择未超限或超限更少的合格人员；如果所有候选人都会超限，则仍须选择其中负荷较低者填满岗位，不得主动留空。
- 重点岗位频率均衡在初始候选排序和生成阶段安全重排中执行。统计键严格为同一“航班号 + 岗位”；先比较排班日期所在自然月的累计承担次数，同数时再比较最近 6 个已归档工作日，仍同数才执行既有工时、疲劳与稳定排序。任一统计层累计至少 2 次且存在更低频合格人员时，生成阶段依次尝试直接接替、同航班两人交换、所有工作时段重叠航班两人交换、包含释放人员的最多三人重排和三岗位闭环交换。目标重点岗位频率必须降低，其他被调整的重点岗位频率不得增加；普通岗位历史累计频率不阻止重点岗位重排。KE166 普通常规岗位可作为重叠航班候选，机动督导及其绑定兼任柜台、值班锁定、人工、引导和行政支援岗位均不参与自动重排。无法安全调整时保留岗位完整性，并在反馈中按实际候选逐项说明无其他资质人员、人员状态或夜班能力、值班或 KE166 锁定、时间冲突、工时上限、岗位衔接、疲劳保护、当日零工时风险或重排会造成岗位空缺。
- 分级连续轮岗在完整排班后执行。重点岗位从上一工作班已承担同一“航班号 + 岗位”时开始复核；普通岗位允许连续两个工作班承担，只在本班将形成第三次连续承担时复核。系统先处理重点岗位，再处理普通岗位，依次尝试同航班两人交换、工作时段重叠航班两人交换和最多三人的闭环交换；普通岗位复核不得移动重点岗位，重点岗位复核形成的结果也不得被后序普通岗位复核撤销。每个方案必须通过岗位资质、人员状态、夜班能力、时间冲突、每日工时上限、岗位衔接和疲劳保护检查，不得减少已填岗位或制造新的岗位空缺。值班锁定岗位、KE166机动督导及其关联岗位、行政支援、引导复用、人工临时岗位和人工修改结果不参与自动交换。第三次连续安排没有安全方案时允许保留以保证岗位完整性，但排班反馈必须显示“连续轮岗未落实”及具体原因；未归档结果不进入跨日判断。
- 跨工作日恢复保护默认把 20:00 以后、距离当日最晚航班开始时间不超过 180 分钟的航班视为最后一批晚班。最近一个已归档工作日的最后一批晚班中，只保护疲劳点最高岗位的承担人员，最高点并列时全部保护。下个工作日先避开启用的结构化早班目标：航班号完全匹配，关键词可命中岗位名称或岗位备注；默认目标为 `CX937 / 一号`、`CX937 / 控制`、`KE166 / 一号`。该保护在重点岗位频率均衡、工时与疲劳均衡之前执行，但低于人员硬约束、值班08:30前早班锁定、KE166机动督导锁定、唯一合格人员和岗位完整性；有其他安全合格人员时必须避开，无安全替代时允许回填。最后一批晚班岗位仍优先把被保护人员安排到不超过配置疲劳上限的靠后岗位。任何突破必须在结构化决策和“上一工作日晚班人员跟踪”反馈中明确写出值班早班锁定、KE166锁定、唯一合格人员、人员状态、夜班能力、时间冲突、工时上限或岗位完整性原因。
- “归档并排后天”先以当前日期替换归档记录，再把日期推进两天并调用同一排班规则；后天航班有变化时可调整航班计划后重新排班。
- 历史疲劳只统计排班日期之前、配置窗口内的记录，默认 7 天。
- 连续工作天数产生额外疲劳惩罚，默认每个连续日 5 点。
- 岗位疲劳点是当前岗位规则的最终当日计分；夜班能力是硬约束，不重复放大已经包含夜间影响的岗位疲劳点。
- 排班结果展示岗位配置备注，但隐藏“一号”字样；单元格临时备注独立保存，不覆盖岗位配置备注。

## 数据模型与生命周期

- `Staff`：人员身份、常规/行政支援类型、分队长标记、CX航前资质、值班资质、夜班能力、状态和备注。
- `Flight`：当天航班、时段、岗位和备注。
- `FlightTemplate`：可被当天航班按航班号套用的时段、岗位和备注预设。
- `PositionRule`：航班与岗位的资质、疲劳点、常规/引导/机动督导/分流/行政支援分类、提前撤岗分钟和手动补位标记。
- `Assignment`：当天生成或人工调整后的岗位分配，分别保存岗位配置备注、当次手工备注、系统说明和结构化决策记录；决策记录保存最终岗位真正命中的规则、阶段、结果和说明，不占用排班单元格的姓名或备注区域。人工换人、交换、清空、重新填写或机动督导同步导致原自动选择失效时，必须同时清除相关系统说明和结构化自动决策记录，反馈只能根据最终排班事实及仍有效的记录生成。
- `HistoryRecord`：已确认并归档的历史工作与疲劳记录。
- `DutyRosterOverride`：按工作班日期保存人工调整后的CX航前、值班和次日两名备勤；未调整日期按当月工作日顺序实时计算，备勤的实际日期由工作班日期加一天得到。
- `DutyPositionPriority`：值班人员目标岗位的有序优先项，保存航班号、岗位或备注关键词及启用状态。
- `MobileSupervisorCoverageRule`：机动督导兼任岗位的结构化允许或禁止规则，保存适用航班、匹配位置、关键词和启用状态。
- 应用状态使用版本化 JSON 存储在浏览器 `localStorage`，当前版本为 3。版本 1 升级时一次性把当时已选“机动督导”的岗位改为“常规”；版本 1/2 升级到版本 3 时把 `TR121 / 一号` 值班优先项迁移为 `TR121 / H02`。连续轮岗不保存按人员定制的迁移或姓名特例，岗位资质始终以用户配置和导入数据为准；版本 3 以后用户手工调整的机动督导分类、值班优先项和岗位资质均按原值保存，不再重复覆盖；损坏或不兼容的数据会回退到内置初始配置。
- 应用没有后端、账号、遥测或远端数据写入。

## Excel 合同

### 导入

识别以下工作表名称或包含这些关键词的名称：

- `人员信息`：编号、姓名、是否可上夜班、状态、备注、人员类型（常规或行政支援）、CX航前资质、值班资质和是否为分队长。
- `航班计划`：当天航班号、开始时间、结束时间、预定人数（运力）、涉及岗位、备注。
- `航班配置` / `航班模板`：可复用航班号、开始时间、结束时间、涉及岗位、备注。
- `岗位配置`：航班号、分类、岗位名称、备注、可胜任人员、疲劳点、启用旅客人数和提前撤岗分钟。
- `各航班岗位疲劳度计算`：航班号、岗位名称、岗位疲劳点数。
- `历史排班` / `排班结果`：日期、航班、岗位、姓名、时段、工时、疲劳点和备注。

导入配置类工作表会替换对应当前配置并清空尚未归档的排班；导入历史记录按日期、航班、岗位和姓名去重合并。

### 导出

- `保障明细`：按现行业务表习惯，以航班为岗位/人员双列并排展示；人员格包含可见岗位备注与当次手工备注。
- `排班结果`：机器可读的逐岗位长表，可再次导入。
- `人员排班`：按人员展开的航班、岗位、时段和工时明细。
- 左侧“导入数据”用于混合导入配置或历史；配置页的“导入配置模板”只更新配置，历史页的“导入历史排班结果”只累计疲劳历史；左侧“导出配置”只导出配置和模板，排班页的“导出排班结果”导出当天结果。
- HTML 文件自包含样式，可离线打开、打印和转发。
- PNG 使用与 HTML 相同的航班视角和人员视角生成高清长图。

## 技术架构

- TypeScript 5.9，严格类型检查。
- Vite 7 负责开发服务和静态生产构建。
- Bootstrap 5 与 Bootstrap Icons 提供组件、布局和图标基础。
- `xlsx-js-style` 处理 Excel 导入、样式和导出。
- `html2canvas` 生成排班 PNG。
- Vitest 4 保护时间、疲劳、约束、排班、Excel 映射和状态恢复等核心逻辑。
- `src/domain/` 只处理业务规则和中央规则阶段合同；`src/infrastructure/` 处理浏览器存储、Excel 和分享文件；`src/ui/` 只负责界面渲染；`src/app/` 分别负责配置、规则设置、排班编辑、历史和工作簿应用等状态动作；`src/app.ts` 只负责应用生命周期、DOM 事件分发、确认提示、重新排班和持久化编排。

## 运行、验证与分发

- 开发：`npm.cmd run dev`
- 类型检查：`npm.cmd run typecheck`
- 核心测试：`npm.cmd test`
- 生产构建：`npm.cmd run build`
- 完整验证：`npm.cmd run verify`
- 构建产物：`dist/`，可部署到任意静态站点或直接作为静态包分发。
- GitHub Pages 由 `.github/workflows/deploy-pages.yml` 在 `main` 分支推送后自动构建和部署。

## 验收标准

- 完整验证命令退出码为 0。
- 默认配置可以生成排班，所有自动分配均满足硬约束。
- 人工改派无法绕过资质、状态、夜班、冲突和工时规则。
- 配置与历史 Excel 可以解析，三类分享文件可以在浏览器端生成。
- 桌面和移动端均能完成核心路径，页面无不可访问的资源或阻塞性控制台错误。

## 授权边界

项目按根目录 `LICENSE` 中的 MIT License 分发，版权人为二次元虎哥。
