# 每周航班计划与下一工作日确认

## 需求文档

### 用户目标

航班按星期一至星期日形成固定计划。排班员先在配置页为每个星期勾选已有航班模板；点击“归档并排后天”时，系统按目标日期所属星期自动预选航班，只需确认或做一次性增减，不再每天对照当前班航班。

### 已确认交互与边界

- 下一工作日继续使用现有规则：当前日期加两天，跨过一个休息日。
- 配置页新增独立“每周航班计划”模块，星期一至星期日分别勾选已有航班模板。
- 目标日期决定使用哪一天的预设。例如目标日期为星期三，弹窗默认勾选星期三计划。
- 弹窗允许临时勾选或取消；修改只影响本次生成，不回写星期预设。
- 星期预设和弹窗候选都只使用已存在的航班模板；新航班必须先建立模板。
- 每周航班计划保存到浏览器本地，并纳入配置 Excel 导入导出。
- 不依赖在线查询，不改变现有排班规则、优先级、硬约束或计算流程。

### 验收标准

- 可以分别配置并恢复星期一至星期日的航班勾选状态。
- 点击“归档并排后天”后，弹窗显示目标日期和星期，并按该星期预设默认勾选。
- 弹窗临时增减不会修改星期预设。
- 选中航班按模板物化，预定人数初始化为 0；未选航班不进入下一工作日排班。
- 星期计划在刷新、本地恢复及配置 Excel 导入导出后保持一致。
- 删除模板后对应星期引用被清理；重命名模板后星期引用同步更新。
- 取消、停止、计算失败或没有完整安全结果时，原日期、航班、历史和班表保持不变。

## 当前事实

- 现有 `next-workday-flight-plan.ts` 将当前班航班作为默认选择，并补入没有模板的当前临时航班；这与新需求不一致。
- 现有确认弹窗提供“沿用本班”“全选”“清空”，其中“沿用本班”需要改为恢复目标星期预设。
- `AppState` 当前版本为 4，没有每周航班计划字段。
- 航班模板由 `configuration-actions.ts`、配置页和 configuration commands 管理。
- 配置 Excel 当前导入导出航班模板，但没有每周航班计划工作表。
- “归档并排后天”已经使用临时 AppState 计算并在成功后原子安装；该事务语义继续复用。
- 工作区包含当前功能的既有未提交改动，不回滚、不提交、不推送、不部署。

## 失败测试

- 目标日期为星期一时，候选默认选择星期一预设，而不是当前班航班。
- 星期一和星期二配置不同航班时，两个目标日期得到不同默认选择。
- 未配置的星期仍显示全部模板候选，但默认不选中；用户可以临时勾选。
- 弹窗临时取消或增加航班后，每周预设保持不变。
- 配置页可以切换七个星期并勾选模板；命令只更新指定星期。
- 模板重命名同步替换七日计划中的旧航班号；删除模板清理对应引用。
- 本地状态版本迁移和保存恢复保留每周计划；旧版本初始化为空计划且不破坏其他数据。
- 配置 Excel 的“每周航班计划”工作表可以往返；工作表缺失时保留现有计划，存在空计划时允许清空。
- 成功生成仍只安装本次弹窗最终选择；失败或停止仍完整回退。

## 目标

- 建立单一、可持久化、可导入导出的七日航班预设。
- 让下一工作日确认弹窗成为“星期预设的本次确认”，而不是“复制当前班再筛选”。
- 沿用现有模板、Store、控制器、Excel 和事务生成边界，不引入第二套航班实体。

## 不做范围

- 不修改在线航班查询。
- 不允许在星期计划或确认弹窗中直接创建无模板航班。
- 不把弹窗的临时修改自动保存为星期预设。
- 不实现法定节假日、单双周、日期例外、远端同步或按日期版本库。
- 不修改排班内核、规则合同、规则优先级或安全复核。
- 不 commit、push、部署或发布。

## 设计

### 1. 数据合同

- 在 `AppState` 增加七日航班计划，按 ISO 星期一至星期日保存规范化航班号列表。
- 状态版本升级；旧版本迁移为空的七日计划，不根据当前班静默推断星期配置。
- 统一领域函数负责日期到星期、计划清洗、预设读取和模板候选生成。

### 2. 配置模块

- 配置页新增独立模块，使用星期一至星期日切换控件和航班模板复选列表。
- 勾选变化通过类型化 UI command、configuration controller 和 Store command 持久化。
- 修改只影响未来确认默认值，不清空当前班表。
- 模板重命名和删除同步维护七日计划引用。

### 3. 下一工作日确认

- 控制器计算目标日期后读取对应星期预设，基于全部已有模板生成候选。
- 弹窗显示目标星期；“恢复星期预设”替代“沿用本班”，继续保留“全选”“清空”。
- 临时选择只保存在 dialog view state；确认后按最终选择物化航班并进入现有事务计算。
- 没有星期预设时不自动猜测，弹窗默认空选并允许现场勾选。

### 4. 持久化与 Excel

- 浏览器状态恢复验证七日和航班号列表，损坏项局部丢弃，不重置无关配置。
- 配置 Excel 新增“每周航班计划”工作表，每个星期一行、航班号逗号分隔。
- 工作表存在时完整替换七日计划，空表表示清空；工作表缺失时保持当前计划。
- 导入计划只保留当前有效航班模板能够识别的航班号。

### 5. 规则保持

本功能仅改变航班输入来源。生成后继续完整执行人员状态、资质、夜班、时间冲突、衔接、工时、岗位完整性、值班、KE166、人工锁定、恢复、轮岗和最终安全复核。

## 实施任务

- [x] 核实现有 picker、配置页、Store、状态恢复和 Excel 边界。
- [x] 先新增日期星期预设和候选默认值失败测试。
- [x] 实现七日计划领域合同与 AppState 版本迁移。
- [x] 先新增配置动作和配置页失败测试，再实现每周计划模块。
- [x] 先新增 Excel 与本地恢复失败测试，再实现往返合同。
- [x] 更新确认弹窗和控制器，使目标星期预设成为默认选择。
- [x] 保持事务生成成功/失败/停止测试通过。
- [x] 同步 `spec.md`、`README.md` 和配置 Excel 合同。
- [x] 运行近端测试、L2 完整功能验证、差异和状态检查。
- [x] 记录真实结果、未验证风险和外部操作状态。

## 验证计划

### 近端测试

```powershell
npm.cmd exec vitest -- run tests/domain/weekly-flight-plan.test.ts tests/domain/next-workday-flight-plan.test.ts tests/app/configuration-actions.test.ts tests/app/application-coordinator.test.ts tests/app/workbook-actions.test.ts tests/infrastructure/storage.test.ts tests/infrastructure/excel.test.ts tests/ui/config-view.test.ts tests/ui/next-workday-flight-picker-dialog.test.ts
```

### L2 完整功能验证

本次涉及 `AppState`、迁移、Excel/localStorage 和跨模块命令，按项目验证矩阵运行：

```powershell
npm.cmd run typecheck
npm.cmd test
npm.cmd run build
git diff --check
git status --short
```

不涉及求解器、Worker 或性能模型，不默认运行性能测试；不执行 commit、push 或部署。

### 手工验收

- 在配置页分别设置两个星期的不同航班，刷新后确认保存。
- 将当前日期设为能分别命中两个目标星期的日期，确认弹窗默认选择不同。
- 临时增减后取消，重新打开确认星期预设未变化。
- 导出配置并在无记录状态导入，确认七日计划恢复。
- 桌面和手机宽度检查星期切换、列表滚动、复选框和按钮无重叠。

## 收口

已完成实现和分层验证：

- 近端测试共 9 个文件、79 项通过。
- `npm.cmd run typecheck` 通过。
- `npm.cmd test` 共 86 个文件、580 项通过。
- `npm.cmd run build` 通过。
- 使用 `playwright-core` 复用本机 Edge 检查 1440×1200 和 390×844 视口；两种视口均无横向溢出，星期选择、航班列表和确认按钮无重叠或截断。
- 最终 `git diff --check` 通过（只有 Git 的 LF/CRLF 提示，没有空白错误）；工作区状态已复核，未回滚既有改动；`src`、`tests`、`scripts` 中未发现 `[DEBUG-` 标记。

未验证风险：

- 动态“导出配置”及导入往返已覆盖新工作表；静态下载模板 `public/template/排班工具配置模板.xlsx` 尚未加入“每周航班计划”工作表。
- 未在另一台真实设备上执行配置导入导出手工验收。
- 未运行性能测试或 `npm.cmd run verify`；本功能未改求解器、Worker 或性能模型，按当前 L2 验证策略收口。
- 未执行 commit、push、部署或发布。
