# 当天排班核心规则精确求解与恢复公平近似优化

## 2026-08-13 后续授权修订

- owner 已明确授权把当天整体模型总计算上限由 120 秒延长为 150 秒；本文件下方关于 120 秒的内容保留为上一阶段实施记录，不再作为当前产品合同。
- owner 已明确授权为“跨工作日恢复目标”增加全局执行强度：默认“优先避开”保持现有 best-effort 顺序；“严格限制”时，上一工作班末班重点岗位受保护人员不得承担配置的次班恢复目标岗位。
- 严格模式属于硬约束阶段，前置于岗位完整性、重点岗位公平及全部恢复/负荷目标；不突破人员状态、资质、夜班、时间、衔接、工时、值班和 `KE166` 锁定，也不允许用空缺绕过。没有安全完整方案时整次排班失败并保留原班表。
- 本次同步维护 `spec.md`、中央合同、默认与归一化、localStorage 恢复、Excel 往返、规则页、整体模型、后置重排安全和最终安全复核；先建立失败测试，收尾执行 `npm.cmd run verify`。
- 当前实现已完成：150 秒常量与用户提示、`nextWorkdayRecoveryMode` 默认/归一化/Excel/规则页、严格模式模型禁选和无空缺绕过、后置交换安全检查、中央规则展示及产品说明；近端类型检查和 7 组相关测试通过。

## 需求文档

- owner 于 2026-08-13 确认新的产品取舍：当天排班最多计算 120 秒；人员安全、岗位完整性、特殊锁定和重点岗位核心轮换继续精确完成；从跨工作日恢复开始的早下班、普通衔接、人员覆盖、负荷和公平目标不再要求证明绝对最优，在明确的小幅 MIP Gap 和总时限内尽量优化。
- 真实业务目标是：8 至 10 个航班时，不再为了证明恢复或公平指标最后一点改进空间而丢弃一张已经完整、安全且满足核心运营规则的班表。
- MIP Gap 只能放宽软目标的最优性证明，不能放宽任何硬约束、岗位完整性、值班、人工锁定、`KE166`、跨工作日资质预留、结构化严格岗位衔接、配置的跨航班重点岗位优先或重点岗位核心轮换。
- 软目标达到允许差距时，锁定当前实际目标值并继续优化更低优先级软目标；整体达到 120 秒时停止，不再启动更低层，采用当前层官方确认有效的完整解，当前层没有有效解时回退到上一层已经保存的完整解。
- 任何被采用的近似或限时结果都必须通过整体模型物化后的安全复核，以及补缺、轮岗、`KE166` 收尾等后置流程结束后的最终安全复核。
- 普通用户只看到业务语言，不显示 `HiGHS`、`MIP`、gap、incumbent、dual bound、变量名、目标 ID 或求解器状态。
- owner 已授权按本计划正式修订 `spec.md`，不需要再次确认上述精确/近似边界。只有发现新的规范冲突、无法证明某个 gap 不改变核心规则，或必须扩大需求范围时才暂停上报。

## 当前事实

- 当前仓库为 `G:\浏览器下载\codex项目\autoschedule`，当前分支为 `全局化highs`。工作区存在大量 owner 未提交改动，实施者不得回滚、覆盖、格式化或清理无关改动，不得 commit、push 或部署。
- 当前整体模型已经使用一个 120 秒总 deadline，逐目标复用同一个 HiGHS 模型，并已接入 `Highs_zeroAllClocks`、官方 `primal_solution_status`、低优先级超时采用和两次最终安全复核。
- 当前产品合同和实现仍把直到 `candidate:cross-workday-load` 的目标标记为 `required`；只有 `workload-balance` 七层和 `historical-fatigue` 是 `best-effort`。这与 owner 本次授权的新边界冲突，必须同步修订产品事实、目标合同、实现和测试。
- 当前 HiGHS 适配器把每层 `mip_rel_gap` 固定为 `0`。对能够证明离散步长的目标，现有 `objectiveValueStep` 会把 `mip_abs_gap` 设置为“小于一个最小离散步长”，这是精确最优的证明捷径，不是允许近似。
- 真实无历史 8 航班诊断规模为 24 人、1413 个变量、1915 条约束。120 秒多次运行有时成功进入低优先级工时公平，有时在 `candidate:high-load-recovery` 超时并拒绝班表，结果不稳定。
- 80 秒分层诊断中，`rolling-load` 约 35.51 秒完成；`high-load-recovery` 约 42.36 秒时已有目标值 16，数学下界停在 15。它只差 1 个最小业务单位，但 16 与 15 的相对差约为 6.25%，单独设置 5% 相对 gap 仍不能解决这个已确认慢点。
- 因此本计划不是“只给最后一层设置 5%”。它必须覆盖从恢复开始的整段软目标，并对已证明离散步长的次数/排名目标允许一个最小业务单位的绝对 gap，同时对软目标设置 5% 相对 gap。
- `src/infrastructure/solver/highs-solver.ts` 当前只从本地 HiGHS 包装器读取模型状态和 primal solution status，尚未读取官方 `mip_gap` 或 dual bound。若不补充该信息，系统会把“容差内完成”误报成“绝对最优”。
- 当前 `DailySchedulePlan.optimizationQuality` 只有 `all-objectives-optimal` 与 `fairness-time-limited`。MIP Gap 提前停止并不等于计算时间用完，需要结构化区分“全部精确最优”“容差内完成”“到时限采用”。
- 当前存在一个按 TDD 保留的红灯：`tests/domain/daily-schedule-optimizer.test.ts` 中 `adds a safe independent-capacity cut for high-load combinations` 失败，缺少 `high-load-independent-capacity:worker` 约束。该有效不等式只收紧下界、不删除合法排班，直接针对 16/15 证明困难，应与 MIP Gap 共同完成。
- 本次真实验收曾使用临时诊断入口和 `[DEBUG-8FLIGHT]` 输出；验收完成后入口、测试和调试输出均已删除，仓库不保留真实姓名、工作簿或本机路径依赖。
- 当前机器存在 `G:\浏览器下载\排班工具配置 (7).xlsx`。它加入模板航班 `AK151` 后形成已诊断的 8 航班场景；旧计划引用的 `(5).xlsx` 当前不在下载目录，不能作为本轮唯一验收入口。
- 相关诊断研究保存在 `docs/research/highs-lexicographic-workload-timeout.md`。其中旧的“恢复、滚动负荷、连续高负荷必须绝对最优”产品结论已被 owner 本次授权替代；求解器计时、解状态、离散步长和模型收紧等技术事实仍然有效。

## 失败测试

- 当前匿名高负荷三航班测试找不到 `high-load-independent-capacity:worker`，证明组合模型尚未加入安全的独立容量下界。
- 当前目标合同测试只把 `workload-balance` 与 `historical-fatigue` 标记为 `best-effort`，没有从 `late-shift-recovery` 开始切换为近似优化。
- 当前求解器测试会看到每一层 `mip_rel_gap = 0`，不能证明软目标获得 5% 相对 gap，也不能证明 required 目标仍为 0。
- 构造 `high-load-recovery` 当前值 16、数学下界 15、最小步长 1 的软目标：当前合同仍要求继续证明绝对最优；新合同应允许一个最小业务单位的绝对 gap，接受 16 并继续下一软层。
- 构造最后一层软目标并模拟 HiGHS 报告实际 `mip_gap = 0.04`：当前结果会被当成全部最优；新结果必须记录为“容差内完成”，不得声称绝对最优。
- 模拟软目标达到 120 秒且有官方确认有效的完整解：应采用当前解并停止更低层；当前层无有效解时应回退上一完整解。
- 模拟 required 目标超时，即使存在 incumbent 也必须失败，不得暴露 assignment。
- 模拟容差内或限时采用的方案违反硬约束、岗位唯一性、锁定或 required 目标值：最终安全复核必须拒绝。
- 真实 8 航班连续运行时，当前版本仍可能在 `high-load-recovery` 超时并显示排班失败。

## 目标

- 明确并固定精确目标边界。以下规则及其模型目标必须继续 `required` 且精确完成：
  - 人员状态、人员类型、岗位资质、夜班能力、时间冲突、适用的全局最小航班衔接、每日工时上限等全部硬约束。
  - `KE166` 机动督导预留、值班12点前上午航班、值班晚撤岗位和人工锁定。
  - 岗位完整性、12点前稀缺岗位、跨航班抽调和同航班空缺下沉所依赖的整体岗位目标。
  - 跨工作日资质预留、结构化严格岗位衔接、配置的跨航班重点岗位优先。
  - 末班分类差值安全边界、末班重点岗位合计轮换、末班督导/一号/申报/送资料公平、一般重点岗位频率和重点岗位连续承担。
- 从 `candidate:late-shift-recovery` 开始，其后的目标全部显式标记为 `best-effort`：跨工作日恢复、末班重点岗位次班截止、高疲劳普通岗位连续承担、当天早晚负荷分散、上一班末班重点人员晚班轻岗优先、普通岗位衔接、当日在岗覆盖、滚动负荷、连续高负荷、跨工作班负荷互补、工时与疲劳均衡、历史疲劳。
- required 目标继续设置 `mip_rel_gap = 0`；若具备经过证明的 `objectiveValueStep`，允许使用“小于一个最小步长”的 `mip_abs_gap` 作为数学上仍然精确的证明捷径。
- best-effort 目标设置 5% 相对 gap。具备经过证明的离散步长时，同时允许一个最小业务单位的绝对 gap；HiGHS 满足相对或绝对任一条件即可结束该层。不得为未证明离散步长的目标猜测绝对单位。
- gap 必须来自每个目标的显式中性求解合同，不得在 `highs-solver.ts` 根据“最后一层”、数组下标、目标 ID 前缀或业务名称硬编码。
- 软目标在容差内结束后，保存官方确认有效的 incumbent，记录实际 gap，按当前实际目标值添加精确锁，再继续下一软目标；不得用容差范围锁定并允许后层反向恶化当前结果。
- 达到整体 deadline 后不再运行更低层。只要全部 required 目标已经精确完成，就可以采用当前有效完整解或上一完整解；required 尚未完成时仍然失败。
- 采用近似或限时结果后，岗位、人员、空缺、工时、疲劳、值班、锁定、预留、`KE166`、分流和全部硬约束事实必须通过现有物化后与最终收尾后安全复核。
- 真实 8 航班场景至少连续运行 3 次，均应在约 120 秒合同范围内返回可用班表，不再因为恢复或公平层没有证明绝对最优而失败。

## 不做范围

- 不放宽任何硬约束、岗位完整性、特殊锁定、人工结果或上列 required 核心运营目标。
- 不改变现有规则顺序；只改变从 `late-shift-recovery` 开始的最优性要求和允许的证明差距。
- 不把所有目标合成一个综合权重，不使用姓名、航班号、岗位名称特例，不用随机种子重试挑结果。
- 不把 `mip_rel_gap = 0.05` 全局设置给 required 目标，不只判断最后一层，也不把任何超时 incumbent 无条件当成可用结果。
- 不为连续值目标擅自假设 0.5 疲劳点或 30 分钟步长；只有当前配置、Excel 和状态合同能够证明离散单位时才能声明绝对 gap。
- 不迁移 OR-Tools、Timefold 或后端服务，不升级 HiGHS，不全局关闭 presolve，不继续增大 120 秒。
- 不自动删除或合并重复航班，不修改与本性能任务无关的页面、配置和排班规则。
- 不提交真实工作簿、真实姓名、本机绝对路径依赖或临时诊断输出。

## 设计

### 产品与目标边界

- 正式修订 `spec.md` 的当前能力、合理排班宏观原则、规则合同与冲突裁决、技术架构和验收说明：核心规则精确完成；从跨工作日恢复开始为限时近似优化；120 秒后可采用通过复核的完整班表。
- 规则执行顺序保持不变。新的唯一边界为：`candidate:priority-position-consecutive` 及之前全部 `required`；`candidate:late-shift-recovery` 及之后全部 `best-effort`。
- `best-effort` 不表示规则关闭。它仍按原顺序优先于后续目标，只是不再要求为了证明最后一个很小的改进空间而耗尽整次计算。
- 后置的分级连续轮岗、末班公平复核和 `KE166` 收尾继续执行现有安全逻辑，不得借本次改动弱化或绕过 required 结果。

### 中性求解合同

- 在 `src/domain/solver/solver-port.ts` 为 `LexicographicObjective` 增加显式允许差距，例如：

```ts
interface AcceptedObjectiveGap {
  relative?: number;
  absolute?: number;
}

interface LexicographicObjective {
  // existing fields
  optimality?: "required" | "best-effort";
  acceptedGap?: AcceptedObjectiveGap;
}
```

- 命名可按仓库现有风格调整，但字段必须表达“产品允许的近似差距”，不能与现有 `objectiveValueStep` 混为一谈。`objectiveValueStep` 证明目标取值的最小离散单位；`acceptedGap` 表示 owner 允许的业务近似。
- 合同验证必须拒绝：required 目标声明非零允许差距；相对 gap 小于 0 或大于等于 1；绝对 gap 为负数或非有限数；required 目标排在 best-effort 之后。
- 最终模型构建阶段按语义为所有 `best-effort` 目标设置 `acceptedGap.relative = 0.05`。只有已经证明存在 `objectiveValueStep` 的目标才同时设置 `acceptedGap.absolute = objectiveValueStep`。
- 对次数/排名类软目标补齐可证明的 `objectiveValueStep`。证明必须来自目标系数和变量语义，不得根据目标名称猜测；无法证明时只使用 5% 相对 gap 和总时限兜底。

### HiGHS 适配器

- `highs-solver.ts` 每层设置参数时遵循：
  - required：`mip_rel_gap = 0`，`mip_abs_gap = objectiveValueStep - epsilon` 或 `0`，保持精确。
  - best-effort：`mip_rel_gap = acceptedGap.relative ?? 0`；`mip_abs_gap = max(精确离散阈值, acceptedGap.absolute ?? 0)`。
- 现有“理论变量边界已达到则跳过求解”继续保留；它是精确结论，不应被标记为近似。
- 扩展本地 `vendor/highs-ts` 中性包装器，使用 `Highs_getDoubleInfoValue` 读取官方 `mip_gap`，必要时一并读取 `mip_dual_bound` 供测试和内部诊断。同步 `src`、构建产物和 `.d.ts`，不得只改某一种运行入口。
- HiGHS 因设置 gap 返回完成状态时，必须结合官方 primal solution status 和实际 `mip_gap` 判断：
  - `mip_gap` 为 0 或在数值容差内为 0，记录为精确完成。
  - `mip_gap` 大于 0 且不超过目标允许值，记录为容差内完成。
  - 没有官方可行解、实际 gap 超过允许值或读取状态失败，不能伪装成成功。
- 容差内完成后读取当前解，计算并保存当前目标值，按该实际值添加精确锁，然后继续下一软层。
- deadline 到达或当前软层 `timelimit`：有官方可行解则优先采用当前 incumbent；无有效 incumbent 则退回上一份已经完整保存的方案；随后停止，不继续更低目标。
- `infeasible`、模型错误、包装器错误、安全复核失败仍然失败，不得包装成“已尽量优化”。

### 结果、反馈与安全

- 扩展结构化结果，至少能区分：
  - 所有运行目标均精确最优。
  - 一个或多个软目标在允许 gap 内完成。
  - 软目标因 120 秒停止并采用当前或上一完整解。
- 可以扩展 `SolverResult`、`BestEffortSolveDetails` 和 `DailySchedulePlan.optimizationQuality`，但不能根据 diagnostic 文本反向解析业务状态。
- `completedObjectiveIds` 必须区分 required 精确完成与 soft 容差内完成；`assertTimeLimitedResultIsEligible` 或等价入口必须继续证明全部 required 目标精确完成。
- gap 内结束但未用满 120 秒时，不得显示“计算时间已用完”。建议普通业务反馈：`班表已满足全部硬性要求和核心排班规则；人员恢复与公平已优化到允许的小幅差距内。`
- 真正达到 120 秒时，建议普通业务反馈：`班表已满足全部硬性要求和核心排班规则；人员恢复与公平已在可用时间内尽量优化，仍可能存在小幅改善空间。`
- 两类提示各最多一次；人工调整使自动优化证据失效后，按现有最终事实机制重新计算或清除，不保留过期说明。
- 页面不得显示百分比、gap、求解器状态或内部目标名称。

### 连续高负荷模型收紧

- 完成当前红灯要求的通用有效不等式，不写死人员、航班、岗位或 `high-load-recovery` 的数据特例。
- 按人员收集高负荷组合组；每组对应一个目标时段、目标选择和 combination penalty。
- 只纳入组内目标选择能够证明互斥的安全节点。遇到分队长并行督导等可能同时成立的组必须跳过，不能错误收紧可行域。
- 根据组之间的来源关系建立冲突图，计算该人员最多可相互避开的高负荷组数量 `alpha`，加入：

```text
sum(combination penalties) - sum(selected high-load targets) >= -alpha
```

- 日航班通常不超过 10 个，小图可以精确计算最大独立集；必须设置明确的节点上限和安全回退。超过边界或无法证明时跳过该 cut，不能使用不安全近似。
- 该 cut 只能增强数学下界，不得删除任何原有合法班表或改变规则优先级。使用匿名等价测试比较加 cut 前后的完整可行解和目标向量。

## 实施任务

- [x] 开始前完整阅读并核对规范、计划、研究资料、工作区和真实验收文件。
- [x] 先建立并确认求解合同、目标边界、Gap、官方状态和 capacity cut 红灯。
- [x] 修订 `spec.md`，明确 required/best-effort 边界、120 秒采用条件和普通业务反馈。
- [x] 扩展 `solver-port.ts` 显式允许差距与结构化结果，默认调用方保持 required 精确。
- [x] 日排班目标从实际可用的恢复目标开始进入 best-effort，声明 5% 相对 Gap；证明离散步长时声明一个最小绝对单位。
- [x] 本地 `vendor/highs-ts` 读取官方 `mip_gap` 与 `mip_dual_bound`，同步源码、类型、构建产物和 WASM。
- [x] `highs-solver.ts` 使用总 deadline、清零官方时钟、官方 primal 状态和显式 Gap；容差完成按实际值锁定，超时停止更低层。
- [x] 物化后与 KE166 等后置收尾后均执行最终安全复核；页面只显示普通业务语言。
- [x] 实现安全 `high-load-independent-capacity` 有效不等式，并在无法证明互斥或超过小图边界时跳过。
- [x] 近端求解器、领域、安全、反馈测试全部通过。
- [x] 使用真实工作簿、日期和 AK151 连续三次验收 8 航班，记录聚合指标。
- [x] 匿名 9 至 10 航班真实工作簿场景未验证；保留为剩余风险。
- [x] 删除临时诊断入口和测试；未发现需保留的 `[DEBUG-...]` 输出。
- [x] `npm.cmd run verify`、`git diff --check`、`git status --short` 已执行；不 commit、不 push、不部署。

## 验证计划

- 求解器近端测试：

```powershell
npm.cmd test -- tests/infrastructure/highs-solver.test.ts tests/infrastructure/highs-solver-native-reuse.test.ts
```

- 领域目标、模型、安全与反馈近端测试：

```powershell
npm.cmd test -- tests/domain/daily-schedule-optimizer.test.ts tests/domain/daily-schedule-safety.test.ts tests/domain/schedule-feedback.test.ts tests/domain/schedule-warning-message.test.ts
```

- 测试必须证明：
  - required 每层 `mip_rel_gap = 0`，离散绝对阈值仍不足一个最小步长，保持精确。
  - best-effort 每层设置 5% 相对 gap；存在已证明离散步长时允许一个最小单位绝对 gap。
  - `16/15` 的高负荷软目标可以因绝对 gap 1 收口；不能错误声称绝对最优。
  - 最后一层软目标实际 gap 为 4% 时提前完成，结构化标记为容差内，不显示“时间已用完”。
  - 实际 gap 超过允许值、没有 primal 可行解、required 超时、infeasible、error 和安全复核失败均不得采用。
  - 软层容差内完成后按当前实际值锁定并继续下一层；120 秒超时后不继续更低层。
  - 人员状态、人员类型、资质、夜班、时间冲突、分流衔接、常规 90 分钟衔接、工时上限、岗位唯一性、值班、人工锁、`KE166`、预留、严格衔接和跨航班重点岗位优先均未改变。
  - `high-load-independent-capacity` cut 在安全小图中出现，在可能并行或超过回退边界时安全跳过。
- 真实 8 航班验收已使用 owner 提供的工作簿、`2026-09-29` 和额外模板航班 `AK151` 连续运行三次；临时验收入口已在收口前删除。
- 每次验收必须满足：约 120 秒合同边界内返回班表；required 目标全部精确完成；官方存在有效完整解；两次安全复核通过；没有因为 `high-load-recovery` 或后续软层未证明绝对最优而失败。三次结果可以是同质量的等价人员互换，不要求逐格姓名完全一致。
- 完整收尾：

```powershell
npm.cmd run verify
git diff --check
git status --short
rg -n "\[DEBUG-" src tests scripts
```

- `verify` 必须退出码为 0，并覆盖类型检查、普通测试、性能测试和生产构建。若工作区已有与本任务无关的基线失败，必须提供修改前后证据并上报，不能伪报全量通过。

## 收口

- required 目标在所有运行入口保持零相对/绝对容差；排班恢复、公平、普通衔接和负荷目标按显式合同使用 5% 相对 Gap，已证明离散步长时使用一个最小业务单位绝对 Gap。
- 软目标在允许 Gap 内完成后，读取当前实际目标值并精确锁定再进入下一层；120 秒到达后仅在 required 全部完成、官方存在完整可行解、两次安全复核通过时采用当前或上一完整解。
- 真实 8 航班三次结果：`121027/120685/120679ms`；均为限时采用，8 航班、64 岗位结果、2 个业务空缺、工时差 1.75 小时、疲劳差 10.5 点。
- 近端测试 61 项通过；真实 WASM 冒烟确认 `mipGap=0`、`mipDualBound=1` 和完整解状态。
- 最终 `npm.cmd run verify`：82 个测试文件/544 项普通测试、3 个性能文件/12 项性能测试、生产构建全部通过；随后正式文件名 WASM 重链、Node gap 冒烟和再次生产构建通过。
- `git diff --check` 通过；`rg -n "\\[DEBUG-" src tests scripts` 无结果。
- 真实 9 至 10 航班场景未验证；现有工作区其他未提交改动不在本计划范围内。
- 外部操作状态：未 commit、未 push、未部署。

## 2026-08-13 后续方案：末班人员优先获得更早安全下班机会

### 需求文档

owner 已明确：跨工作日恢复中的“次班截止时间”不是要求人员恰好在该时刻下班，而是一个“最晚允许继续工作的界线”。只要当前班次存在更早的完整安全下班机会，就必须优先把该机会给上一工作班承担末班重点岗位的人员。

业务例子：曾子卓上一班承担 `TR121/H02` 末班重点岗位；本班曾子卓与董玲都能承担下午岗位，且让任意一人完成下午航班后下班都不会造成岗位空缺、资质冲突、时间冲突或其他硬约束违规，则必须选择曾子卓完成下午航班后下班，不能因为普通公平或其他软目标让董玲先下班。

### 当前事实

- 当前页面已能配置“跨工作日恢复目标强度”，并区分“优先避开”和“严格限制”。
- 当前“次班早班避让目标”的严格模式只约束配置的次班目标岗位，例如 `CX937/G18`；它不自动禁止上一班末班重点人员继续承担本班晚间 `TR121/一号`。
- 当前“次班截止时间”在排班完成后的后置换位阶段处理，目标是让受保护人员的最终下班时间提前到截止时间之前；它尚未把“任何更早安全下班机会优先归给受保护人员”建模为全局优先级。
- 当前后置换位对每个受保护人员逐项尝试，受 `maxParticipants: 3`、岗位资质、时间冲突、岗位完整性、锁定岗位、每日工时、值班和公平回退检查约束。
- 本方案只处理规则优先级和后置换位目标，不回滚工作区已有改动，不提交、推送或部署。

### 失败测试

先在后置换位或其正确的领域测试接缝建立匿名红灯：

- 上一工作班有一条末班重点历史记录，人员 A 受跨工作日恢复保护；人员 B 没有上一班末班重点保护。
- 本班存在一个下午岗位，A 与 B 均具备资质，A 完成该岗位后即可在配置截止时间之前结束；B 的候选方案也能安全完成。
- 在普通公平、历史疲劳或其他软目标可能偏向 B 的情况下，结果仍必须让 A 获得更早下班机会。
- 如果 A 只有在承担更晚岗位时才能满足岗位完整性，测试必须允许 A 继续工作，并要求结构化原因说明“没有更早完整安全方案”。
- 测试必须覆盖多个受保护人员：截止时间更早、上一班结束更晚的人员优先处理；不得让未受保护人员抢占受保护人员的更早安全下班机会。

### 目标

- 将“上一班末班重点人员优先获得更早安全下班机会”变成跨工作日恢复后置阶段的显式优先级，而不是普通公平目标中的一个可让步项。
- 将配置的截止时间定义为受保护人员的最晚工作界线：早于该时间结束更好，达到或晚于该时间才视为需要回退说明。
- 保持“严格限制”对次班恢复目标岗位的硬约束语义不变；本方案不把截止时间误改成无条件硬约束。
- 在所有硬约束和岗位完整性满足的方案中，先最大化受保护人员获得的提前下班机会，再比较普通人员的早下班、公平、工时和疲劳软目标。
- 没有完整安全方案时保留原班表或当前安全方案，并记录具体原因；不得通过制造空缺、违反资质或破坏锁定岗位来满足提前下班。

### 规则优先级裁决

本需求只改变“截止保护”在既有保护阶段中的比较方式，不改变中央合同的阶段顺序：

1. **绝对不能突破的硬约束**：人员状态、岗位资质、夜班能力、时间冲突、普通岗位最小衔接、每日工时上限、岗位唯一性、岗位完整性、值班与 `KE166` 锁定、人工锁定，以及“严格限制”配置下的次班恢复目标禁止进入。
2. **特殊岗位与覆盖**：硬约束通过后，继续执行值班/`KE166` 特殊岗位、12 点前岗位完整性、稀缺资质和跨工作日资质预留。截止保护不能通过制造空缺或抢占这些必需岗位来满足。
3. **保护阶段原顺序保持不变**：跨工作日恢复目标避让 → 次班截止保护 → 高疲劳连续保护 → 当天早晚负荷分散 → 上一班末班人员晚班轻岗 → 普通衔接偏好 → 在岗覆盖 → 滚动负荷 → 连续高负荷恢复 → 跨工作班负荷互补 → 工时/疲劳均衡 → 历史疲劳。此次只强化第二项的全局比较语义。
4. **截止保护的精确定义**：配置时间是“最晚界线”，不是必须恰好下班的时间。对所有通过前两级硬约束的完整方案，先比较上一班末班重点人员的最终工作结束时间；能让其更早结束的方案优先，即使普通人员的下班时间、工时均衡或历史疲劳因此略差。
5. **机会归属**：同一个下午岗位由受保护人员或未受保护人员承担都安全时，优先由受保护人员承担，使其获得更早结束机会。未受保护人员不能因普通公平目标抢走这类机会。
6. **多名受保护人员**：先保护截止时间更早者；截止相同则优先上一班结束更晚者；仍相同才比较实际可提前分钟数和稳定的结构化顺序。不得按姓名、数组下标或固定航班特例决定。
7. **冲突与回退**：截止保护与严格次班目标冲突时严格目标胜出；截止保护与岗位完整性、硬约束或特殊锁定冲突时硬约束胜出；没有完整安全方案时保留原安排并记录具体原因。截止保护不能把软约束静默升级为硬约束，也不能让后置轮岗再次破坏已获得的恢复结果。

这份裁决的关键是“提高截止保护在软目标中的优先级和全局性”，而不是把所有相关规则合并成一个权重或新增另一套互相矛盾的优先级。

### 不做范围

- 不改变“严格限制”与“优先避开”的配置含义。
- 不把截止时间变成强制空缺或强制停止所有后续岗位的硬约束。
- 不改变人员状态、资质、夜班能力、时间衔接、岗位唯一性、值班、`KE166`、人工锁定和岗位完整性等既有硬约束。
- 不用真实姓名、固定航班或固定岗位名称写入领域逻辑；测试使用匿名人员和配置化规则。
- 不修改页面之外的无关功能，不删除历史数据，不清理其他工作区文件。

### 设计

1. **规则层**：为跨工作日截止保护定义清晰的比较语义：受保护人员的“最终工作结束时间”按最小化目标处理；截止时间只用于判断是否已获得恢复，不作为固定目标时间。
2. **后置换位层**：在生成每个候选整体换位方案时，先比较受保护人员最终结束时间是否提前，以及提前幅度；只有受保护人员之间的截止保护结果等价后，才进入普通公平和负荷比较。
3. **机会归属**：如果同一个更早岗位可以由受保护人员或未受保护人员承担，且两种方案都通过所有硬约束，应优先选择受保护人员承担并因此提前结束后续工作。
4. **多人员排序**：先按最早截止时间升序，再按上一班结束时间降序，最后按可提前的分钟数和结构化稳定顺序比较；不得用姓名、数组下标或航班号特例决定结果。
5. **安全复核**：每次换位后重新执行现有资质、状态、夜班、时间、工时、岗位完整性、值班、锁定岗位、跨工作日严格目标和最终安全复核；任何失败都拒绝该方案并尝试下一方案。
6. **回退**：在截止时间之前没有完整安全方案时保留原安排，并记录“未找到更早完整安全下班方案”；在严格模式下，若严格次班目标没有安全完整方案，继续保持整次排班失败并保留原班表。
7. **证据**：重排成功后重建受影响岗位的自动决策证据；人工调整后不得继续展示失效的旧恢复结论。

### 实施任务

- [x] 在当前后置换位领域接缝补充匿名红灯测试，复现“未受保护人员先下班”的具体症状。
- [x] 明确并实现跨工作日截止保护的多人员目标比较器，保证提前下班机会优先归给上一班末班重点人员。
- [x] 将该比较器接入现有 `reviewLateShiftCutoff` / `optimizeReassignment` 方案排序，不绕过现有安全检查和参与人数上限。
- [x] 增加无解回退测试：锁定岗位时不得制造空缺，并保留安全班表。
- [x] 增加严格次班目标与截止保护同时存在的冲突测试，确认严格目标先满足，截止保护在安全方案中继续优先。
- [x] 核对 `spec.md` 现有合同并更新 `README.md`，明确“截止时间是最晚界线，存在更早安全机会时优先给上一班末班重点人员”。
- [x] 结构化决策沿用现有 selected/fallback 证据，成功与回退分别记录。
- [x] 完成近端测试、`npm.cmd run verify`、`git diff --check` 和 `git status --short`；不 commit、不 push、不部署。

### 验证计划

- 运行后置换位与恢复近端测试，至少覆盖：单人提前、两人竞争同一提前机会、多名受保护人员排序、无解回退、严格目标冲突和人工/后置换位安全复核。
- 用匿名等价场景比较修改前后的硬约束、岗位数量、空缺、受保护人员最终结束时间、普通人员结束时间、工时、疲劳、决策记录和反馈。
- 验收标准：在 A（上一班末班重点人员）与 B（未受保护人员）均可安全承担下午岗位时，A 必须先获得更早下班机会；只有没有完整安全方案时才允许 A 晚于截止时间，并显示可验证原因。
- 运行完整验证命令：`npm.cmd run verify`、`git diff --check`、`git status --short`、`rg -n "\\[DEBUG-" src tests scripts`。

### 收口

- 只有失败测试先红、实现后绿，且完整验证退出码为 0，才能把本需求标记为完成。
- 任何未验证的真实浏览器班表、真实工作簿场景或剩余性能风险必须单独列出，不得写成已完成。
- 交付中必须明确：实际改变的规则优先级、保持不变的硬约束、真实命令结果、未验证项和外部操作状态。
