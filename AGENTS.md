# autoschedule Agent 工作规约

本文件只保存需要每次常驻遵守的协作铁律。产品事实、排班规则和数据合同以 `spec.md` 为准；专项开发流程按命中的 `.agents/skills/*/SKILL.md` 执行。子目录存在更具体的 `AGENTS.md` 或 `AGENTS.override.md` 时，仅在其范围内优先。

## 权威来源与冲突升级

- `AGENTS.md` 规定协作边界，`spec.md` 规定当前产品事实、规则优先级、数据合同和验收边界；二者都必须遵守。
- 发现用户需求、规范、中央规则合同、代码执行顺序或测试断言冲突时，不自行选择覆盖方案；列出冲突、影响和可选方案，等待 owner 明确裁决。
- 当前实现只服务当前生效事实；除 `spec.md` 明确规定的迁移和导入合同外，不新增旧别名、兼容转发、历史适配层或过渡文件。

## 协作与授权边界

- 始终使用简体中文与 owner 交流，读取文本时使用正确编码。
- 面向 owner 的常规输出采用 `caveman` 风格（定义见 `.agents/skills/caveman-setup/SKILL.md`）；涉及测试结果、commit、验证清单，或 owner 明确要求详细说明时，改用精确、完整的表述，不使用 `caveman` 风格。
- 不回滚、覆盖或清理 owner 已有改动；只处理当前授权范围。
- commit、push、发布、部署、破坏性操作、权限扩大和付费操作，必须有 owner 明确授权。
- 不泄露密钥、令牌、个人数据或私有配置；不把推测、计划或未验证结果写成已完成事实。
- 禁止未经审查的机械替换、批量命名迁移和无边界重构。
- 排查排班相关 bug 时默认采用“人复现、机器查代码”：agent 不主动打开浏览器或点击界面复现；需要场景时，直接请 owner 按顺序提供日期与时间、航班与岗位、人员、实际现象和期望结果。需要中间数据时一次只请求一项，明确说明所需截图或文字内容，不让 owner 猜测；owner 说“我自己来”时立即停止相关操作并等待结果。只有 owner 明确说“你自己跑”时，agent 才可自行进行浏览器或真实入口复现。

## 需求变更前确认

- 会改变产品行为、排班规则、数据合同、页面交互或用户工作流的任务，写文件前先用业务例子确认：理解、修改范围、不变范围、边界、失败回退和验收效果。
- 需求明确且属于已授权的小改动，可直接执行；发现新冲突、歧义或需要扩大范围时停止写入并重新确认。
- 纯解释、只读排查、状态查询和不改变语义的协作文档检查，可不重复确认，但仍遵守本文件和 `spec.md`。

## 新功能自动闭环门禁

- owner 只需描述业务目标，不需要自行选择 Skill 或重复粘贴开发流程；主 agent 必须根据任务类型自动加载适用 Skill，并在交付前完成闭环。
- 所有新功能或行为变更默认按以下顺序执行：业务例子与验收合同 -> 唯一事实源 -> 完整执行路径/影响矩阵 -> 失败测试 -> 最小职责实现 -> 后置流程与最终不变量复核 -> 文档同步 -> 分层验证 -> 真实入口验收 -> 交付报告。
- 只要功能经过排班、求解、补缺、恢复、轮岗、覆盖、人工调整或最终展示中的任一阶段，就必须检查完整链路，不能只验证最初的候选或单个函数。
- 每条硬约束必须同时具备：唯一 domain 事实源、所有写入路径的消费点、最终 assignments 不变量检查，以及至少一个“先生成合法结果、再尝试被后置流程改坏”的反向回归测试。
- 自动化测试通过不等于需求完成；主 agent 还必须逐项对照 owner 需求、`spec.md`、不变规则、失败回退、页面结果和实际命令输出。未验证内容必须明确列出。
- 只有发现需求与 `spec.md`/中央规则合同冲突、优先级或失败回退无法合理确定、职责归属不清，或需要扩大授权时，才暂停写入并向 owner 提供冲突和选项；不得让 owner 负责发现流程遗漏。

### 硬规则传播与回归门禁

- 每条硬规则只能有一个事实 owner；其他模块只能消费该事实，不得复制一套等价判断、影子状态或局部默认值。
- 新增或修改硬规则前，必须先建立“规则传播矩阵”，逐项列出输入事实、候选池、求解模型、补缺/覆盖、恢复、轮岗/公平、人工调整边界、最终复核、保存/展示和测试；明确写出每个工序的消费点，或说明为什么不适用。
- 新增规则或修改排班逻辑前，必须先填写根目录的 `rule-propagation-matrix.md`，逐项列出该规则要经过哪些环节以及每个环节是否已接入；矩阵填写完成后才能开始修改，不允许跳过。
- 每条硬规则必须有反向测试：先生成合法结果，再通过真实后置写入路径故意改坏，系统必须拒绝提交、拒绝采用或保留原结果。
- 每修复一个已确认 bug，必须新增一个长期保留的回归测试；测试应保护用户可观察行为，不以内部函数调用次数或临时实现细节作为唯一断言。
- 每次交付必须分开报告：自动测试结果、真实页面验收结果、未验证项；需要人工判断时另列人工验收，不得用任一层的通过替代其他层。

## 事实与交接纪律

- 判断必须基于 owner 明确输入、当前仓库、命令输出或真实运行结果；区分已确认事实、未验证输入、推测、计划和已完成事实。
- `HANDOFF.md` 只是继续工作的短期快照，不是产品事实来源；“继续上次工作”时读取它并以当前规范、代码、测试和 Git 事实校正。
- 只有 owner 明确要求交接时才更新 `HANDOFF.md`；只记录继续工作所需的完成事实、验证、风险和下一步，不记录密钥或未经验证的推测。

### 历史纪律

- 规矩一：写历史。每次修复 bug、走过弯路、推翻旧方案后，必须更新 `history.md`。记录：问题、原因、尝试、结论、怎么防再犯。不允许只改代码不改历史。
  - 问题：
  - 原因：
  - 尝试：
  - 结论：
  - 怎么防再犯：
- 规矩二：读历史。只有 owner 明确要求“读一下 history”或“看看历史”时，才读取 `history.md`；其他情况无需自动读取。

## 最小职责边界

- `src/domain/` 只处理排班、轮值、时间、疲劳和反馈等业务事实，不读取 DOM 或浏览器存储。
- `src/infrastructure/` 只负责 localStorage、Excel、分享、Worker 和求解器等外部边界，不编排页面事件。
- `src/ui/` 只渲染只读投影并发出类型化命令；`src/app/` 通过唯一 Store、actions 和 controllers 提交状态与编排流程。
- `src/app.ts` 只组装入口；新增规则进入所属职责模块，不直接堆入 kernel 或入口文件。
- 同一业务事实只能有一个拥有者；找不到清晰归属、出现重复事实源或跨模块耦合时，先做只读架构审查。

## Skill 与专项流程

- 任务命中 Skill description 时，先读取对应 `SKILL.md`；不要把专项流程复制回本文件。
- 修改实现、测试、`spec.md`/`README.md`、数据模板、构建或运行配置：使用 `autoschedule-dev`。
- 需要计划：仅 owner 明确要求时使用 `plan`；排查未知故障使用 `investigate-first`；窄范围修复使用 `surgical-patch`；行为保持重构使用 `safe-refactor`；只做验收证明使用 `verify-and-stop`。
- owner 点名架构改进或任务涉及模块耦合、性能模型、职责拆分和测试 seam 时，使用 `improve-codebase-architecture` 与 `codebase-design`，不得借重构改变产品规则。
- Caveman、cavecrew 及其他专用 Skill 只在其触发条件满足时加载和执行。

## 模型分工与验收

- 需求分析、业务规则确认、架构设计、测试设计、代码实现、测试失败诊断和修复，默认使用 `gpt-5.6-sol`。
- 定向测试、类型检查、构建、性能测试、Worker/WASM 烟雾测试和完整门禁，默认委派给 `gpt-5.6-terra` 执行。
- Terra 验证代理只执行命令并报告证据，不修改实现、测试、规范或配置。
- 验证失败时，Sol 负责判断失败原因并完成修复，Terra 负责复跑相关验证。
- 最终验收由 Sol 对照 owner 需求、`spec.md`、代码 diff、测试结果和剩余风险完成；门禁通过不自动等于需求完成。
- 排班规则、硬约束、疲劳恢复、Worker/WASM、求解器、Excel、localStorage、共享状态和数据迁移等高风险内容不得降级给 Terra 审查。
- 模型不可用时必须明确报告，不得静默替换模型。
- 本节是协作政策，不是权限隔离；若需要强制禁止验证代理写入，必须由运行器、hooks 或命令白名单提供技术约束。

## 验证与交付

- 按风险执行 `autoschedule-dev` 定义的验证层级；先验证直接受影响职责，再按影响范围扩展。动态导入、Worker/WASM、Excel/localStorage、共享状态迁移和隐式数据合同必须显式检查。
- 未运行、失败、不可用或有剩余风险的验证，必须如实报告；不得用“测试全绿”替代未授权影响检查。
- 任何实现、测试、产品事实、数据模板、构建或运行配置改动，必须先完成 `npm.cmd run verify` 且退出码为 0，才允许 commit 或 push。
- 纯协作文档只需 `git diff --check` 和人工核对引用；不因文档变更运行应用测试。
- 交付说明完成事实、实际命令结果、未验证项、剩余风险和外部操作状态；不交半成品。
