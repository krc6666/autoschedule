# autoschedule Agent 工作规约

本文件是本仓库根级 AI 协作规范。产品事实看 `spec.md`；只有 owner 明确要求计划时才维护 `plan.md`。子目录存在更具体的 `AGENTS.md` 或 `AGENTS.override.md` 时，仅在其范围内优先。

## 基础约束

- 始终使用简体中文和项目 owner 交流，除非 owner 明确要求其他语言。
- 阅读文本时使用正确编码；显示异常时先验证编码，不误判文件损坏。
- 不回滚 owner 已有改动，只处理当前任务相关文件。
- commit、push、发布和部署只在 owner 明确授权时执行。
- 禁止机械替换、无审查批量替换和大小写不受控的命名迁移。
- 除 `spec.md` 明确规定的现行数据迁移和导入合同外，禁止新增旧别名、兼容转发、旧语义包装、历史适配层和过渡文件。
- 当前实现只服务当前生效事实。

## 不可突破核心规则

- AGENTS.md 是仓库级 AI 协作规范，spec.md 是产品需求边界。
- 【严格禁止】在没有获得用户明确书面许可的情况下，自行修改、删减、弱化、曲解 AGENTS.md 和 spec.md 内任何条款。
- 【严格禁止】实现功能时，无视文档里已定义的约束；任何代码实现必须同时满足两份文档全部要求。
- 如果新需求和现有 AGENTS.md / spec.md 存在冲突：
  - 禁止自行决定“以新需求覆盖旧规则”
  - 第一时间列出冲突条目，主动向用户上报冲突，等待用户二选一：
    - 方案A：调整新需求，兼容原有规则
    - 方案B：授权你正式修订 AGENTS.md / spec.md，更新文档后再开发
- 开始处理任务前先核对适用的 `AGENTS.md`；涉及产品行为、排班规则、数据合同、架构或验收标准时，再读取并核对 `spec.md` 的相关章节。排班规则优先级变化必须完整核对“合理排班宏观原则”和“规则合同与冲突裁决”。

## 需求确认机制

- 对功能开发、规则调整、页面交互、数据结构、导入导出或会改变现有行为的需求，禁止仅凭 agent 自行推断后直接修改。
- 在上述会改变现有行为的写入、实现或配置变更前，必须先向 owner 简要确认：
  - 对需求的具体理解，必要时用一个贴近业务的例子说明预期结果；
  - 本次会修改的范围，以及明确不会改变的既有行为；
  - 关键业务边界、异常场景和可验证的完成效果。
- 需求存在多种合理实现、信息不足、术语可能有歧义，或可能影响已有规则时，必须主动提出针对性问题，等待 owner 明确选择；不得选择“看起来最合理”的方案自行执行。
- 多个待确认问题存在前后依赖、前一个答案可能改变后一个问题，或会影响规则优先级与冲突裁决时，必须逐个询问。相互独立的问题可以一次提出，但每次最多 4 个；必须编号并给出推荐答案及简短理由。不确定问题是否相互独立时，按存在依赖处理并逐个询问。
- 只有在 owner 明确确认上述理解、明确授权“直接执行”，或请求本身已经给出无歧义的小改动范围后，才可开始修改文件、配置或数据。确认只授权已经说明的范围；后续新增范围、改变既有规则或发现冲突时，必须重新确认。
- 纯粹的解释、只读排查、状态查询，以及 owner 已明确提出且不改变业务规则、产品合同或数据语义的无歧义小改动，可不额外重复确认；但仍须遵守 AGENTS.md、spec.md 和授权边界。

## 排班规则变更纪律

- 任何新增或修改排班规则必须先对照 `spec.md` 的“合理排班宏观原则”检查岗位完整性、相邻工作班动态恢复、重点岗位轮换、当天工作形态和月度公平；发现方向冲突或优先级不明确时必须先列出冲突并等待 owner 裁决，不得用局部规则静默覆盖宏观原则。
- 排班规则、候选排序、岗位锁定、岗位完整性、疲劳保护、轮岗或反馈发生变化前，必须明确对应的规则 ID、所属执行阶段、前置规则、后置规则、允许突破条件和回退行为。
- `spec.md` 的“规则合同与冲突裁决”是业务优先级的唯一规范来源；README、规则页面和代码注释只能引用或展示该合同，不得独立发明另一套优先级。
- 新规则不得直接堆入 `src/domain/kernel/scheduling-kernel.ts` 或其他入口文件。应进入所属职责模块；尚无合适边界时，先完成不改变行为的职责拆分，再单独开发规则。
- 行为保持型重构与规则行为修改必须分开交付。重构前先建立语义等价测试，比较岗位、人员、空缺、工时、疲劳、系统说明、决策记录和反馈；发现差异立即停止并上报。
- 规则反馈必须来自最终排班事实或结构化决策记录。人工调整导致原决策失效时，不得继续把旧记录展示为当前规则已经执行。
- 发现规范、中央规则合同、真实执行顺序或测试断言不一致时，不得自行选择其中一套覆盖其他内容；先列出差异及影响，等待 owner 确认。

## 事实纪律

- 判断必须基于 owner 明确输入、当前仓库、命令输出或真实运行结果。
- 区分已确认事实、owner 提供但未验证的事实、推测、计划事项和已完成并验证事项。
- 任何未实现或未验证内容，在任何载体中都不能写成已经实现或已经通过。
- 当前任务已经存在有效 `plan.md` 时，新事实推翻计划应先更新计划再继续；不存在当前计划时不得因此自行创建。

## 文件职责

- `AGENTS.md`：工作规则和协作纪律。
- `spec.md`：当前产品事实、边界和验收标准。
- `plan.md`：当前中大型任务的单文件执行合同。
- `README.md`：面向用户和新读者的使用入口。

## 代码职责

- `src/domain/` 只处理排班、轮值、时间、疲劳和反馈等业务规则，不读取 DOM、不写浏览器存储。
- `src/infrastructure/` 负责浏览器存储、Excel 和分享文件等外部边界，不承载页面事件编排。
- `src/ui/` 只负责页面 HTML 渲染和展示数据，不直接修改应用状态。
- `src/app/` 通过唯一 Store、应用动作和专用控制器提交状态变化；应用动作不读取 DOM，不负责确认或事件编排。
- `src/app/configuration-actions.ts`、`policy-actions.ts`、`schedule-actions.ts`、`history-actions.ts` 和 `workbook-actions.ts` 分别负责各自领域的状态变更。
- `src/app/controllers/` 负责类型化 UI 命令编排和确认提示；`application-coordinator.ts` 负责应用生命周期、视图切换和命令分发。
- `src/app.ts` 只作为组合入口挂载根组件并连接 Store 与 coordinator；不得承载命令特例或业务规则。

## Skills

- 项目级 skill 放在 `.agents/skills/`。
- 任务命中 skill 的 `description` 时，必须先读取对应 `SKILL.md` 再行动。
- owner 明确要求计划时使用 `.agents/skills/plan/SKILL.md`，并维护根目录 `plan.md`。
- 修改实现、测试、`spec.md` / `README.md`、数据模板、构建或运行配置时使用 `.agents/skills/autoschedule-dev/SKILL.md`；纯协作规范和无产品行为变化的文字修正不强制进入完整开发门禁。
- owner 点名架构改进，或任务涉及性能模型膨胀、模块耦合、职责拆分、测试接口和深模块设计时，允许并应当使用 `.agents/skills/improve-codebase-architecture/SKILL.md`；它必须与 `autoschedule-dev` 和 `codebase-design` 的约束共同执行，不得借架构重构改变 `spec.md` 业务规则、规则优先级或验收标准。
- 业务专属 skill 只在真实业务需要时新增，不把临时任务或产品事实硬编码进通用 skill。

## 计划规则

- owner 明确要求计划时才创建或维护 `plan.md`；owner 明确免除时不得擅自创建。
- 计划按需求、事实、失败测试、目标、不做范围、设计、任务、验证、收口组织。
- checklist 随进度更新，不用口头说明代替计划更新。

## 工程规则

- 优先沿用仓库已经成立的技术栈、命名和模块边界。
- 单一职责按变化原因判断；文件超过 300 行时审查职责，但不机械拆分。
- 状态、规则、数据、展示、外部接线和错误处理变化原因不同时，拆清边界。
- 测试保护真实规则、边界和核心路径，不保护废弃行为。
- 文档、实现、测试和配置在同一次交付中保持一致。

## 验证

- 测试策略采用测试金字塔、左移测试、风险测试和变更影响分析的组合；依据、仓库现状与来源见 `docs/research/development-testing-strategy.md`。不得把“每次都跑全量”或“只跑改动文件”当作统一答案。
- 验证分为不同反馈周期，不得在开发过程中反复执行后续层级：
  - L0 编辑反馈：行为变更先运行能稳定失败的测试，再运行本职责及直接消费者的最小测试集合；不运行性能测试和生产构建。
  - L1 本地变更集：显式相关测试 + Vitest `related`/`--changed` 候选集合 + `npm.cmd run typecheck`，再按风险补充真实入口检查。`related`/`--changed` 只能辅助选测，不是完整影响证明。
  - L2 完整功能验证：`npm.cmd run typecheck`、全部普通测试和 `npm.cmd run build`。用于高影响代码的本地交付，以及未来所有 PR/合并的必需检查；当前尚未建立 PR 必需检查，不得写成已经由 CI 承担。
  - L3 性能验证：仅在改动求解、Worker、候选搜索、大数据路径、性能阈值、依赖或运行时性能时运行 `npm.cmd run test:performance`；固定环境的定时基线尚未建立，不得把单次本机墙钟结果写成可靠基线。
  - L4 发布完整验证：发布、部署、owner 明确要求完整验收，或当前仓库 commit/push 门槛要求时运行一次 `npm.cmd run verify`。
- 变更风险而非文件数量决定是否扩大范围。动态导入、Worker/WASM、读取源码文本的架构测试、Excel/localStorage、共享 `AppState`、迁移和跨模块数据合同可能不会被静态依赖图选中，必须显式补测；具体矩阵见 `autoschedule-dev` skill。
- 纯协作文档只运行 `git diff --check` 并人工核对规则引用，不运行应用测试。普通局部代码改动完成 L0/L1 即可收尾；高影响代码按矩阵补 L2，性能相关改动再补 L3，不因“习惯”升级到 L4。
- 核心路径按实际影响范围检查；涉及排班、Excel、存储或页面交互时验证对应真实入口。视觉改动应检查目标视口和交互状态，不能只以单元测试代替页面事实。
- 无法运行必要验证时，说明具体命令、原因和剩余风险。

## 推送门槛

- 任何实现、测试、产品事实、数据模板、构建或运行配置改动，必须先完成 `npm.cmd run verify`，退出码为 0 后才允许 commit 和 push；纯协作文档按“验证”一节执行。
- 任一验证失败、未运行或存在未说明的剩余风险时，不得推送到远端。
- push 只在 owner 明确授权后执行；授权不替代完整验证门槛。

## 安全与交付

- 不泄露密钥、令牌、个人数据或私有配置。
- 破坏性操作、生产变更、权限扩大和付费操作需要明确授权。
- 交付必须说明完成事实、实际验证、未验证项和剩余风险，不交半成品。
