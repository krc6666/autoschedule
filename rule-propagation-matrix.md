# 规则传播矩阵

## 2026-09-23：分队长补差负荷保护分级与相对疲劳优选

### 规则基本信息

- 规则名：分队长补差负荷黄灯与末班段相对疲劳优选
- 规则 ID：`team-leader-gap-fill-load-relief`
- 阶段：只作用于分队长补差的局部候选、安全检查、方案优选、预览和确认复核；不进入全天自动排班、普通轮岗或人工换岗。
- 唯一事实源：高负荷与滚动负荷继续复用现有 `hasHighLoadTransition`、`rollingLoadCost`；上一工作班休息需求继续复用 `previousWorkdayLoadForStaff`，固定比较顺序为“疲劳点 -> 最晚结束 -> 工时 -> 末班重点数量”。不新增综合分或第二套疲劳统计。
- 分级：资质、真实时间冲突、最小衔接、工时、保护岗、岗位完整性和不并行督导仍硬拒绝；跨工作日资质预留与超过 15 格只在本入口黄灯放行。高负荷疲劳、滚动负荷、跨工作日恢复及无法满足相对疲劳偏好继续只在本入口黄灯放行。
- 当前末班段范围：只比较本次局部可移动岗位范围内、补差后实际承担当前末班段岗位的人员；保留在该局部岗位上的人员仍参与公平比较，但仅在早班出现的人员不得带入。

### 传播矩阵

| 环节                                   | 要检查吗 | 消费点                                                                                                       | 失败 / 回退                                                                    | 回归证据                                |
| -------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------ | --------------------------------------- |
| 输入事实                               | 是       | 复用补差日期、当前 assignments、上一工作班负荷、岗位疲劳/时间/重点事实                                       | 缺少可核对事实时不猜综合分                                                     | 字典序事实测试                          |
| 候选池                                 | 是       | 通用重排安全检查新增默认关闭的负荷让步参数；仅补差 planner 显式开启                                          | 其他调用方继续把两类负荷保护作为硬拒绝                                         | 默认关闭与补差开启测试                  |
| 局部求解 / 方案优选                    | 是       | 在同一批空缺、同一可行域内，先寻找满足相对疲劳顺序的完整方案；有多个完整方案时优先更需要休息者负担更轻的方案 | 找不到改善方案时回退到完整安全方案，不因此判整批无解                           | 可改善时选轻方案、不可改善时黄灯        |
| 高负荷 / 滚动负荷安全检查              | 是       | 生成方案和确认复核均允许本入口的负荷让步，同时从最终计划重新计算具体触发事实                                 | 只生成黄灯，不删除其他硬拒绝原因                                               | 两类保护分别 ready + 黄灯 + 可落地      |
| 当前保护岗 / 完整性                    | 是       | 继续复用岗位保护 owner、全部空缺补齐和不新增空缺守卫；15 格作为建议上限记录黄灯                              | 任一硬约束失败整批拒绝，原班表不变；超过 15 格但其余硬约束通过时允许预览和确认 | 保护岗/完整性反向确认，超 15 格黄灯测试 |
| 页面预览                               | 是       | 可采用方案展示完整换人链；黄灯至少写人名、目标航班/岗位/时间和保护类型；失败候选写具体人岗时间硬原因         | 不展示内部目标名、`infeasible` 或笼统“负荷保护”                                | UI 文案测试                             |
| 确认提交                               | 是       | 使用同一补差专属让步参数复核，避免预览可出但确认被通用负荷硬规则拒绝                                         | 任何真正硬约束失败仍整批拒绝                                                   | ready 后 apply 测试                     |
| 全天排班 / 普通轮岗 / 人工换岗         | 否       | 不传补差专属让步参数                                                                                         | 保持现有高负荷与滚动负荷规则                                                   | 既有安全测试                            |
| 保存 / Excel / localStorage / 历史统计 | 否       | 不新增配置或持久化字段；黄灯只属于本次预览                                                                   | 沿用现有补差落地与归档合同                                                     | 影响核对                                |

### 验收清单

- [x] 仅命中高负荷疲劳保护时仍返回 `ready`，黄灯含人名、航班、岗位、时间，确认可整批落地。
- [x] 仅命中滚动负荷保护时仍返回 `ready`，黄灯含人名、航班、岗位、时间，确认可整批落地。
- [x] 多个完整方案可选时，更需要休息者按既定四层顺序优先得到更早、更低疲劳、非重点的末班段负担。
- [x] 无法改善相对疲劳时仍返回完整方案，黄灯明确比较双方及无法继续调整的硬边界。
- [x] 未参与本次变化或只在早班出现的人员不进入相对疲劳比较。
- [x] 通用重排和全天排班未显式开启时仍把高负荷、滚动负荷作为原有硬保护。
- [x] 仅补差路径可在严格预留方案无解时让步跨工作日资质预留，并以目标、人名、末班结束界线后消耗和人数变化写黄灯。
- [x] 仅补差路径超过 15 格但其余硬约束通过时仍可预览和确认，并写明实际改变岗位数与建议上限。

## 2026-09-23：分队长补差岗位保护范围

### 规则基本信息

- 规则名：分队长补差岗位保护范围
- 规则 ID：`team-leader-gap-fill-position-protection`
- 阶段：只作用于普通排班完成后的分队长局部补差候选池、整体方案安全复核和确认提交；不进入全天自动排班。
- 唯一事实源：`ScheduleSettings.teamLeaderGapFillPositionPolicies[]`，每项以稳定的“航班号 + 岗位名”记录该具体岗位是否允许参与补差换人。岗位分类和普通重点属性只提供未配置岗位的默认值，不作为第二份人工设置。
- 默认值：普通非重点常规岗位和引导岗位允许移动；普通重点岗位、普通分流岗位及机动督导默认保护。排班员可在规则页按当前岗位分类批量设置，也可展开修改单个具体岗位。
- 固定保护：值班人员当前岗位、`KE166`、人工岗位、行政支援岗位或行政支援人员、已补差落地岗位、督导兼任关联格和特殊布局关联格始终保护，页面只展示原因，不允许配置关闭。
- 失败回退：设置变化只标记当前班表需重新排班；补差预览或确认命中固定保护、资格、真实时间冲突、岗位完整性或不并行督导时整批拒绝，原班表不变。超过 15 格不再单独拒绝，仅作为黄灯建议上限。

### 传播矩阵

| 环节                   | 要检查吗 | 唯一消费点/计划                                                                                  | 失败/回退                                                    | 回归证据                               |
| ---------------------- | -------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------ | -------------------------------------- |
| 输入事实 / 默认值      | 是       | 结构化设置统一清洗“航班号 + 岗位名 + 是否允许移动”；未配置岗位由补差保护 domain owner 计算默认值 | 非法或空引用丢弃，重复引用归并                               | 默认、清洗、旧状态测试                 |
| 规则页编辑             | 是       | 从当前 `positionRules` 分组展示；分类按钮一次修改当前组全部可配置岗位，单岗开关写同一设置        | 固定保护岗位禁用并展示具体原因；修改后班表标记过期           | 页面投影、批量与单岗命令测试           |
| 普通候选池             | 是       | `team-leader-gap-fill` 只把统一保护判断为可移动的已排岗位加入局部模型                            | 需要移动保护岗位时不入模型，并保留具体人岗与保护类型         | 普通重点默认拒绝、手动放开后可成链     |
| 局部求解               | 是       | 继续复用现有重排求解器、资格、冲突和完整性；15 格只作为补差专属黄灯阈值，不进入其他 review       | 无完整方案整批无解                                           | 连环换人与硬约束既有回归，超 15 格黄灯 |
| 最终安全复核 / 确认    | 是       | 预览确认时重新消费同一保护判断，防止设置或班表变化后绕过                                         | 任一移动格现为保护状态则整批拒绝，原班表不变                 | 先生成预览、再改保护范围的反向测试     |
| 全天排班 / 恢复 / 轮岗 | 否       | 本配置仅供分队长补差路径消费                                                                     | 不改变全天 HiGHS、恢复或轮岗                                 | 既有排班回归                           |
| localStorage / 迁移    | 是       | 设置随共享 `ScheduleSettings` 保存；旧状态缺字段时使用空例外集合                                 | 损坏项清洗，不猜测岗位                                       | 新旧状态往返测试                       |
| Excel                  | 是       | 独立“分队长补差保护范围”工作表导入导出稳定业务键                                                 | 缺表保留当前设置；存在空表表示清空人工例外；非法行整表不覆盖 | 配置工作簿往返与非法行测试             |
| 岗位改名 / 删除        | 是       | 配置动作同步“航班号 + 岗位名”引用；删除岗位清除对应引用                                          | 不留下失效保护引用                                           | 改名、删除测试                         |
| 归档 / 统计            | 否       | 配置只决定能否移动，不改变补差来源、工时、疲劳和轮换统计                                         | 沿用既有补差统计合同                                         | 既有统计回归                           |
| 展示 / 反馈            | 是       | 无解理由使用人员、航班、岗位、时间和“固定保护/规则页保护”类型                                    | 不显示内部 ID 或求解器术语                                   | 具体文案测试                           |

### 明确不变

- 真实时间冲突、无资质、产生新空缺、所选空缺未全部补齐和不并行督导不能通过规则页关闭；15 格是补差建议上限，不是可配置的硬保护。
- 固定保护不接受单岗例外；普通重点岗位只是默认保护，排班员明确放开后可参与补差换人。
- 恢复与相对疲劳仍按既定黄灯口径处理，不由本配置启停。

## 2026-09-22：分队长局部补差

### 规则基本信息

- 规则名：分队长局部补差
- 规则 ID：`team-leader-gap-fill`
- 阶段：普通自动排班完成后的人工确认式局部补缺；不属于全天自动求解阶段。
- 唯一事实源：最终 `Assignment` 上的结构化补差来源。只有经本入口确认写入的分队长岗位带该来源；备注文字、人员身份和岗位名称均不得反推补差事实。
- 参与边界：用户一次选择一名分队长和多个当前空缺；所有空缺进入同一个整体模型，只允许在所选空缺附近形成开放式腾挪或闭环换人。整体方案不按参与人数限额；改变 15 个岗位是建议上限，超过时允许预览和确认并生成黄灯。
- 失败回退：先生成只读预览，不修改当前班表；确认时重新校验当前状态，任一步失败则整批拒绝，原班表不变。
- 统计边界：补差所得督导正常计算工时、疲劳、冲突和工时上限，但不计入督导轮换次数；其他分队长安排仍正常计数。

### 2026-09-22 补差约束分级与诊断扩展

- 硬约束：资质、真实时间冲突、岗位完整性（所选空缺全部补齐且不产生新空缺）、当前受保护岗位，以及全局最小/结构化严格岗位衔接继续整批拒绝。
- 可让步黄灯：仅在分队长补差路径中，跨工作日资质预留、超过 15 个岗位建议上限、跨工作日末班重点恢复/次班截止让步，以及当前末班段内相对疲劳偏好未满足，允许进入预览和一次性确认；不改变全天自动排班、普通轮岗或普通恢复重排规则。
- 相对疲劳唯一事实：复用最近已归档工作班的 `previousWorkdayLoadForStaff`、末班重点事实和当前岗位 `workHours`/`fatiguePoints`/结束时间；比较顺序固定为“上一班疲劳点 → 最晚结束 → 工时 → 末班重点数量”，不新增综合分。
- 诊断合同：每条可采用或被拒绝的候选链必须保留人员、来源航班/岗位/时间、目标航班/岗位/时间、硬拒绝或黄灯分类及具体保护类型；求解器状态和内部目标名只能留在开发诊断，不进入页面文案。

### 传播矩阵

| 环节                                 | 要检查吗 | 唯一消费点/计划                                                                                                                                                                                                             | 失败/回退                                                                                                 | 回归证据                                                                       |
| ------------------------------------ | -------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------ |
| 输入事实                             | 是       | 分队长身份继续由 `Staff.teamLeader` 提供；补差事实只由最终 `Assignment` 的结构化来源提供                                                                                                                                    | 非分队长、非空缺、重复空缺或过期选择拒绝                                                                  | 输入校验测试                                                                   |
| 普通候选池                           | 是       | 自动排班候选统一排除分队长；分队长允许整天无岗                                                                                                                                                                              | 普通人不足时保留空缺，不自动启用分队长                                                                    | 自动排班不使用分队长测试                                                       |
| HiGHS 初排模型                       | 是       | 仅消费已经排除分队长的普通候选；不新增大权重或姓名特例                                                                                                                                                                      | 无普通候选时留下可见空缺                                                                                  | 候选与最终班表测试                                                             |
| 并行督导补缺                         | 是       | 停用现有自动分队长并行督导补缺阶段及其配置入口                                                                                                                                                                              | 不再生成一人同时承担重叠督导的例外                                                                        | 后置流程不得恢复并行督导测试                                                   |
| 局部方案生成                         | 是       | 新 domain planner 复用统一 `optimizeReassignment`、候选资格与完整方案安全复核；把全部所选空缺作为同一个模型的硬要求，只围绕所选空缺生成整体方案；先严格检查资质预留，只有该障碍导致无解时才以补差专属开关重算               | 任一空缺不能补齐或硬约束失败时整批无解，不改 assignments；资质预留让步或超过 15 格时生成黄灯预览          | 多空缺整体求解、资质预留严格/让步、超 15 格黄灯与硬约束回归测试                |
| 引导                                 | 是       | 继续消费“同航班已上岗人员”的被动岗位语义；引导不作为独立可填实岗                                                                                                                                                            | 无合法来源则保持空缺并说明                                                                                | 引导来源回归测试                                                               |
| 重点岗位 / 锁定                      | 是       | 已排好的督导、一号、申报、送资料等重点岗位，以及 `KE166` 机动督导/兼任组、值班锁定、人工/临时岗位均不可移动；所选空缺自身可为重点岗位                                                                                       | 方案需要移动任一受保护岗位时判无解                                                                        | 各类保护岗位不可移动测试                                                       |
| 行政支援                             | 是       | 行政支援人员与行政支援岗位不进入补差可移动集合                                                                                                                                                                              | 需要行政支援参与时判无解                                                                                  | 行政支援隔离测试                                                               |
| 工时 / 冲突 / 疲劳 / 恢复 / 资质预留 | 是       | 统一资格判断和完整方案安全复核按普通实岗检查分队长及所有被移动人员；跨工作日恢复、跨工作日资质预留、高负荷、滚动负荷和相对疲劳偏好只在本路径分类为黄灯；资质预留使用独立 `allowCrossWorkdayReservationRegression`，默认关闭 | 资质、真实重叠、全局/结构化衔接、工时上限等硬约束整批拒绝；恢复、资质预留、负荷或相对疲劳偏好只能黄灯放行 | 预留 1→0 黄灯、恢复黄灯、重叠硬拒绝、保护岗硬拒绝、相对疲劳可改善/不可改善测试 |
| 普通重点轮换                         | 是       | 局部补差不得移动已排好的重点岗位；普通非重点岗位继续经过统一安全复核，不主动改善或恶化轮换                                                                                                                                  | 无法保持既有重点岗位时判无解                                                                              | 原重点岗位不动测试                                                             |
| 人工确认 / 原子提交                  | 是       | app/controller 保存预览基线并在确认时重算或校验；一次命令整批替换 assignments                                                                                                                                               | 班表已变化、方案过期或提交校验失败时整批不写入                                                            | 预览不改状态、过期确认回退测试                                                 |
| 最终 assignments 守卫                | 是       | 最终守卫复核岗位完整性、资格、时间、工时、疲劳、锁定和非并行督导                                                                                                                                                            | 后置写入试图破坏时拒绝采用并保留原班表                                                                    | 先合法再故意改坏的反向测试                                                     |
| Store / UI                           | 是       | 排班页提供分队长选择、空缺勾选、生成预览、确认/取消；UI 只发类型化命令并展示整体换人链、黄灯与逐候选硬原因                                                                                                                  | 无解或过期只显示具体人岗时间/保护类型；不显示 `infeasible`、目标名或“双向岗位资质”等内部术语              | 命令、预览、具体诊断文案测试                                                   |
| 历史归档 / 统计                      | 是       | 归档保留补差来源；督导轮换统计统一排除“补差来源 + 督导”，工时和疲劳统计不排除                                                                                                                                               | 来源缺失时按普通安排计数，不凭人员身份豁免                                                                | 归档后轮换不计、工时仍计测试                                                   |
| Excel / localStorage                 | 是       | 机器可读班表与状态往返保留可选补差来源；旧记录缺字段按普通安排处理                                                                                                                                                          | 非法来源拒绝或清洗，不猜测旧数据                                                                          | 新旧数据往返测试                                                               |
| 展示 / 导出                          | 是       | 补差岗位按普通实岗显示和导出，可附结构化来源对应的说明                                                                                                                                                                      | 不因轮换豁免隐藏岗位或工时                                                                                | 页面与导出测试                                                                 |

### 明确不做

- 不重跑全天自动排班，不自动提交，不允许补一半；15 个岗位是补差建议上限，超过后仍可在其他硬约束满足时生成方案并黄灯提示，不改变其他路径的限制。
- 不以综合分、大权重、人员姓名或备注关键字决定补差。
- 不移动任何已排好的重点岗位或特殊锁定；不把局部补差当作改善重点岗位轮换的机会。
- 不保留分队长并行督导作为兜底。

## 2026-09-22：普通重点岗位集合与普通重点次数账本

### 规则基本信息

- 规则名：普通重点岗位集合与普通重点轮换次数
- 规则 ID：`position-frequency`、`position-rotation`（复用现有一般同岗轮换阶段，不新增末班规则）
- 事实 owner（唯一）：普通重点集合配置与普通重点次数修正账本；岗位是否为普通重点不得再由五类关键词函数单独决定。
- 普通重点集合项：`航空公司 + 规范岗位`。规范岗位沿用现有 `positionRotationGroupKey` 的航司提取和规范岗位语义，但集合匹配只消费集合事实。
- 普通重点次数唯一维度：`自然月 + 人员 + 航空公司 + 规范岗位`。历史/当前排班实际次数与人工修正合并，最低为 0。
- 迁移边界：上线时可把当前五类关键词命中的普通常规岗位规范键预填入集合；迁移完成后，集合外关键词岗位不再是普通重点。
- 与末班重点隔离：末班重点继续使用现有“月份 + 人员 + 航班 + 四类类别”账本、统计页和 Excel，不读取普通重点集合或普通重点修正。

### 传播矩阵

| 环节                    | 要检查吗 | 唯一消费点/计划                                                                 | 失败/回退                                                               | 回归证据                          |
| ----------------------- | -------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------- | --------------------------------- |
| 输入事实 / 配置         | 是       | 普通重点集合 domain owner；规范化键由现有航司/岗位规范化函数提供                | 非法或重复集合项拒绝/去重；未配置集合时按空集合，不回退到关键词影子判断 | 集合规范化与默认迁移测试          |
| 普通次数账本            | 是       | `月份 + staffId + airline + normalizedPosition` 修正记录                        | 非法人员/键/月份/负最终次数拒绝整批；手改后标记当前班表需重新排班       | 加减、清零、导入导出与 stale 测试 |
| 候选池 / 一般频率       | 是       | `positionFrequencyProfileForRule/Assignment` 消费普通重点集合匹配和普通次数账本 | 无安全低频候选时保留岗位完整并报警                                      | CA123/CA456 同岗跨航班计数测试    |
| 求解模型                | 是       | 继续消费一般岗位频率 profile；不接入末班四类目标                                | 普通重点目标失败不制造空缺                                              | 初排低次数人员优先测试            |
| 连续轮岗                | 是       | `position-rotation-review` 与连续轮岗计划复用同一普通重点判定                   | 无安全方案保留原结果并反馈                                              | 合法结果被后置改坏时拒绝/回退     |
| 补缺 / 覆盖 / 恢复      | 是       | 沿现有重排安全与最终 assignments 复核消费普通重点 profile                       | 不因普通重点主动制造岗位空缺；安全约束优先                              | 后置写入反向回归                  |
| 末班重点统计 / 账本     | 是       | 不适用，继续消费现有 `latePriority*` 事实                                       | 普通重点配置或修正不得改变末班统计                                      | 末班既有测试保持不变              |
| UI 集合维护             | 是       | 规则/配置页只编辑集合事实，保存后标记班表待重排                                 | 取消或非法编辑不改状态                                                  | 集合增删改与 stale 测试           |
| UI 普通重点统计         | 是       | 新增普通重点统计投影消费普通账本                                                | 不并入末班统计页                                                        | 页面投影及手工加减测试            |
| localStorage / 迁移     | 是       | 状态版本新增普通集合与普通修正字段；旧状态按迁移合同预填集合、保留旧末班账本    | 缺字段补空/迁移默认，禁止静默丢末班数                                   | 旧状态迁移往返测试                |
| Excel 往返              | 是       | 普通重点集合与普通次数使用独立工作表/专用入口；末班工作表保持原合同             | 缺表保留原值；非法表整批拒绝                                            | 二进制往返、非法行和隔离测试      |
| 最终 assignments / 守卫 | 是       | 从最终 assignments 重算普通重点 profile；后置或人工写入不得绕过公共比较/复核    | 发现普通重点频率恶化时拒绝采用或保留原结果，按现有软规则反馈            | 先合法再后置改坏的反向测试        |

### 明确不适用

- 普通重点集合不改变末班航班范围、末班结束界线、四类差值、末班恢复或末班次数 Excel。
- 普通重点人工修正不生成历史记录，不进入末班账本，也不改变未进集合岗位的高/低疲劳分级。

## 2026-09-20：引导取整条岗位链最下方人员、全部空缺标红

### 规则与边界

- 引导来源唯一事实：同一航班中已经上岗、人员状态正常且人员类型为常规的人员；排除引导岗位自身，但不按其来源岗位分类排除行政支援岗位。
- 来源顺序：行政支援岗位上实际填写的常规人员为第一层；没有这类来源时，再按排班页该航班整条岗位显示顺序从下向上取人；每层多个来源按页面从下向上，多个引导依次使用不同人员。
- 既有开关：继续消费 `adminSupportEnabled` 控制行政支援岗位是否进入当前班表，不新增引导开关；关闭时没有行政支援岗位来源，开启且该岗位由常规人员填充时自动进入第一优先层。
- 空缺展示：排班表内所有没有人员的岗位格和人员格统一使用红色空缺状态；半休、KE166 和人工越权仍保留各自文字或图标，不能把红色覆盖成普通白色或黄色。
- 明确不变：引导复用仍不增加工时、疲劳和历史；不改变岗位资格、求解、空缺产生条件或人工调整权限。

| 环节                        | 消费点                                                           | 失败 / 回退                          | 回归保护                             |
| --------------------------- | ---------------------------------------------------------------- | ------------------------------------ | ------------------------------------ |
| 规则页 / 开关               | 复用现有行政支援模式开关                                         | 不新增配置字段                       | 既有开关测试                         |
| 初排被动岗位                | `schedule-passive-position.ts` 消费统一引导来源与排序事实        | 没有来源则引导留空                   | 行政岗位常规人员优先，其次最下方来源 |
| 补缺 / 覆盖                 | `schedule-coverage.ts` 重新绑定引导                              | 没有来源则引导留空                   | 与初排使用同一比较器                 |
| 人工调整 / 刷新             | `schedule-actions.ts`、`assignment-eligibility.ts` 消费同一事实  | 非本航班、引导来源或行政支援人员拒绝 | 行政岗位常规人员允许、行政人员拒绝   |
| 页面展示                    | `schedule-grid.ts` 统一产生 `is-unfilled`；`styles.css` 最终着红 | 空岗位仍可编辑                       | 普通空缺 DOM 与 CSS 回归             |
| Store / Excel / 历史 / 统计 | 不适用；不新增状态或改变数据结构                                 | 沿用现有事实                         | 影响核对                             |

### 验收清单

- [ ] 行政支援岗位由常规人员填充时，该人员优先成为引导。
- [ ] 没有上述来源时，自动引导选择整条岗位链最下面的人员。
- [ ] 多个引导继续从下向上选择不同人员。
- [ ] 行政支援人员永远不能成为引导来源。
- [ ] 补缺和人工调整后的引导刷新与初排一致。
- [ ] 普通、手动、半休和 KE166 空缺均保持红色可见。
- [ ] owner 执行完整验证；本轮 agent 按要求不运行测试。

---

## 2026-09-20：跨航班重点人员优先

### 规则基本信息

- 规则名：跨航班重点人员优先
- 规则 ID：`cross-flight-priority`
- 规则类型：按页面顺序生效的跨航班人员去向软优先；`KE166` 兼任额外真人为既有明确留空例外
- 事实 owner（唯一）：`ScheduleSettings.crossFlightPriorityPolicies[]` 的 `{ id, enabled, flightNo, staffIds }`
- 规则定义文件：`src/domain/rules/cross-flight-priority.ts`
- 规则版本影响：本地状态版本升至 v7；旧 v6 的岗位列表不能猜成人员，迁移时保留规则行和航班但清空人员选择
- 关联历史：`history.md` 的“跨航班重点岗位优先”及 `KE166` 兼任容量条目；本次完成后追加人员化迁移结论

### 规则定义与边界

- 用户可观察行为：每行配置“优先航班 + 优先人员”；两航班时段冲突且同一选中人员两边都可合法安排时，从上到下优先把该人员留在该行航班的任一合法岗位。
- 同一优先航班内部换岗位仍满足规则；未选中的人员不受本规则保护。
- 不突破人员状态、资质、夜班、时间冲突、全局衔接、工时和值班/人工锁定等更高层约束。
- 一般规则不制造额外空缺；`KE166` 兼任督导的额外真人保护继续允许低优先重叠航班留空，行政支援不计入真人容量。
- 当前组页面和 Excel 只编辑当前组人员编号；共享规则中的另一组人员编号必须原样保留。
- 无合法优先航班岗位时不强行安排；排班失败仍保留原班表。

### 传播矩阵

| 环节                 | 要检查吗 | 计划消费点                                                                  | 失败/回退                            | 回归证据                           |
| -------------------- | -------- | --------------------------------------------------------------------------- | ------------------------------------ | ---------------------------------- |
| 输入事实 / 配置      | 是       | `structured-policy-contract.ts`、`structured-policy-settings.ts`            | 非法/空编号清洗；不保留岗位影子字段  | 结构化设置与迁移测试               |
| 候选池 / 选人        | 是       | 合法 choice 仍由既有资格与硬约束生成                                        | 不新增越权候选                       | optimizer 回归                     |
| 求解模型 / 目标      | 是       | `daily-schedule-model.ts` 按 `flightNo + staffId` 建人员去向目标            | 无合法 choice 时跳过                 | 选中人员留在优先航班任一岗位       |
| 补缺 / 覆盖          | 是       | 一般完整性继续先锁定；KE166 走容量专项                                      | 一般规则不增空缺；KE166 保留既有例外 | 完整性与 KE166 回归                |
| 恢复 / 轮岗 / 公平   | 是       | `crossFlightPriorityReassignmentReasons` 阻止把已保留人员调去重叠低优先航班 | 同航班换岗允许，非重叠不拦           | 反向后置改坏测试                   |
| KE166 特殊收尾       | 是       | `ke166-assignment.ts`、`daily-schedule-model.ts` 按人员 rank 消费           | 无第二名安全常规人员仍允许合法兼任   | 第二真人、行政支援、低优先留空测试 |
| 人工调整边界         | 是       | 沿用现有人工调整权限，不新增硬拦                                            | 人工结果按现有生命周期处理           | 现有人工调整测试                   |
| 最终复核 / evidence  | 是       | `daily-schedule-result.ts` 按已选人员附加证据                               | 不匹配不附加                         | 反馈测试                           |
| Store / localStorage | 是       | v7 恢复迁移与统一设置清洗                                                   | v6 岗位选择清空，规则行保留          | storage migration 测试             |
| 页面展示             | 是       | 规则页显示当前组人员姓名和编号                                              | 切组只换可见名单，不抹另一组选择     | UI + policy actions 测试           |
| Excel 导入导出       | 是       | 新工作表和人员编号列；当前组增量替换                                        | 缺表保留；非法编号拒绝；另一组保留   | Excel + workbook actions 测试      |
| 人员改号 / 删除      | 是       | `configuration-actions.ts` 同步引用                                         | 改号迁移，删除移除                   | configuration actions 测试         |
| 历史统计             | 是       | 不增加历史次数字段                                                          | 不适用                               | 既有统计回归                       |
| 保存 / 展示          | 是       | 规则变更标记当前班表过期                                                    | 原班表保留直至重排                   | app action 回归                    |

### 写入路径与反向门禁

| 写入路径          | 是否消费统一事实 | 必须验证                                                   |
| ----------------- | ---------------- | ---------------------------------------------------------- |
| HiGHS 初始求解    | 是               | 选中人员不会因岗位名称变化失去保护                         |
| 后置恢复/轮岗     | 是               | 先生成合法结果，再尝试把选中人员送去重叠航班，必须拒绝采用 |
| KE166 兼任收尾    | 是               | 不能把人员规则重新解释成岗位规则；额外真人不被行政支援替代 |
| 工作簿导入        | 是               | 只替换当前组编号，另一组编号不丢失                         |
| localStorage 恢复 | 是               | v6 岗位配置不错误映射为人员                                |
| 人员删除/改号     | 是               | 不留下悬空人员引用                                         |

### 验收清单

- [x] 领域红灯：rank 和匹配只依赖 `flightNo + staffId`，同航班换岗仍保护，未选中人员不保护。
- [x] 求解红灯：选中人员在重叠航班之间优先留在配置航班的任一合法岗位。
- [x] 反向红灯：后置流程把选中人员调离优先航班时被拒绝，非重叠调整允许。
- [x] KE166 红灯：兼任额外真人链路改用人员配置且原有绝对优先不回归。
- [x] 分组红灯：当前组编辑/导入不抹掉另一组人员编号。
- [x] 迁移红灯：v6 规则行保留但旧岗位选择清空。
- [x] UI/Excel/反馈文案全部改为“跨航班重点人员优先”。
- [x] 直接测试、类型检查、构建和 `npm.cmd run verify` 通过。
- [ ] 真实页面验收单列，不以自动测试代替。

### 分层验证记录

- 完整门禁：`npm.cmd run verify` 退出码 0。
- 类型检查：通过。
- 普通测试：109 个文件、874 个用例全部通过。
- 性能测试：5 个文件、16 个用例全部通过。
- 生产构建：通过，共转换 276 个模块；保留 4 条既有 `node:module` 浏览器兼容 externalize 提示，不影响构建。
- 真实页面验收：按协作约定未自行打开浏览器，尚未执行。

---

## 规则基本信息

- 规则名：申报/送资料二合一岗位归入送资料
- 规则 ID：`late-priority-aggregate-rotation`、`position-frequency`、`position-rotation` 共用的岗位语义事实
- 规则类型：软目标的分类事实
- 违反时的处理：不得生成第二套分类；统计、候选和后置复核统一重算
- 事实 owner（唯一）：岗位名称和备注的重点岗位语义分类函数
- 规则定义文件：`src/domain/rules/priority-position-semantics.ts`
- 规则版本或指纹影响：不新增状态字段或迁移版本；岗位规则仍进入现有排班指纹，已有班表需重新排班后才体现新的选人结果
- 本次变更日期：2026-09-14
- 关联历史条目：`history.md` 的“2026-09-14：申报/送资料二合一岗位统一归入送资料”

## 规则定义与边界

- 规则要保护的用户可观察行为：同一岗位规则的名称和备注合起来同时命中“申报”和“送资料”时，只计送资料 1 次；分开的申报岗和送资料岗仍各计 1 次。
- 允许的例外：没有例外；如果同一航班还存在独立申报岗，航班统计仍显示并累计申报类别。
- 明确不改变的行为：督导、一号、控制、岗位完整性、人员资格、值班、`KE166`、恢复、时间冲突、工时和允许差值不变。
- 失败时必须保留的旧状态：排班失败继续保留原班表；次数文件校验失败不应用任何修正。
- 需要携带的上下文：岗位规则或历史记录的 `name/position + remark`、航班号、人员、月份、当前排班日期和已勾选末班航班范围。
- 禁止出现的第二事实源或重复判断：UI、Excel、候选模型和后置复核不得自行判断“申报/送资料”，必须消费统一领域分类。

## 传播矩阵

| 环节                   | 要检查吗 | 当前状态 | 唯一消费点/调用函数                                                      | 输入事实与上下文                        | 失败处理                                 | 测试证据                                                                     | 备注                                     |
| ---------------------- | -------- | -------- | ------------------------------------------------------------------------ | --------------------------------------- | ---------------------------------------- | ---------------------------------------------------------------------------- | ---------------------------------------- |
| 输入事实 / 配置        | 是       | 已接入   | 岗位规则 `name + remark`                                                 | 同一规则同时命中两个关键词              | 文本不命中则按原类别                     | `late-priority-policy.test.ts`、`airline-rotation.test.ts`                   | 不新增配置字段                           |
| 候选池 / 选人          | 是       | 已接入   | `latePriorityFrequencyProfileForRule`、`positionFrequencyProfileForRule` | 历史、人工修正、当前目标岗位            | 无法公平时仍保岗位完整                   | `late-priority-frequency.test.ts`、`scheduling-kernel.test.ts`               | 二合一目标只消费送资料频率               |
| 求解模型 / 目标        | 是       | 已接入   | `daily-combination-model.ts` 的末班合计与分类目标                        | 候选 profile 的 `targetKinds` 和 counts | 沿用现有软目标兜底                       | `daily-schedule-optimizer.test.ts`                                           | 不新增目标或改变目标顺序                 |
| 补缺 / 补人            | 是       | 已接入   | 统一重排安全策略消费同一 profile                                         | 最终岗位及候选人员                      | 不为分类公平制造空缺                     | 相邻岗位完整性回归                                                           | 无单独分类分支                           |
| 恢复 / 次班保护        | 是       | 不适用   | 既有末班重点命中判断                                                     | 是否属于重点岗位                        | 沿用原保护                               | 现有恢复回归                                                                 | 二合一仍是重点岗位，保护强度不变         |
| 轮岗 / 公平            | 是       | 已接入   | `late-priority-frequency-review.ts`、一般岗位频率/连续复核               | 送资料规范岗位和末班 delivery 次数      | 无安全方案时保留完整性并警告             | `late-priority-frequency-review.test.ts`、`position-rotation-review.test.ts` | 一般同岗轮换也归送资料                   |
| 覆盖 / 岗位完整性      | 是       | 不适用   | 既有覆盖模块                                                             | 岗位是否有人                            | 保持岗位完整性优先                       | 现有覆盖回归                                                                 | 分类变化不改变岗位资格或空缺             |
| 人工调整边界           | 是       | 已接入   | 最终 assignments 重新统计                                                | 人工换人后的岗位规则                    | 人工结果保留，统计实时重算               | 现有人工调整与统计回归                                                       | 不新增拖拽限制                           |
| 统一守卫 / Ledger 提交 | 是       | 已接入   | `schedule-guard.ts` 的末班频率检查                                       | 最终 profile                            | 软公平只警告                             | 现有 guard 回归                                                              | 不升级为硬约束                           |
| 最终复核 / final       | 是       | 已接入   | `reviewLatePriorityFrequency`                                            | 完整班表及统一分类                      | 无安全方案保持原结果                     | `late-priority-frequency-review.test.ts`                                     | 复核不得按申报再次处理二合一             |
| 安全凭证               | 是       | 已接入   | 现有安全凭证携带岗位规则、历史和人工账本                                 | 同现有频率事实                          | 凭证不匹配时拒绝采用                     | 现有安全凭证回归                                                             | 不新增凭证字段                           |
| Store 安装 / 保存      | 是       | 不适用   | 现有 Store 和班表生命周期                                                | 原四类账本保持不变                      | 保存失败不改状态                         | 现有 Store 回归                                                              | 无 schema/version 变化                   |
| 页面展示 / 用户反馈    | 是       | 已接入   | `statistics-page.ts`                                                     | 每个航班实际适用类别                    | 无适用类别不渲染调整行                   | `statistics-view.test.ts`                                                    | 已按航班适用类别展开，二合一只显示送资料 |
| 历史归档 / 统计        | 是       | 已接入   | `monthly-late-priority-statistics.ts`、专用次数 Excel                    | 历史/当前班表/人工修正                  | 无效旧申报修正忽略；无效次数文件整表拒绝 | 月度统计、Excel、app actions 测试                                            | 航班小计、导出和导入均消费动态类别投影   |

## 写入路径盘点

| 写入路径         | 文件 / 函数                                                        | 是否经过统一守卫   | 是否可能绕过                     | 反向测试                           |
| ---------------- | ------------------------------------------------------------------ | ------------------ | -------------------------------- | ---------------------------------- |
| 初始求解结果     | `daily-schedule-optimizer.ts` / `optimizeDailySchedule`            | 是                 | 否                               | 完整排班测试证明低频送资料人员优先 |
| 补缺或覆盖       | `schedule-coverage.ts`                                             | 是                 | 否                               | 现有岗位完整性与安全回归           |
| 恢复或轮岗       | `late-priority-frequency-review.ts`、`position-rotation-review.ts` | 是                 | 否                               | 二合一后置复核只按送资料比较       |
| KE166 / 特殊收尾 | `ke166-supervisor-finalizer.ts`                                    | 是                 | 否                               | 现有 KE166 与末班公平回归          |
| 人工调整         | `schedule-actions.ts`                                              | 是                 | 人工可覆盖软公平，但最终统计重算 | 人工换人后统计类别正确             |
| 导入或恢复旧班表 | 历史/工作簿导入                                                    | 不直接提交自动方案 | 历史无 rule ID，依赖统一文本分类 | 导入二合一历史只产生送资料 1 次    |
| 最终替换 / 安装  | `schedule-lifecycle.ts`                                            | 是                 | 否                               | 现有失败保留原班表回归             |

## 测试与验收

### 必须具备的自动测试

- [x] 二合一历史和当前岗位只计送资料 1 次。
- [x] 分开的申报岗和送资料岗仍分别计 1 次。
- [x] 一般同岗轮换把二合一规范为送资料。
- [x] 候选排序和 HiGHS 目标消费送资料次数。
- [x] 后置公平复核不把二合一重新算作申报。
- [x] 统计 UI 只展示航班实际适用类别。
- [x] 专用次数 Excel 拒绝二合一岗位的申报行，缺项或多项均不应用。
- [x] 旧申报人工修正不迁移且不再参与算法，送资料修正继续有效。
- [x] 所有直接消费者的相邻测试通过。
- [x] 软公平违反时仍只产生 warning，不得被守卫当作硬错误拒绝。

### 分层验证记录

- 自动测试命令与结果：初始目标测试按预期出现 7 个失败文件；补充旧申报修正航班小计测试后，红灯为期望 0、实际 5。实现后目标测试 9 个文件、193 项通过；最终 `npm.cmd run test` 为 102 个文件、804 项全部通过。
- 类型检查与构建结果：最终 `npm.cmd run verify` 退出码 0；`tsc -b --pretty false` 与 `tsc -b && vite build` 通过。Vite 保留既有 `node:module` 浏览器外部化提示。
- 性能测试（如适用）：首次 `verify` 暴露专用次数性能夹具仍固定乘四类，并有两项本机耗时阈值波动；夹具改用动态类别后，未放宽阈值，单独复跑及最终 `verify` 均为 4 个文件、14 项通过。
- 真实页面验收步骤与结果：未启动真实浏览器；Lit DOM 测试已验证二合一航班只显示送资料，同航班存在独立申报岗时同时显示申报。
- 人工验收：未执行，建议在实际配置中展开二合一航班并导出一次专用次数文件复核。
- 未验证项：未用 owner 的真实历史和旧次数文件做人工导入；旧申报修正按约定保留原始账本但不迁移。

## 结论

- 漏了哪些环节：传播矩阵内已知消费点均已接入；实现复核补上了旧申报修正在航班单格小计中的隐藏计数缺口。
- 哪些环节确认不适用，为什么：不改资格、空缺、恢复强度、Store schema 或迁移，因为此次只改变现有四类中的归类。
- 哪些写入路径确认使用同一守卫：自动求解、覆盖、后置轮岗、KE166 收尾和最终安装沿用现有安全链。
- 后置改坏是否被拦截：软公平由后置复核按送资料重新评估并由 guard 告警；回归测试证明不会重新按申报处理二合一。
- 失败时旧结果是否保留：沿用现有排班事务与次数导入整表拒绝合同。
- 规则版本、证据和页面提示是否同步：无需状态版本；`spec.md`、README、领域分类、统计投影、页面、Excel 和测试已同步。
- 剩余风险与下一步：旧组合岗位的申报修正保留原始数据但失效，这是 owner 已接受的边界；真实浏览器和真实旧文件导入仍需人工验收。

---

## 规则：TR121/H02 工作班冷却

### 规则基本信息

- 规则名：TR121/H02 工作班冷却
- 规则 ID：`tr121-h02-cooldown`
- 规则类型：岗位完整性之后、月度轮次公平之前的可突破优先目标
- 违反时的处理：存在完整非冷却方案时不得使用冷却人员；否则保留岗位完整并选择距离上次 H02 最久者，记录突破原因
- 事实 owner（唯一）：`src/domain/rules/tr121-h02-cooldown.ts`
- 配置 owner：`ScheduleSettings.tr121H02CooldownWorkdays`，默认 3、范围 0 至 30、0 表示关闭
- 规则版本或指纹影响：设置进入现有排班规则指纹；旧状态由设置规范化补默认值，不新增兼容别名
- 本次变更日期：2026-09-14

### 规则定义与边界

- 保护行为：承担 `TR121/H02` 一号后，随后 N 个全队已归档工作班内优先不再承担。
- 匹配边界：必须同时匹配规范航班号 `TR121`、物理岗位名 `H02` 和现有一号语义；不扩展到其他一号、督导、申报或送资料。
- 允许例外：所有完整方案都必须使用冷却人员时允许突破；月上限 2 次、人员资格、状态、夜班、时间冲突、最小衔接和工时仍不可突破。
- 顺序：值班人员冷却时先尝试其他晚撤目标；整体岗位完整性锁定后，冷却先于末班分类差值和月度轮次公平。
- 数据语义：冷却只读取带日期的实际历史；人工次数修正没有日期，不参与冷却。
- 禁止第二事实源：候选、值班、后置安全和反馈不得自行拼接 `TR121/H02` 关键词。

### 冲突矩阵

| 冲突规则                             | 冲突场景                               | 当前优先级                             | 冷却规则后的优先级                                 | 修改点                     | 兜底                                   |
| ------------------------------------ | -------------------------------------- | -------------------------------------- | -------------------------------------------------- | -------------------------- | -------------------------------------- |
| 值班锁定                             | 值班人员仍在冷却但 H02 是首选晚撤目标  | 值班首选锁定优先                       | 值班身份不变，先试其他晚撤目标；都不可行再突破冷却 | 值班目标排序消费冷却 owner | 回到 H02 并记录突破                    |
| 月上限 2 次                          | 冷却兜底人员已达上限                   | 月上限候选过滤                         | 月上限继续优先                                     | 沿用现有上限 owner         | 全员达上限时 H02 留空                  |
| 全员完成第 N 次                      | 低次数人员仍冷却，高次数人员已过冷却   | 月度轮次优先                           | 冷却优先，月度次数在非冷却池内均衡                 | 冷却目标置于频率目标之前   | 冷却结束后低次数人员重新优先           |
| 后置重排                             | 恢复、频率或连续轮岗把冷却人员换回 H02 | 目前无冷却保护                         | 非覆盖重排不得扩大冷却违反                         | 统一重排安全比较           | 覆盖确需补空缺时允许突破               |
| 跨工作日恢复                         | 非冷却人员受恢复保护，冷却人员可填 H02 | 恢复目标晚于重点公平，但严格目标可排除 | 硬性严格恢复优先；普通恢复不得推翻冷却             | 候选硬约束与后置安全       | 无完整非冷却方案时突破冷却             |
| 督导/申报/送资料公平                 | 同班多个重点岗位竞争人员               | 末班合计与分类公平                     | H02 冷却先行，其余类别事实和顺序不变               | 仅 H02 候选 profile 生效   | 后续分类公平在剩余完整方案中优化       |
| 疲劳/工时/时间冲突                   | 非冷却人员因硬约束不可用               | 硬约束优先                             | 不变                                               | 不改资格 owner             | 使用未达月上限的冷却人员；仍无人则空缺 |
| 跨工作日资质预留/严格衔接/跨航班优先 | 非冷却 H02 人员必须为前序稀缺目标保留  | 前序规则优先                           | 不变                                               | 冷却目标放在这些目标之后   | 无完整非冷却方案时突破                 |

### 传播矩阵

| 环节              | 要检查吗 | 目标状态 | 唯一消费点                         | 失败处理                             | 回归证据           |
| ----------------- | -------- | -------- | ---------------------------------- | ------------------------------------ | ------------------ |
| 输入配置          | 是       | 已接入   | 标量设置定义、规则页               | 无效值规范到 0 至 30                 | 设置/页面测试      |
| localStorage 恢复 | 是       | 已接入   | `normalizeScheduleSettings`        | 缺字段补默认 3                       | storage 测试       |
| Excel 往返        | 是       | 已接入   | 通用“规则参数”表                   | 非法值按设置合同校验                 | 既有通用设置测试   |
| 历史事实          | 是       | 已接入   | 冷却 owner 消费最近归档工作班日期  | 无完整日期不猜测                     | domain 测试        |
| 候选池            | 是       | 已接入   | 候选保留全部硬合格且未达月上限人员 | 冷却不硬删                           | 完整排班回归       |
| HiGHS 目标        | 是       | 已接入   | `tr121-h02-cooldown` 字典序目标    | 岗位完整后最小化违反                 | domain 入口测试    |
| 值班锁定          | 是       | 已接入   | 值班晚撤目标排序                   | 其他目标无解才回 H02                 | preparation 接入   |
| 覆盖/补缺         | 是       | 已接入   | 重排安全的 coverage 例外           | 允许为减少空缺突破                   | 复用 coverage 语义 |
| 恢复/轮岗/公平    | 是       | 已接入   | 统一重排安全比较                   | 不得扩大已成立冷却结果               | 后置反向测试       |
| 最终 assignments  | 是       | 已接入   | 冷却 profile 与决策证据            | 合法突破保留并说明，不升级为拒绝守卫 | 完整入口测试       |
| 人工调整          | 是       | 不适用   | 现有人工入口                       | 人工可覆盖软规则                     | 现有人工测试       |
| 页面反馈          | 是       | 已接入   | assignment 决策证据                | 显示冷却突破原因                     | 决策 trace 回归    |
| 统计              | 是       | 不适用   | 月度统计维持原事实                 | 不新增第五类统计                     | 原四类测试         |

### 写入路径与反向保护

| 写入路径                        | 是否接入 | 保护方式                             |
| ------------------------------- | -------- | ------------------------------------ |
| 初始整体求解                    | 已接入   | 岗位完整性后执行冷却目标             |
| 值班目标                        | 已接入   | 冷却 H02 移到其他晚撤目标之后        |
| 覆盖补缺                        | 已接入   | 只在减少空缺时允许突破               |
| 末班频率/一般频率/恢复/连续轮岗 | 已接入   | 统一拒绝扩大冷却质量向量             |
| KE166 收尾                      | 已接入   | 共享重排安全保护，不能跨航班改坏 H02 |
| 人工调整                        | 不适用   | 软规则允许人工覆盖                   |

### 验收清单

- [x] 默认 3 班冷却选择非冷却人员。
- [x] 配置 0 时关闭，1 至 30 按设置值生效。
- [x] 全员冷却仍填满 H02，并选择距离上次最久者。
- [x] 值班人员冷却时先采用其他晚撤目标，均无解时允许回到 H02。
- [x] 月上限 2 次不被冷却兜底突破。
- [x] 后置真实写入路径不能把非冷却结果改成冷却结果。
- [x] 督导、申报、送资料和其他一号岗位不受影响。
- [x] 设置保存、旧状态恢复和 Excel 往返完整。
- [x] 目标测试、完整测试、性能测试和 `verify` 通过。

---

## 规则：KE166 机动督导无法安排时留空告警

### 规则基本信息

- 规则名：KE166 机动督导无法安排时留空告警
- 规则 ID：`ke166-supervisor`
- 规则类型：最终完整性提示；不再是阻断提交的硬守卫
- 违反时的处理：保留 `unfilled`，班表继续生成，写入 warning，人员空格标红并提示人工复核
- 事实 owner（唯一）：`isKe166MobileSupervisor()` 判断岗位身份，`assessKe166AssignmentSnapshot()` 判断最终快照是否缺失
- 规则定义文件：`src/domain/flights/schedule-tasks.ts`、`src/domain/kernel/schedule-guard.ts`
- 规则版本或指纹影响：不新增状态字段，不迁移旧数据；现有岗位配置和排班指纹不变
- 本次变更日期：2026-09-14
- 关联历史条目：`history.md` 的“2026-09-14：KE166 机动督导空缺由阻断改为告警”

### 规则定义与边界

- 规则要保护的用户可观察行为：KE166 机动督导无法安排时，整张班表仍可生成；该 assignment 留空，人员格标红，显示“KE166机动督导未安排”，并产生“KE166机动督导未安排，岗位已留空，请人工复核”告警。
- 允许的例外：仅 KE166 顶部机动督导最终空缺降级；已形成的兼任组仍必须保持两格同人及工时去重。
- 明确不改变的行为：候选资格、独立安排、缺员兼任、安全重排、连续轮岗、普通柜台岗位完整性、其他守卫和其他航班机动督导规则均不变。
- 失败时必须保留的旧状态：其他阻断守卫失败仍拒绝提交；本规则只保留 KE166 督导空缺，不采用其他不安全结果。
- 需要携带的上下文：最终 assignments、航班与岗位配置、KE166 快照事实、warning sink。
- 禁止出现的第二事实源或重复判断：页面必须调用 `isKe166MobileSupervisor()`，不得用航班号和岗位名称另写一套判断。

### 传播矩阵

| 环节                   | 要检查吗 | 当前状态 | 唯一消费点/调用函数                                 | 输入事实与上下文                      | 失败处理                               | 测试证据                    | 备注                    |
| ---------------------- | -------- | -------- | --------------------------------------------------- | ------------------------------------- | -------------------------------------- | --------------------------- | ----------------------- |
| 输入事实 / 配置        | 是       | 不变     | `isKe166MobileSupervisor()`                         | 航班、岗位规则                        | 不匹配则不适用                         | 既有任务生成测试            | 不新增配置              |
| 候选池 / 选人          | 是       | 不变     | KE166 finalizer 既有候选与兼任判断                  | 人员状态、资质、冲突、工时、兼任范围  | 继续尝试既有候选路径                   | 既有 KE166 测试             | 本次不修重排漏解        |
| 求解模型 / 目标        | 是       | 不适用   | KE166 顶部督导在普通柜台整体模型后收尾              | 普通柜台完整方案                      | 不改模型                               | 既有 kernel 测试            | 无新变量或目标          |
| 补缺 / 覆盖            | 是       | 不变     | `ke166-supervisor-finalizer.ts`                     | 独立、兼任和安全重排结果              | 全部失败后保留 `unfilled`              | kernel 新回归               | 不扩大搜索              |
| 恢复 / 次班保护        | 是       | 不变     | 既有 KE166 收尾与安全检查                           | 跨工作日规则事实                      | 沿用现有优先级                         | 既有恢复回归                | 不改变规则顺序          |
| 轮岗 / 公平            | 是       | 不变     | `ke166-rotation-review.ts`                          | 历史连续承担与最终 assignment         | 无安全替代时仍按既有处理               | 既有轮岗回归                | 本次不改重排器          |
| 覆盖 / 岗位完整性      | 是       | 已接入   | `assessKe166AssignmentSnapshot()`                   | 最终 KE166 督导/兼任组                | KE166 督导空缺转 warning；其他异常不变 | ledger 新回归               | 仅修改该守卫级别        |
| 人工调整边界           | 是       | 不变     | 既有人工调整入口                                    | 最终人员与兼任关系                    | 沿用现有拒绝/告警                      | 既有人工调整测试            | 空格仍可人工填写        |
| 统一守卫 / Ledger 提交 | 是       | 已接入   | `createKe166SnapshotScheduleGuard()`                | final 阶段快照与 warning sink         | 不抛错，写专用 warning                 | `schedule-ledger.test.ts`   | 其他 guards 未修改      |
| 最终复核 / final       | 是       | 已接入   | 默认 guard 集合                                     | 最终 assignments                      | 允许仅此项空缺通过                     | `scheduling-kernel.test.ts` | 仍执行全部其他守卫      |
| 安全凭证               | 是       | 不变     | 现有完整安全方案流程                                | 最终方案                              | 其他安全失败仍不可采用                 | 完整测试                    | 不新增凭证字段          |
| Store 安装 / 保存      | 是       | 不变     | 现有 schedule lifecycle                             | 成功结果 assignments                  | 原子安装                               | 既有 lifecycle 测试         | warning 不持久化        |
| 页面展示 / 用户反馈    | 是       | 已接入   | `schedule-grid.ts` 复用 `isKe166MobileSupervisor()` | `status === "unfilled"` 的 KE166 督导 | 人员格专用红色与 placeholder           | `schedule-view.test.ts`     | 不依赖瞬时 warning 保存 |
| 历史归档 / 统计        | 是       | 不变     | 现有归档与统计                                      | `unfilled` assignment                 | 空缺不计人员次数                       | 既有历史测试                | 无 schema 变化          |

### 写入路径盘点

| 写入路径         | 文件 / 函数                     | 是否经过统一守卫 | 是否可能绕过 | 反向测试                                |
| ---------------- | ------------------------------- | ---------------- | ------------ | --------------------------------------- |
| 初始普通柜台求解 | `daily-schedule-optimizer.ts`   | 是               | 否           | kernel 回归证明普通柜台仍生成           |
| KE166 收尾       | `ke166-supervisor-finalizer.ts` | 是               | 否           | 无合格督导时输出 `unfilled`             |
| 恢复或轮岗       | `ke166-rotation-review.ts`      | 是               | 否           | 既有轮岗与完整性回归                    |
| 人工调整         | 既有 schedule actions           | 是               | 受控人工入口 | 既有兼任边界回归                        |
| 最终替换 / 安装  | schedule lifecycle              | 是               | 否           | guard warning 不阻断，其他 error 仍阻断 |

### 测试与验收

- [x] 红灯证明原守卫会拒绝 KE166 督导空缺。
- [x] ledger 测试要求改为 warning 且不抛错。
- [x] 完整排班入口覆盖“普通柜台可排、无合格机动督导”的结果。
- [x] UI 测试要求 KE166 督导人员格专用标红和专用文案。
- [x] 目标测试实现后转绿：3 个目标测试通过，随后 3 个相关测试文件共 172 项通过。
- [x] 完整 `npm.cmd run test` 通过：102 个测试文件、805 个用例通过。
- [x] 完整 `npm.cmd run verify` 通过：类型检查、102 个测试文件/805 个用例、4 个性能测试文件/14 个用例和生产构建全部通过。
- [ ] 真实浏览器人工验收。

### 结论

- 本次只降低 KE166 最终快照守卫的严重级别，不扩大或修复 KE166 重排搜索。
- KE166 仍优先尽力安排；只有既有全部方案失败后才保留空缺。
- 其他守卫、其他机动督导和普通岗位完整性继续按原合同执行。

## 规则：上午半休的时间范围与补缺

- 规则 ID：`half-rest`；事实所属文件：`src/domain/rules/half-rest.ts`。
- 选择“上午半休”只禁止该人员承担运营日早班（06:00 至 12:00）的岗位；下午、晚间及跨午夜凌晨岗位仍可正常参与。由此产生的上午空缺必须继续进入统一补缺和完整性流程，由其他合格人员补位。
- 所有写入 `assignments` 的阶段仍须通过 `schedule-guard.ts` 和最终安全复核；不得把上午半休扩大为全天禁排。

| 阶段                                                                                                                       | 消费点                                                         | 预期行为                                                   | 失败/兜底                        |
| -------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------- | ---------------------------------------------------------- | -------------------------------- |
| 选人                                                                                                                       | `half-rest.ts`、`daily-schedule-model.ts`                      | 仅从 12:00 前候选移除上午半休人员，保留其后续时段候选      | 无替代人时保留空缺并记录半休原因 |
| 排班                                                                                                                       | `daily-schedule-model.ts`、`daily-schedule-optimizer.ts`       | 上午岗位仍按完整性目标求解                                 | 不得全天排除上午半休人员         |
| 补人                                                                                                                       | `schedule-coverage.ts` 及统一后置计划                          | 对半休造成的上午空缺尝试其他合格人员                       | 仅硬约束确实无解时留空并反馈     |
| 恢复                                                                                                                       | `late-shift-recovery-review.ts`、`late-shift-cutoff-review.ts` | 只按目标时段判断冲突，不放大为全天限制                     | 按既有恢复兜底                   |
| 轮岗                                                                                                                       | 轮岗/频率复核                                                  | 换位不得把上午半休人员换入上午岗位，其他时段可正常换位     | 安全复核失败保留原结果           |
| 补漏                                                                                                                       | `schedule-finalizer.ts` 及各补缺执行器                         | 候选继续消费统一半休时段判断                               | guard 拒绝违规写入               |
| 最后检查                                                                                                                   | `schedule-guard.ts`、`daily-schedule-safety.ts`                | 最终 assignments 上午无违规，下午/晚间安排不因上午半休消失 | 违规拒绝采用并保留原班表         |
| 补充边界：半休事实 owner 将运营日早班定义为 06:00 至 12:00；00:00 至 06:00 的跨午夜凌晨岗位继续按午后/晚间可工作时段处理。 |

## 规则：KE166 机动督导初排可完成性预留（2026-09-14）

- 当前状态：初排模型与现有 KE166 收尾已接入；两轮红灯均已转绿，最终完整 `verify` 退出 0；真实页面尚未人工复现。只提前两个非行政支援普通岗位空缺目标，不整体搬迁其他完整性目标；避免改变次日值班保护等相邻结果。
- 规则 ID：沿用 `ke166-supervisor`；事实 owner 为现有 `isKe166MobileSupervisor`、基础资格判断和 `evaluateMobileSupervisorCoverage`，不创建姓名名单或第二套兼任规则。
- 规则类型：岗位完整性之上的可完成方案选择，不是硬删除其他航班候选；独立督导优先，确实没有独立人员时才允许同航班合格柜台兼任。
- 用户可见边界：有完整独立方案时顶部独立安排；人员紧张时优先让合格督导留在 KE166 允许兼任的柜台并关联顶部，CX937 不因预留新增空缺；没有可行的独立/兼任完整方案时保留原空格标红和 warning。
- 冲突：现有 `candidate:ke166-supervisor` 只在基础合格人数恰好 1 时保护跨航班占用。存在 KE166 督导任务且至少一名基础合格者时，先锁定上午及全部非行政支援普通岗位最少空缺，再比较现有预留与新单人可用性目标，之后比较值班及其余目标；值班身份不构成资格排除，具体值班目标的时间/工时冲突不能突破。
- 不变边界：状态、人员类型、资质、夜班、时间冲突、最小衔接、工时、值班目标、手动岗位及禁止兼任规则不被突破；值班身份及已承担末班重点岗位本身都不排除 KE166 督导，只有实际岗位冲突按既有硬约束处理；不改变 CX937 及其他航班自身的岗位资质，不加姓名特例。

| 环节                    | 现状                                     | 本次消费点 / 目标                                                                                                                                                    | 无完整安全方案时                      |
| ----------------------- | ---------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------- |
| 输入事实 / 配置         | KE166 顶部、柜台及可兼任范围已有唯一配置 | 消费任务列表、真实候选资格、兼任规则，不新增配置                                                                                                                     | 保持既有设置                          |
| 候选池 / 选人           | 基础资格人数恰为 1 才触发早期预留        | 已接入：消费原基础资格、半休事实、原自动资格检查及同航班兼任规则；已承担末班重点岗位或兼值班身份不单独剔除                                                           | 不硬删 CX 候选                        |
| 求解模型 / 目标         | 顶部不在普通柜台模型内                   | 已接入：两个普通岗位空缺目标先锁定；每名合格督导对冲突岗位及已有工作加督导的累计工时建约束，同航班合法柜台工时只计一次；只要至少一名可用就达到目标，不强求全部人预留 | 保持普通岗位完整性                    |
| 值班锁定                | 具体岗位由现有锁定保护                   | 已接入：KE 可完成目标先于冲突的值班晚撤目标；人员值班身份保留，目标依原规则回退；无需改值班事实 owner                                                                | 不突破实际时间冲突                    |
| 补缺 / 覆盖             | 统一后置 mutation 可换柜台               | 复核不得把可行 KE166 柜台安排换成跨航班占用且无法收尾                                                                                                                | 未找到可行换位时留原结果              |
| 恢复 / 次班保护         | 后置恢复可重排                           | 同上，仍消费原有恢复事实及守卫                                                                                                                                       | 原有严格恢复和硬约束优先              |
| 轮岗 / 公平             | 后置公平可重排柜台                       | 先保证可用方案，继续执行不增加空缺的轮换                                                                                                                             | 不为兼任牺牲资格或完整性              |
| 补漏 / 收尾             | finalizer 先独立，再兼任/安全重排        | 沿用独立优先、合法柜台关联；初排先选择可完成方案，不新增从 CX937 抽人的路径                                                                                          | 顶部留空，原告警不变                  |
| 人工调整边界            | 现有人工入口与可兼任范围校验             | 不新增人工硬锁；人工调整仍核对兼任组                                                                                                                                 | 沿用现有人工告警                      |
| Ledger / schedule-guard | 后置替换和 KE166 收尾均通过账本          | 提案逐次安全检查，兼任关联与工时去重，所有阶段不得绕过                                                                                                               | 拒绝非法提案并保留原班表              |
| 最终复核 / 安全凭证     | KE166 空缺为 warning                     | 最终 CX937、KE166 两格、资格/锁定/工时和决策证据一致                                                                                                                 | 允许 KE166 留空并标红，不放宽其他守卫 |
| Store / 页面 / 历史     | 现有 assigned/unfilled 投影及归档        | 不新增存储字段；独立/兼任沿用关联和专用红色空缺文案                                                                                                                  | 不把空缺算作实际承担                  |
| 测试                    | 已有单航班兼任、独立及缺员测试           | 已接入：真实入口三情形、带值班和前次末班重点岗位经历的初排红灯，以及累计工时已满者不能独立接督导的第二轮红灯；现有后置关联、安全守卫与值班相邻回归均保留             | 定向及完整排班、性能、验证门禁        |

写入路径：整体模型生成完整普通岗位方案；覆盖、恢复、轮岗、补漏各自经既有事务账本提交；KE166 finalizer 只在已形成的同航班合法柜台上兼任或采用全量安全重排，结果经 `ledger.commit`；最终仍由默认 `schedule-guard` 检查并产生安全凭证。任何新局部写入必须先复用同一守卫，不允许直接清空 CX937 或 KE166 柜台。

## 规则：KE166 兼任后额外真人绝对预留（2026-09-18）

- 当前状态：已实现，定向回归与完整 `verify` 已通过；owner 已裁决新合同覆盖上述“CX937 不因预留新增空缺”旧边界，真实页面仍待 owner 验证。
- 规则 ID：沿用 `ke166-supervisor`；不新增页面配置、航班名单或人名特例。
- 规则事实：KE166 机动督导与允许兼任的普通柜台是两个人力需求；同一常规人员兼任时实际只提供一名不同人员，因此存在 1 名真人缺口。
- 唯一事实 owner：`daily-schedule-model.ts` 的 KE166 可完成性模型，复用现有自动资格、兼任范围和工时去重事实。
- 固定顺序：人员状态、类型、资质、夜班、时间冲突、全局最小衔接和工时上限仍是硬约束；当允许兼任的 KE166 柜台在配置中排在重叠岗位上方时，先最大化 KE166 不同真人容量，再保留未排在其后的重叠岗位，最后比较普通岗位完整性和值班等目标。
- 绝对边界：有安全替代人时由不同常规人员分别承担 KE166 督导与普通岗；没有其他人能同时保住配置中较低优先的 CX937 时，允许 CX937 留空，不得因岗位完整性把该常规人员放给 CX937，也不得改为腾空无关唯一资质岗位。
- 不可行回退：硬约束下根本不存在第二名合格常规人员时，仍允许现有合法兼任兜底；独立、兼任均不可行时，沿用顶部督导留空告警。
- 行政支援边界：A1-A6 不进入自动候选，不计入 KE166 常规真人容量；事后人工补位不倒推自动阶段人手充足。
- 明确不变：兼任同时段工时仍只计一次，督导与柜台疲劳分别累计；不改 Excel、localStorage、页面配置或人工调整边界。

| 传播环节              | 消费方式                                                                                               | 失败 / 回退                      | 回归保护                          |
| --------------------- | ------------------------------------------------------------------------------------------------------ | -------------------------------- | --------------------------------- |
| 输入事实              | 现有 KE166 顶部督导、普通柜台、兼任范围和常规人员类型                                                  | 无新配置                         | 无 Excel/localStorage 变更        |
| 候选池                | 只使用通过现有自动硬约束的常规人员                                                                     | 行政支援不计入                   | 人员类型回归                      |
| HiGHS 模型            | 在岗位完整性前锁定 KE166 不同真人容量，再保护未排在 KE166 后方的重叠岗位                               | 无第二名可行人员时允许兼任       | 有/无 CX 替代人及无关唯一岗位回归 |
| 值班 / 完整性         | KE166 真人容量高于配置中较低优先的重叠岗位完整性                                                       | 不突破硬约束，不牺牲无关唯一岗位 | CX937 可留空                      |
| 补缺 / 覆盖           | 后置提案不得把已保留的人放给重叠航班                                                                   | 无合法方案保留原结果             | 后置改坏反向测试                  |
| 恢复 / 轮岗 / 公平    | 不得逆转已锁定的 KE166 真人容量                                                                        | 保留 KE166 并认可较低规则让步    | 现有相邻规则回归                  |
| KE166 收尾            | 先独立；只有真无第二人时才兼任                                                                         | 兼任工时去重                     | 最终 `generateSchedule()` 断言    |
| 最终守卫 / 反馈       | 从最终 assignments 确认兼任时没有可从较低优先岗位安全调回的第二名常规人员，CX 空缺沿用既有红色缺员证据 | 不安全结果拒绝采用               | Ledger 后置改坏反向测试           |
| Store / UI / 导入导出 | 不适用；消费既有 assignments                                                                           | 不新增字段                       | 只做影响核对                      |

## 规则：同航班人员互斥（2026-09-15）

- 规则 ID：`same-flight-staff-exclusion`。
- 唯一事实 owner：`src/domain/rules/same-flight-staff-exclusion.ts`；配置事实为 `ScheduleSettings.sameFlightStaffExclusions`，每条保存无序人员对和可选航班号，空航班号表示适用于任何航班。
- 自动规则：同一最终 `flightId` 内不得同时出现配置的一对人员；无替代人员时只安排其中一人，其他岗位允许留空，不得突破互斥。不同航班即使同一时段也不触发。
- 人工边界：拖拽、输入或交换可以显式突破，但必须写入红色人工越权警告；下一次自动重排重新执行硬互斥。
- 不变范围：不改变人员资质、状态、夜班、时间冲突、工时、值班、KE166、岗位公平和统计口径；不按姓名写特例，不新增启停或软模式。

| 环节                | 是否适用 | 消费点 / 预期行为                                                                             | 失败或兜底                                              | 测试证据                             |
| ------------------- | -------- | --------------------------------------------------------------------------------------------- | ------------------------------------------------------- | ------------------------------------ |
| 输入事实 / 默认值   | 是       | `structured-policy-contract.ts`、`structured-policy-settings.ts` 定义并清洗人员对；默认空集合 | 缺失字段补空，不改变旧配置                              | settings / storage 回归              |
| 规则页              | 是       | 独立“同航班人员互斥”卡片；两个人员下拉、可选航班、增删，无启停开关                            | 同人、重复无序人员对或人员缺失不保存                    | UI / policy actions 回归             |
| Excel 导入导出      | 是       | `同航班人员互斥` 工作表保存规则 ID、航班、两端人员编号与姓名                                  | 表缺失保留当前值；仅表头清空；非法行整表不覆盖并提示    | Excel 往返回归                       |
| localStorage / 恢复 | 是       | `ScheduleSettings` 统一清洗入口恢复字段                                                       | 旧状态缺失时补空集合                                    | storage 回归                         |
| 候选池 / 选人       | 是       | 单个候选仍可进入；互斥依赖同航班组合，不能在孤立候选阶段误删两端                              | 交给整体模型判断组合                                    | 不同航班允许同班测试                 |
| 当天整体模型 / 初排 | 是       | 对每个航班、每条适用人员对加入“甲在本航班任一岗”和“乙在本航班任一岗”不能同时成立的线性约束    | 有替代人时完整填岗；无替代时 vacancy 参与既有完整性目标 | 公共 `generateSchedule` 四类范围测试 |
| 值班 / KE166        | 是       | 值班目标和 KE166 可完成性只能在互斥可行域中选人；互斥不因身份突破                             | 目标不可兼得时按硬约束保留互斥并走既有空缺反馈          | 相邻规则测试                         |
| 补人 / 覆盖         | 是       | 通过统一 Ledger guard 消费同一 owner；禁止自动补成同航班双人违规                              | 拒绝提案并保留原结果                                    | Ledger 反向测试                      |
| 恢复                | 是       | 重排候选模型与提交后的统一 guard 均检查互斥                                                   | 无安全换位时保留恢复前结果                              | 后置反向测试                         |
| 轮岗 / 公平         | 是       | 重排优化器硬约束或最终 guard 不允许为了公平把人员对凑到同航班                                 | 无安全轮换时保留原结果并沿用既有反馈                    | 后置反向测试                         |
| 补漏 / 收尾         | 是       | 所有 replace/update 提案继续经过 Ledger；KE166 兼任以同一人员计一次，不会自行形成一对人员     | 无安全候选时留空                                        | finalizer / guard 回归               |
| 人工调整            | 是       | `manual-assignment-override` 诊断互斥，允许落位并写 `same-flight-staff-exclusion` 红色警告    | 不阻止拖拽；清除/换人后按最终事实刷新警告               | 拖拽突破并报警测试                   |
| 最终自动复核        | 是       | 默认 schedule guard 检查最终 assignments；自动结果存在同航班互斥则拒绝采用                    | 保留生成前班表                                          | guard 与安全凭证反向测试             |
| Store / 页面        | 是       | 设置保存后标记当前班表待重排；人工警告沿用红色提示投影，自动 vacancy 沿用红色待补位           | 刷新只恢复状态，不冒充重排                              | app / UI 测试                        |
| 历史 / 统计         | 否       | 互斥只约束当日同一航班组合，不读取历史、不改变次数                                            | 归档仍保存实际人工越权说明                              | 既有历史测试保持                     |

### 写入路径盘点

| 写入路径               | 自动互斥                                   | 守卫边界                           | 人工例外                       |
| ---------------------- | ------------------------------------------ | ---------------------------------- | ------------------------------ |
| 整体模型初排           | 线性硬约束                                 | 最终默认 guards                    | 不适用                         |
| 覆盖、恢复、轮岗、补漏 | 重排可行域尽量前置，并由 Ledger guard 兜底 | `createScheduleLedger().commit()`  | 不适用                         |
| KE166 收尾             | 候选/重排消费同一事实，Ledger guard 兜底   | replace proposal                   | 不适用                         |
| 人工拖拽、输入、交换   | 不硬拦                                     | 人工资格诊断与警告刷新             | 允许并留下红色警告             |
| 最终安装               | 自动安全凭证不得包含未授权互斥违规         | `assertScheduleSafetyCredential()` | 已保存人工班表不冒充新自动结果 |

## 规则：双组共享配置与严格隔离工作区（2026-09-18）

- 当前状态：数据模型、旧单组状态迁移、Store 当前组投影、页面组切换、当前组排班/统计/历史/值班、配置导入导出均已接入；领域模块仍通过兼容投影读取当前组，不复制第二套规则事实。
- 业务合同：A、B 两组共用一份规则、航班模板和岗位配置；人员、当前班表、历史、值班、备勤和统计事实严格按组隔离。两组人员不得重复，当前阶段不考虑跨组顶班。
- 事实 owner：共享配置由 `AppState.shared` 持有；组内事实由 `AppState.groups[groupId]` 持有；`activeGroupId` 只表示当前工作区，不复制事实。
- 旧数据迁移：没有组信息的旧单组状态整体迁移到默认 `A` 组；不得复制历史或人员到 `B` 组；迁移必须可追溯并保留原规则/航班/岗位配置。
- 失败处理：无法识别或校验失败时回退到默认空工作区/原有安全恢复路径，不把一组数据猜测分配到另一组。

| 环节                      | 是否适用 | 第一阶段状态 | 唯一消费点 / 目标                                     | 失败或边界                              |
| ------------------------- | -------- | ------------ | ----------------------------------------------------- | --------------------------------------- |
| 输入事实 / 数据模型       | 是       | 已接入       | `AppState.shared`、`AppState.groups`、`activeGroupId` | 组 ID 唯一；人员不得跨组重复            |
| 旧 localStorage 恢复      | 是       | 已接入       | 版本迁移把旧根级人员/历史/班表/值班迁入 A 组          | 不复制到 B；迁移失败保留旧安全回退      |
| 当前组选择                | 是       | 已接入       | Store `switchGroup` 交换当前组投影；缺失时为 A        | 当前组不存在时回退 A，不切换到未知组    |
| 候选池 / 排班             | 是       | 已接入       | Worker/主线程从当前组投影生成事实                     | 不读取另一组历史、值班或人员            |
| 求解模型                  | 是       | 已接入       | 共享规则 + 当前组工作区事实                           | 不读取另一组历史、值班或人员            |
| 补缺 / 覆盖               | 是       | 已接入       | 只写当前组 assignments                                | 不允许跨组找人                          |
| 恢复 / 冷却 / 轮岗        | 是       | 已接入       | 只读取当前组历史和当前组人员                          | A/B 历史不得互相影响                    |
| 值班 / 备勤               | 是       | 已接入       | 按当前组日期和人员保存、读取                          | 不共享值班或备勤事实                    |
| 最终 assignments / Ledger | 是       | 已接入       | 写入当前组工作区并随切组投影                          | 切组不得带入另一组临时班表              |
| 统计 / 历史页面           | 是       | 已接入       | 默认投影当前组                                        | 不做跨组合并统计                        |
| 导入 / 导出               | 是       | 已接入       | 默认导入导出当前组；共享配置单独共用                  | 当前组资质/人员规则合并，保留另一组编号 |
| 页面切换                  | 是       | 已接入       | 切换 `activeGroupId` 前检查 Store dirty 状态          | 有未保存变更先提示保存，取消则不切换    |
| 规则配置                  | 是       | 已接入       | 只修改共享配置，规则改一次两组生效                    | 不在组工作区复制规则                    |

### 第一阶段红灯与不变边界

- 红灯必须证明：旧版本单组状态迁移到 A；B 保持空；A 的历史、人员和值班完整保留；共享配置只保留一份；非法/未知组 ID 不会制造第三组或串组。
- 当前阶段仍不改变：排班算法、候选顺序、岗位完整性、值班规则、KE166 和统计计算口径；领域模块通过当前组投影运行，未新增跨组顶班或跨组统计。

## 展示规则：未达旅客门槛的正常空白不标红（2026-09-20）

- 当前状态：已实现；页面回归覆盖未达门槛、达到门槛、零门槛、半休和 KE166，自动排班门槛直接消费者回归保持通过。
- 唯一事实：航班 `bookedPassengers` 是否达到岗位 `minPassengers`；复用 `schedule-tasks.ts` 的岗位旅客门槛判断，不在 UI 复制比较口径。
- 展示合同：未达门槛且无人时保留可编辑空白格，但不添加通用 `is-unfilled`；达到门槛或 `minPassengers=0` 时，无人继续添加 `is-unfilled`。半休、KE166 和人工越权等专用状态类及说明保持原样。
- 不变范围：不改变任务生成、候选、求解、补缺、恢复、轮岗、最终守卫、人工填人权限、Excel 或 localStorage。
  | 环节                    | 是否适用 | 消费点 / 预期行为                                                    | 验证                        |
  | ----------------------- | -------- | -------------------------------------------------------------------- | --------------------------- |
  | 输入事实                | 是       | 读取现有 `Flight.bookedPassengers` 与 `PositionRule.minPassengers`   | 50/100、120/100、0 门槛三例 |
  | 候选池 / 求解模型       | 否       | 不改任务是否进入自动排班                                             | 既有领域行为保持            |
  | 补缺 / 覆盖             | 否       | 不改空缺生成和补位                                                   | 不修改领域模块              |
  | 恢复 / 轮岗 / 公平      | 否       | 不改后置调换                                                         | 不修改领域模块              |
  | 人工调整                | 是       | 空白格继续保留原输入和拖放入口                                       | UI 结构保持                 |
  | 最终复核 / 守卫         | 否       | 不改变 assignment 状态或安全凭证                                     | 不修改守卫                  |
  | Store / 保存 / 导入导出 | 否       | 不新增字段，不改变持久化                                             | 无数据迁移                  |
  | 页面展示                | 是       | `schedule-grid` 只对已达门槛的普通空缺添加通用红色类；专用标红类不变 | 页面渲染回归                |

## 规则：全航班机动督导独立优先与缺员兼任（2026-09-23）

- 当前状态：已实现；失败测试已锁定非 KE166 航班的独立优先、缺员兼任、安全重排、禁止范围与后置拆组拒绝，完整 `npm.cmd run verify` 已通过；真实页面仍待 owner 验收。
- 通用事实 owner：岗位是否为机动督导只认 `PositionRule.category === "机动督导"`；兼任范围只消费 `mobileSupervisorCoverageRules`，不得按岗位名或航班号猜测。
- 通用顺序：普通岗位模型先完成当天岗位方案；所有机动督导顶部岗随后依次尝试独立安排、同航班允许柜台兼任、现有安全重排后兼任。
- 通用兼任不变量：顶部与柜台必须为同一人员且通过 `supervisorSourceAssignmentId` 关联；同一时段工时只计一次，两个岗位疲劳点分别累计；非法、跨航班或被拆开的关联不得提交。
- KE166 专项继续独立：第二真人容量预留、跨航班重点人员专项、KE166 专属留空文案与督导组连续轮岗不推广到其他航班。
- 失败回退：独立、直接兼任和安全重排都无解时，非 KE166 顶部保留空缺并使用普通空缺告警；不让整张班表失败，不突破硬约束，也不拆已形成的普通岗位方案。

| 环节                           | 是否适用 | 消费点 / 目标                                                                                        | 失败或回退                                        | 回归保护                     |
| ------------------------------ | -------- | ---------------------------------------------------------------------------------------------------- | ------------------------------------------------- | ---------------------------- |
| 输入事实 / 配置                | 是       | `schedule-tasks.ts` 提供通用机动督导分类；`mobile-supervisor-coverage.ts` 继续唯一判定允许与禁止范围 | 无新配置、无名称猜测                              | 分类及默认禁止项测试         |
| 候选池 / 选人                  | 是       | 所有机动督导顶部岗退出普通岗位模型，由收尾复用现有状态、类型、资质、夜班、半休及硬约束候选           | 无独立候选时才进入兼任                            | 非 KE166 独立优先测试        |
| 当天整体模型 / 求解            | 是       | 普通岗位先排；通用机动督导不与模型重复抢人；KE166 可完成性与第二真人容量目标仍只识别 KE166           | 普通岗位模型保持原方案                            | KE166 模型目标回归           |
| 补缺 / 覆盖                    | 是       | 通用收尾先找同航班已排的允许柜台，再调用现有重排把合格督导安全放入允许柜台                           | 找不到安全方案则顶部留空                          | 非 KE166 直接兼任与重排测试  |
| 恢复 / 硬约束                  | 是       | 重排继续消费统一资格、时间冲突、最小衔接、每日工时、结构化严格衔接及 Ledger guards                   | 任一硬约束失败则不采用该方案                      | 禁止范围及硬约束回归         |
| 轮岗 / 公平                    | 部分     | 通用收尾沿用候选优先级；`ke166-rotation-review.ts` 与 KE166 连续督导专项不推广                       | 非 KE166 不进入 KE166 轮岗复核                    | KE166 连续轮岗测试           |
| 人工调整边界                   | 是       | `moveSupervisorWithinFlight`、同步清空、范围校验继续按分类与关联工作                                 | 人工非法兼任拒绝；重新自动排班清除旧结果          | 现有 app/domain 调整测试     |
| 最终复核 / Ledger              | 是       | `daily-schedule-safety.ts` 通用识别合法机动督导兼任；Ledger 与默认 guards 拒绝拆组、跨航班或非法范围 | 拒绝后置改坏提案，保留上一步结果                  | 合法后再拆坏的反向测试       |
| 保存 / 展示 / 归档             | 是       | 复用现有 assignments 关联字段、工时与疲劳投影，不新增 schema                                         | 非 KE166 无解显示普通空缺告警；KE166 专属文案不变 | 工时去重、疲劳累计及告警测试 |
| KE166 第二真人容量             | 仅 KE166 | `daily-schedule-model.ts` 与 `assessKe166DistinctStaffCapacitySnapshot` 保持航班专项判断             | 沿用既有 KE166 回退                               | 现有第二真人回归             |
| KE166 留空 / 轮岗 / 跨航班专项 | 仅 KE166 | KE166 快照 warning、`ke166-rotation-review.ts`、跨航班重点人员目标保持原入口                         | 不推广到普通机动督导航班                          | 现有 KE166 测试全保留        |
| 测试                           | 是       | 先红灯覆盖非 KE166 缺员兼任、独立优先、禁止范围、安全重排、后置拆坏；再跑 KE166 与人工清理回归       | 任一旧专项退化即停止交付                          | 定向测试、完整 `verify`      |

### 写入路径盘点

| 写入路径               | 通用机动督导行为                                               | 最终边界                                                   |
| ---------------------- | -------------------------------------------------------------- | ---------------------------------------------------------- |
| 整体模型初排           | 不生成任何机动督导顶部结果，只生成普通岗位及其他自动岗         | KE166 专属容量目标可影响普通岗位方案，其他航班不新增该专项 |
| 机动督导收尾           | 对全部明确分类的顶部岗执行独立、兼任、安全重排                 | 每次 replace 经过 Ledger guard                             |
| 后置恢复 / 轮岗 / 公平 | 继续按既有管线处理；KE166 轮岗仅处理 KE166 组                  | 不得拆开合法兼任组或制造硬约束违规                         |
| 人工拖拽 / 清空        | 继续复用现有通用机动督导入口                                   | 范围非法拒绝；重新自动排班以新结果覆盖旧人工兼任           |
| 最终安装               | `assertDailyScheduleSafety` 核对每个任务唯一结果与合法兼任关联 | 非法并发、拆组、资质、时间、衔接和工时违规拒绝提交         |
