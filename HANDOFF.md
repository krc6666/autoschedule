# autoschedule 排班系统交接

更新时间：2026-09-20（旅客门槛空缺标红已收窄）

## 一、用户是谁

- 做机场地勤排班系统，自称小白。
- 要大白话，一次说一件事，别堆术语和比喻。
- 自己跑 `npm.cmd run verify`，不让 Agent 跑测试（省 token）。
- 会质疑建议，关心 token 消耗。
- 用户说「不改了」就是不改了，别劝。

## 二、项目

- 机场地勤自动排班，纯浏览器本地运行。
- TypeScript + Vite + Lit + HiGHS 求解器 + Web Worker。
- 数据存 localStorage，Excel 导入导出。
- 路径：`G:\浏览器下载\codex项目\autoschedule`
- 当前分支：`全局化highs`

## 三、排班流水线（核心）

选人 → 排班 → 补人 → 恢复 → 轮岗 → 补漏 → 最后检查

老毛病：前面改了，后面不知道，把结果改回去。

防御：`schedule-guard` 守卫 + ledger 账本 + `history.md` 错题本 + 传播矩阵。

## 四、已完成（近期）

1. TR121/H02 冷却规则（导入导出已修）
2. 上午半休（只影响上午时段，下午/晚上/凌晨照常）
3. KE166 机动督导空缺（黄海莹兼任，不从 CX937 抽人）
4. 规则页开关被代码绕开的问题
5. 同航班人员互斥（可配多对，可指定航班）
6. 测试脚本改造（从零跑、一个月、总耗时、不分月）
7. 两组人员隔离（A组/B组）
8. 引导人员自动填修正（从岗位链最下面选；排除的是行政支援人员，不是行政支援岗位）
9. 岗位空缺标红（统一红底红边线）
10. 跨航班重点人员优先（状态版本升到 v7）
11. 空缺标红收窄：未达「启用旅客人数」门槛的正常空白不标红；已启用仍无人继续标红。唯一门槛判断：`meetsPassengerThreshold()`。页面人工验收未做；完整 `npm.cmd run verify` 未跑。
12. 历史编辑期间允许只读导出（Excel/HTML/PNG）；重新排班等写命令仍禁止。

## 五、两组人员隔离

- 需求：两组人上一休一，共用一套规则，数据完全隔离。
- 结构：`AppState = shared + groups.A + groups.B + activeGroupId`
  - `shared`：规则、航班模板、岗位配置（只一份）
  - `groups.A` / `groups.B`：各自的人员、班表、历史、值班、备勤
- 状态版本 5 → 6 → 7，旧数据迁移到 A 组，B 组为空。
- 严格隔离，不考虑顶班。
- 页面顶部有 A组/B组 切换；未保存时切组会提示。
- 导入导出默认当前组；岗位资质导入只替换当前组，保留另一组。
- 跨组人员编号禁止重复。
- 用的是「兼容投影」过渡方案：底层读老字段，Store 负责把当前组投影写进去。以后要逐步收口。

## 六、跨航班重点人员优先（最新，v7）

- 匹配对象改为「航班号 + 人员编号」。
- 页面从上到下决定同一人员的航班优先级。
- 人员在优先航班内部换岗位仍受保护。
- KE166 兼任督导后额外保留一名常规人员的绝对优先逻辑保留。
- A/B 组只显示和编辑当前组人员，另一组选择不会被覆盖。
- Excel、localStorage、人员改号/删除、反馈、HiGHS 求解和后置守卫均已接入。
- 迁移冲突采用保守方案：旧 v6 的「岗位选择」无法可靠推断成「人员选择」，升级 v7 后保留规则 ID、航班、启用状态，但清空人员选择。升级后需重新勾选人员，不会偷偷猜测映射。

## 七、历史记录界面（正在处理）

- 历史编辑采用临时草稿、禁止重新排班、取消后恢复原当前班表：这是明确设计（`spec.md` 有写）。
- 历史编辑时连导出也被挡住：这是 bug，白名单 `COMMANDS_ALLOWED_DURING_HISTORY_EDIT` 拦得过宽。已给提示词要修（把 `export-schedule` / `export-share-html` / `export-share-png` 加进白名单）。
- 取消编辑后直接退回排班页：这是设计，测试有断言，但跟「保存后回历史页」不一致。
- 用户想在历史日期上重排某一天的班：属于要改 `spec` 合同的新能力，风险高，还没做。

## 八、行政支援岗位 + 引导人员（已理清）

- 常规人员够 → 不往行政支援岗位填行政支援人员，由常规人员顶上。
- 常规人员不够 → 行政支援岗位空出来，留用户手动填。
- 引导人员判断依据是「人是不是行政支援人员」，不是「岗位是不是行政支援岗位」。
- 行政支援人员永远不能当引导。
- 行政支援岗位上是常规人员 → 这个人优先当引导（最高优先）。
- 否则按老规矩：整条岗位链最下面那个岗位来选。

## 九、测试脚本

- 路径：`G:\浏览器下载\autoschedule-real-two-week.mjs`
- 跑法：

```powershell
cd "G:\浏览器下载\codex项目\autoschedule"
node "G:\浏览器下载\autoschedule-real-two-week.mjs" --zero --excel "G:\浏览器下载\排班工具配置 (25).xlsx" --start 2026-09-21
```

- 默认 15 个工作班（约一个月），间隔 2 天。
- 能测：每天成功/失败、待补位、耗时、末班重点岗位次数。
- 测不了：Worker 启动、页面渲染（浏览器层的事，另需 Playwright 测试）。

## 十、关键教训（`history.md` 有记）

- 脚本结果和浏览器不一致时，先怀疑脚本口径，别怪算法。
- 独立脚本要复用浏览器公共入口，别自己另写一套。
- Agent 说「能存」要验，规则页改了要导出再导入试。
- Agent 改代码前先问「规则页有没有现成开关」。
- Agent 说「冲突」时让它举具体例子，举不出来就是它错了。
- 改规则要小步改，别断裂式重构。
- 报警提示文字要用大白话，逐条列，别合并、别用术语。
- 不让 Agent 自己开浏览器复现，让用户手动复现，Agent 查代码。
- 判断依据要落到「人」还是「岗位」，别混（引导人员那次就是混了）。

## 十一、当前待办

1. 旅客门槛空缺标红：代码已改，待浏览器看一眼（50/100 不红；120/100 无人红；门槛 0 无人红）。完整 `verify` 未跑。
2. 历史日期重排能力：要改 `spec`，未做。
3. 两组隔离功能，还没在浏览器人工验收（切A组、切B组、导入导出、未保存提示）。
4. 兼容投影是过渡方案，以后要逐步收口到显式读当前组。
5. 跨航班重点人员优先（v7）还没人工验收，重点验「升级后人员被清空但规则还在」。
6. 一堆改动没 commit、没 push。
7. 岗位上「最小间隔」那条规则（CX931→TWB616）还没确认，可能是配置问题也可能是 bug。

## 十二、协作方式

- 给用户的指令用代码块包起来。
- 不要通读大文件，用关键词搜索定位。
- 一次说一件事，别一口气塞太多。
- 不用复杂比喻。
- 用户问「这是什么意思」时，用最直白的话解释。
- 用户经常新开窗口，需要交接文档。
- 用户说「不改了」就是不改了，别劝。
