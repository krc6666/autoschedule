# autoschedule 窗口交接入口

更新时间：2026-08-13

## 新窗口第一步

当前项目目录：`G:\浏览器下载\codex项目\autoschedule`

请先完整阅读以下文件，再回答或修改代码：

1. `AGENTS.md`
2. `spec.md`
3. `.agents/skills/autoschedule-dev/SKILL.md`
4. `plan.md`（如果本次任务涉及当天排班求解、120 秒限时或 HiGHS）
5. 本文件 `HANDOFF.md`

新窗口可以直接使用这段话开始：

> 请先阅读 `G:\浏览器下载\codex项目\autoschedule\HANDOFF.md`，再检查当前 `git status --short`。不要回滚工作区已有改动，不要 commit、push 或部署。先复述当前已完成内容、未验证风险和你准备处理的问题；需要修改时沿用现有模块边界并先写失败测试。

## 当前已完成的主要工作

### 1. 当天排班 120 秒限时和恢复公平近似优化

- 所有硬约束、岗位完整性、特殊锁定、人工结果、跨工作日资质预留、结构化严格岗位衔接和配置的重点岗位优先仍需严格满足。
- 低优先级恢复、早下班、普通衔接、覆盖、负荷、疲劳和历史公平目标在总时限内尽量优化。
- best-effort 目标通过显式 `SolverPort` 合同传递 5% 相对 MIP Gap，不按“最后一层”、数组下标、航班号或姓名硬编码。
- 每层在容差内完成后按当前实际值锁定，再进入下一层；总 deadline 到达后不再进入更低层。
- 只有 required 目标完成、存在完整可行解、物化后安全复核和最终收尾复核都通过，才可以采用限时结果。
- required 超时、无完整解、模型错误或安全复核失败，仍然失败并保留原班表。
- HiGHS 适配层已读取官方 `mip_gap`、`mip_dual_bound`、primal solution status，并在每层开始前清零官方时钟。
- 增加了通用 `high-load-independent-capacity` 有效不等式；不能证明安全时会跳过，不写死人员、航班或岗位。

### 2. 跨航班重点岗位优先修复

用户遇到的典型问题是：上一班承担重点岗位的人，本班又被放进重点岗位；系统过去只要看到有人被调离，就直接保护原姓名，即使存在轮换质量相同或更好的合格替代人。

现在的逻辑是：

- 比较原人员和替代人员在同一“航班号 + 岗位”的本月次数；
- 次数相同时比较最近工作班次数和连续承担次数；
- 替代人员次数更少，或次数相同且连续承担不更差，允许整体换位；
- 替代人员轮换更差时，才保留原人员；
- 仍严格执行人员状态、岗位资质、夜班、时间冲突、90 分钟衔接、工时上限、锁定、岗位完整性、跨工作日预留和最终安全复核；
- 没有写死 `CX937/G18`、`KE166/H02` 或任何真实姓名，所有配置重点岗位都适用；
- 决策说明使用普通业务语言，不再暗示当前姓名不可替换。

关键文件：

- `src/domain/rules/cross-flight-priority.ts`
- `src/domain/reviews/rotation-review-safety.ts`
- `src/domain/kernel/daily-schedule-result.ts`
- `tests/domain/cross-flight-priority.test.ts`
- `tests/domain/daily-schedule-optimizer.test.ts`

### 3. 其他已存在的工作区改动

工作区中还包含停止排班、限时安全结果、排班原因分析、规则配置导入导出、分流岗位、页面交互和 HiGHS 本地适配等 owner 已有改动。它们不是本交接文件要回滚或重做的内容。

## 已验证结果

最近一次完整验证：

```powershell
npm.cmd run verify
```

结果：通过。

- 类型检查通过；
- 常规测试 83 个文件、561 项通过；
- 性能测试 3 个文件、12 项通过；
- 生产构建通过；
- `git diff --check` 通过；
- `rg -n "\[DEBUG-" src tests scripts` 无结果。

重点回归测试包括：

- 配置重点岗位存在同频替代人时允许换位；
- 替代人轮换质量更差时仍阻止换位；
- 上一班重点岗位人员通过真实恢复重排流程被自动换下；
- 换位不制造空缺；
- 同航班内部安全交换仍允许；
- 不重叠航班不触发跨航班保护。

## 真实 8 航班验收记录

工作簿：`G:\浏览器下载\排班工具配置 (7).xlsx`

日期：`2026-09-29`，额外加入模板航班 `AK151`。

连续 3 次均返回安全完整班表，结果均为约 120 秒限时采用：64 个岗位，2 个业务空缺，工时差 1.75 小时，疲劳差 10.5 点。没有因为低优先级目标无法证明绝对最优而丢弃班表。

尚未验证真实 9 至 10 航班场景；继续增加航班时应优先记录耗时、是否有完整解、岗位数、空缺、工时差和疲劳差，不要先放宽硬约束。

## 当前工作区和操作边界

- 工作区有大量 owner 或此前任务的未提交修改，禁止批量恢复、清理或覆盖。
- 不要 commit、push、部署，除非 owner 在当前窗口明确授权。
- 排班规则变化必须先核对 `spec.md` 的规则合同和优先级；实现不得直接堆进 `scheduling-kernel.ts`。
- 新行为先写失败测试，再实现，再运行相关测试和 `npm.cmd run verify`。

## 快速启动工具

项目已提供：`start-autoschedule.bat`

双击它即可启动开发服务并打开浏览器；如果启动失败，黑色窗口会保留错误信息。也可以在项目目录运行：

```powershell
python dev.py
```

以终端实际显示的 `http://127.0.0.1:<端口>/` 为准，不要固定使用旧端口。

## 新窗口推荐开场方式

不要重新粘贴长聊天记录。只发送：
